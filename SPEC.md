# Guardian が満たすこと / 満たさないこと(要件)

Guardian は、**AIと一緒に開発するときの「静かに壊れる」を機械で見張る塊**です。
Node の標準機能だけで動き(外部依存ゼロ)、どのプロジェクトにも1行で入ります。
この文書は**要件** ── 何を満たし、**何を満たさないか**を書いています。

★**なぜ注意や規約ではなく機械なのか**は [METHOD.md](METHOD.md) に、
  **その根拠になった事故の記録**は [WHY.md](WHY.md) にあります。
  この文書だけを読むと「規約に書けばいいのでは」と思われますが、それは最初に否定した道です。

---

**この文書は機械が見張っている。**新しい口(`--xxx`)やエンジンのファイルを足したら、
ここに書くまで `selfcheck.mjs` が赤くなる ── 「更新すること」と注意書きするのではなく、
**更新しないと通れない**ようにしてある(RULES.md 46条: 注意書きは検査ではない)。

---

## 1. 解く問題

対象は「人がAIに指示して作らせる進め方」。速いが、放っておくと必ず同じ壊れ方をする。
Guardian が相手にするのは、**LLMが構造的に持つ弱点6つ**(詳細は [METHOD.md](METHOD.md)):

①最小の変更を好む / ②文章の規則を守れない / ③「動いた」で満足する /
④見えない失敗を作る / ⑤知識が古い / ⑥人に確認を投げる

★これらは**注意や気合では直らない**。同じ日に同じ規範を破った実例が [WHY.md](WHY.md) に残っている。
だから**機械が止める**という形以外を採らない。

---

## 2. 満たすこと(要件)

| # | 満たすこと | どう確かめるか |
|---|---|---|
| R1 | **完了を名乗る手前で止まる** | `verdict.mjs` が4語(通過/差戻/注意/不明)に落とし、差戻なら `hooks/stop.js` が止める |0
| R2 | **測っていないものを合格と言わない** | 証拠が無ければ「不明」。不明は出口2で、緑に数えない |
| R3 | **禁じた語は書く手前で拒否する** | `hooks/no-fixed-names.js` がファイルに届く前に止める。禁止語は門が持たず宣言から読む |
| R4 | **同じ意味の値が2箇所でずれたら落ちる** | `check.mjs` の7種の検査(same/sameMap/sameSet/shape/onlyIn/noInline/callArgs) |
| R5 | **修正の2つ外側に答えるまで完了できない** | `neighbors.mjs --gate`。全項に「触れた/影響なし/報告」+理由が要る。回答はコミットに残る |
| | ★近傍の辺は4種 | ①記号の定義→参照 / ②その呼び出し元 / ③ノートの欄→読み手 / ★**④子として回す(spawn)と、それを名指しする案内**(2026-09-01。出口の意味を変えたのに読み手を3人が3回 落とした事故から) |
| R6 | **検査そのものが生きていることを示せる** | `selfcheck.mjs` が見本を建て、1箇所ずつ壊して「壊したら赤/壊さなければ緑」を確かめる |
| R7 | **地図(CODEMAP)と実装のずれが分かる** | `check.mjs` の実在照合 + `audit.md` の定期監査(4観点) |
| R8 | **鳴りすぎない** | 催促は「前回から3時間以上」**かつ**「その後に実装が変わった」の両方。止まっている間は鳴らない |
| R9 | **どの現場へ持って行っても同じ中身** | エンジンは特定の宛先を1つも持たない(`selfcheck` が見張る)。固有は宣言へ |
| R10 | **配った先の直りが、元へ還る道がある** | `ENGINE_FP` がファイルごとに照合し、`--report` が渡す1枚を作る。**人の承認を挟む** |
| R12 | **配った先の【事故】が、元へ還る道がある** | `WHY_SEEN`(配られたとき何があったか)と突き合わせ、`--why` が足された事故だけを1枚にする。**個人情報が混ざっていれば作らない**。送信は `--send` を明示したときだけ |
| R13 | **逃げ道(guardian:ok)が黙って増えない** | 理由の無い逃げ道は落とす。数は宣言の `okMax` と突き合わせ、**増えたら落ちる**(`--tighten` で下げるラチェット) |
| R14 | **どれだけ測れたかが分かる** | 合否が**測れた割合**を出す。前回より下がったら「宣言が実装に追いついていない合図」と言う。★割合は**判定に使わない**(表示だけ) |
| R15 | **事故が増えても、読む量が増えない** | `index.mjs` が重み付きの索引を作る。**◎(構造の芽)だけが全部読む価値がある** ── ○(型)と・(個別)は関係するものだけ。全文は WHY.md に残す |
| R16 | **配るものと、その現場のものを取り違えない** | 正本には両方が同居する。`pull.mjs` は【配るもの】と【配らないもの】を**両方**宣言し、**どちらにも無いものが1つでもあれば取らずに止まる**。`selfcheck` が同じ宣言を読んで3方向を照合 ── 未分類が無いか / エンジンが配る側に在るか(**欠落**) / `install.mjs` が作るものが配らない側に在るか(**混入**)。★一方だけのリストは、必ず逆向きの事故を引き受ける |
| R17 | **塊のフォルダを、プロジェクトの根と取り違えない** | 根を探す判定は、塊らしいフォルダ(エンジンを持ち `.git` を持たない)で止まらない。`selfcheck` が見本を建てて実測する。★取り違えると導入もフックも別の場所を見て、**しかも成功と報告する** |
| R18 | **「もっと良く出来ないか」を問う側がある** | [hunch.md](hunch.md) の2段(違和感→サーチライト)。★他の要件が全部**ブレーキ**(止める・緑にさせない・戻らせない)なのに対し、これだけが**アクセル**。★**これは自発性ではない** ── 引き金は必ず外(人が呼ぶか、完了の瞬間)。★宣言の `context`(運用の文脈)が空なら**運用の欠落は永久に出ない**ので、`selfcheck` が「見ていません」と言う |
| R19 | **重複していて正しい場所を、誤検出にしない** | 走査(`--sweep`)のコーパスから外す場所を宣言が持つ(`neighbors.skip_dirs`)。★**門(`--gate`)には効かない** ── 外した場所を編集すれば近傍は問われる。実測: 実験の見本を足した日、写経の疑いが12→46件になり、増えた34件は全部が意図した重複だった |
| R11 | **この文書が実装から遅れない** | 下の「口の一覧」を `selfcheck` が実装と突き合わせる(この文書自身の要件) |

---

## 3. 満たさないこと(境界)

**ここに書いてあることは、Guardian の仕事ではない。**期待されて裏切るより、先に言う。

- **コードが正しく動くかは見ない。** 単体テストでも型検査でもない。見るのは「**宣言と実装がずれていないか**」だけ
  ★**ただし、そのプロジェクトが持つ test / e2e を【証拠】として合否に使うことはできる**
  (宣言の `evidence` に並べる)。Guardian 自身は実行時の正しさを**判定しない** ──
  他人が出した合否を**集めて4語に落とすだけ**である。
- **実行時のバグは見ない。** 落ちるのは書いた形の矛盾であって、動かした結果ではない
- **プロンプトやAIの出力の質は見ない。** 相手がAIかどうかに関係なく、残った成果物だけを見る
- **セキュリティ検査ではない。** 秘密の混入は宣言した語しか見張らない(`private`)
- **性能は見ない。**
- **勝手に直さない。** 死にコードも重複も**列挙して報告する**まで。消すかは人が決める(監査の義務2)
- **列挙は判定ではない。** `--sweep` の死にコード候補には誤検出が混じる(export の無い古典スクリプト等)
- **他の開発ツールでは、自動で鳴る仕掛けが【3本まで】。** 検査・門・合否・監査は動く。
  `githooks/pre-commit` を有効にすれば(`git config core.hooksPath guardian/githooks`)、
  **コミットの手前で合否が回り、差戻なら止まる**。★ただし**編集の直前に地図を差し込む**(codemap.js)と
  **応答の直前に監査を催促する**(clock.js)は**代替できない** ── その瞬間が git に無い。
  ★また pre-commit は Claude Code のフックより**一段遅い**(AIは既に「できました」と言い終えている)。
- (旧) フックとスラッシュコマンドは
  Claude Code 固有。無い環境では導入時に**手で回す手順を出す**(本体は1つも減らない)
- **無人の定期実行は付いてこない。** リポジトリに置けるのは呼び出し口まで

---

## 4. 動作の前提

- **Node のみ**(外部パッケージゼロ・`package.json` なし・`npm install` 不要)
- **git があると門が働く**(差分から近傍を出すため)。無くても検査・合否は動く
- **git があれば正本から取り直せる**(`pull.mjs`)。正本は**公開**なので**アカウントも認証も要らない**
- OS: Windows で常用、**Linux(GitHub Actions)でも実際に回っている**

---

## 5. 口の一覧(★機械が実装と突き合わせる)

**新しい口を足したらここに足すこと。**足さなければ `selfcheck` が落ちる。

| ファイル | 口 | 何をするか |
|---|---|---|
| `check.mjs` | `--root` | 別のリポジトリを外から診る |
| | `--口一覧` | **この道具が知っている口**を1行1件で出す(機械が読む口)。★SPEC の表とこの出力を selfcheck B11 が突き合わせる ── 綴りではなく**答えさせて**測る |
| | `--片方だけ` | **片方しか機械に見えていない項**を全部1行1件で出す(機械が読む口)。★数えた道具から取り出せないと、直す人が式を推測して数え直すことになる |
| | `--名指しされていない` | この現場の実装のうち、**地図が名指ししていない**ものを1行1件で出す(機械が読む口)。★A5 が出す数の中身 ── 数だけを出すのは「直せ」と言ってどこを直すかを言わないのと同じ |
| | `--宣言の外` | **どの宣言にも入っていないコードのファイル**を1行1件で出す(機械が読む口)。★宣言に無いものはどの検査にも掛からず、**全部が緑でもそこは見ていない** |
| | `--呼び先が地図に無い` | 地図に載っているファイルが **import / require している先**のうち、地図のどこにも載っていないものを1行1件で出す(呼び元→呼び先)。★**項も実名もファイルも在るのに接点が足りない**形は、A4 も A5 も出さない |
| | `--逃げ道の理由` | 逃げ道(`guardian:ok`)を**場所 ← 理由**で1行1件出す。あわせて素の走行でも**同じ理由が並んでいる数**を出す(【いちばん多い理由】/【2件以上ある理由の合計】・全文一致)。★**検査は「理由が在るか」しか見ていない** ── 近傍の門と同じ穴なので、数だけ出す。落とさない・止めない |
| | `--冷たい順` | 場所を**最後に触った日**で並べて出す(日数→場所・タブ区切り)。あわせて素の走行でも**冷たい上位3つと、いちばん熱い1つ**を出す。★**冷たい=悪いではない**(触る必要が無い所も在る)── ★★判定にも上限にもしない。★★★実測: 今夜の本物は【冷たい所】と【直した直後】から出た |
| | `--予想 "<文>"` | ★**予想の欄**。いまの数と一緒に予想を `.guardian/予想.json` に置く。次に素で走らせると【そのときの数】と突き合わせ、**動いた行だけ**出す。★当たれば何も出ない ── **予想は、外れたときにだけ材料になる**(実測: 6回書いて3回外れ、そのうち2件が本物になった)。落とさない・止めない |
| | `--selectors` | 画面のセレクタ照合だけを回す |
| | `--tighten` | 口が減っていれば許容(max)を実数まで下げる(一方通行) |
| `selfcheck.mjs` | `--tighten` | 守りの件数の下限を上げる(ラチェット) |
| | `--口一覧` | この道具が知っている口を1行1件で出す(機械が読む口) |
| | `--stamp` | エンジンの指紋を押し直す(意図した変更のとき) |
| | `--判定` | ★**構造化された【判定】を JSON で出す**(25.0)── `{ 対象, 状態, 根拠, 走査集合, 件数, 正本由来 }` の並びと、その勘定。★★`ok/ng/未測` の件数は【押した回数】であって判定の数ではない ── 実測: `ok.push` 61件のうち大半は「やった事の報告」だった。★★★通していない押しは `未仕分けの押し` として数え、**暫定であることを数に出す**。`正本由来` は緑の必須フィールド(`null` = その検査には要らない) |
| | `--report` | 直した分を、正本へ渡す1枚にまとめる(配布先で回す) |
| | `--changed` | 指紋が食い違ったファイル名だけを1行1件で出す(機械が読む口)。★`pull.mjs` がこれを受け取り、正本の中身と突き合わせて【この現場の直り】と【古いだけ】を仕分ける |
| | `--sha` | この塊を**どの中身から取ったか**(取り直しの受領証の SHA)を1行で出す。無ければ空行。★「どれを取ったか」と「どれを測ったか」を機械で照合するための口 |
| | `--receipt` | **受領証**を1回で返す(JSON: 取得元SHA / いまの中身の digest / 合否)。★`--sha` は履歴でしかなく、取ったあとに1本壊れても同じ値を返す ── 読み取りと合否を**別のコマンド**でやると別時点の結果を結び付けられるので、原子的に返す。赤=出口1 / 未測あり=出口2 |
| | `--why` | **その現場で足した事故だけ**を1枚にまとめる(配布先で回す)。個人情報が混ざっていれば**1枚を作らない** |
| | `--send` | `--why` と一緒に使うと、その場で Issue にして送る(**人の承認の表明**。既定は送らずコマンドを見せるだけ) |
| | `--dry` | `--send` と一緒に使うと、**送らずに**宛先・題・バイト数・見張った語数だけを出す。★外向きで不可逆な口には、必ず下見の口を対で置く(この道具で実際に公開 issue を立てた事故から) |
| `neighbors.mjs` | `--gate` | 近傍に全部答えたか(答えていなければ出口1) |
| | `--list` | 修正の【2つ外側】を出し、回答の下書きを書く(口を書かないときの既定でもある) |
| | `--口一覧` | この道具が知っている口を1行1件で出す(機械が読む口) |
| | `--base` | 比べる範囲を指定する |
| | `--escaped` | 事故が門を逃したとき、犯人が何環目に居たかを測る |
| | `--sweep` | 全体の棚卸し(死にコード候補・写経の疑い・参照の集中点) |
| | `--定義一覧` | この現場の**定義**を1行1件で出す(ファイル→名前→種類・機械が読む口)。★定義の拾い方はここが正本 ── `check.mjs` が写さずに**答えさせる**(39条) |
| | `--跨ぐ記号` | **ファイルを跨いで使われている記号**を1行1件で出す(名前→何ファイル→定義した場所→**書き出しているか**)。★接点の入口を数えるための**物差し**(検査ではない・赤にしない)。★2列目の数は**その名前が【現れる】ファイルの数**(定義していなくてよい) ── `--sweep` の写経の疑いは**【関数として定義している】ファイル**を数えるので、★★2つは食い違う。**食い違いは、別の書き方で同じことをしている所の手掛かり**になる |
| | `--root` | 別のリポジトリを外から診る |
| `verdict.mjs` | `--fast` | 自足する証拠だけ回す(飛ばしたものは名前を出す) |
| | `--口一覧` | この道具が知っている口を1行1件で出す(機械が読む口) |
| | `--json` | 結果を機械向けに出す |
| | `--root` | 別のリポジトリを外から診る |
| `install.mjs` | `--dry` | 何が起きるか見るだけ |
| | `--口一覧` | この道具が知っている口を1行1件で出す(機械が読む口) |
| | `--hooks` / `--no-hooks` | フックを入れるかを明示(既定は現場を見て自動判定) |
| `pull.mjs` | `--check` | 取らずに、正本と何が違うかだけ見る |
| | `--口一覧` | この道具が知っている口を1行1件で出す(機械が読む口) |
| | `--distributed` | **何を配るか**の一覧を1行1件で出す(ネット不要・機械が読む口)。★`selfcheck` がこれを回して指紋の対象と突き合わせる ── 検査の側で歩き方を写すと門の退行を測れないため(44条の双子) |
| | `--force` | この現場の直りを**上書きして**取り直す(何が消えるかを先に出す)。★前回の取り直しが途中で終わった現場の出口 ── 自分自身を更新する道具は、更新の1回目には古い自分で走るため |
| | `--降格を承知` | ★**版が下がる／配ると宣言した物が消えるのを承知で取る**(24.0)。★★`--force` から分けた ── 直す前は1つの口が2つの決定を運んでいて、「途中で終わった取り直しを直したい」人が打つと、版が 23.x → 13.24 へ戻り `外す.mjs` が消えた。★★★2つ要るなら2つ打つ。通すときも【何が消えるか】を名前でもう一度出す(二重の警告) |
| | (知らない口) | **その場で出口1**。黙って無視すると、`--at X` を打った記憶のまま main を測って「X を測った」と報告することになる(測定契約が2回目で崩れる) |
| | `--at <SHA>` | **その中身を指して**取り直す(既定は main)。★測定依頼を契約にするための口 ── 依頼後に main が進んでも同じ物を再現できる。指定と実際に取れた SHA が違えば配らない。★穴: 任意の古い SHA を許すので、既知の危険版も取れる |
| | `--由来` | ★**配る一式の由来を出す**(22.0)── 形 / 作った時刻 / 版 / commit / ★★作業木が汚れているか / 出どころ / 件数 / 一式(rel + **SHA-256** + バイト + `commitと同じ`) / ★★★`commit と違うバイト`(件数・道・測れなかった数)。★指紋は【作業木のバイト】であって commit の中身ではない ── `git status` が綺麗でも一致しないことが在る(23.8、実測18件)ので、件ごとに突き合わせて名前で出す。★★★外部監査へ渡すのは【物】だけで、弱点も既知の赤も渡さない。指紋が SHA-256 なのは、外の人が同じ値を出せるようにするため(塊の内部で使う FNV とは別) |
| `外す.mjs` | (引数なし) | ★**下見**(既定)── 読むだけ。何も消さない |
| | `--dry` / `--check` | ★★下見の【別名】── この塊は他の道具でそう使っているので、人はそれを打つ。知らない口として拒まず、下見として効かせる |
| | `--外す` | 実際に外す |
| | `--走査` | 台帳が無い現場に当てる(消さない。候補と証拠を出す) |
| | `--root <道>` | 別の現場を見る(★走査でだけ) |
| | `--json` | 機械向けに出す。★★見分けは `mode`(打った口そのもの)と `schema` ── どちらの JSON も同じ鍵を持つ(24.5)。`版`/`形` は人が付ける名前なので機械の見分けに使わない。走査は `profileCoverage: not-collected` も出す(★★★版ごとの道を集める仕掛けは無い、と言う) |
| | `--名指しで消す <一覧>` | ★**台帳が無い現場から、人が名指しした物だけを消す**(27.0、依頼主の依頼)。`--走査` の中でしか使えない ── ★★走査が見た集合の外は消さない(打ち間違いで別の物を消させないため)。一覧は1行に1つ、`#` で始まる行は覚え書き。★★★消す前に【何を・どの証拠で】消すかを出し、消した物を `.guardian/名指しで消した.jsonl` に**追記**する(★1行1走行・27.2)── ★★上書きすると、2回目の撤去で1回目の記録が消える(git の無い現場では戻せない)。受け取り自身は**予約名として断る**(履歴が消えるため)。出口: 0=名指し全部 消した / 1=断った物か消せない物が在る / 2=一覧が読めない。★この口は**現場に何も残っていないとは言わない**(走査の外は見ていないため) |
| | `--口一覧` | この道具が知っている口を1行1件で出す(名前・値の個数・mode・別名) |
| | (知らない口 / 値なし / 同じ口が2回 / mode が2つ / その mode で使えない口) | ★★★その場で**出口3**。0/1/2 は判定で埋まっている。★「無害だから通す」にしない ── 通すと、打った人の意図が黙って捨てられる |
| `index.mjs` | (口なし) | WHY の索引(重み付き)を作る |
| | `--口一覧` | この道具が知っている口を1行1件で出す(機械が読む口) |
| | `--check` | 作らずに、索引が古くなっていないかだけ見る |

### フック(口を持たない ── 標準入力を読んで標準出力に返す。呼ばれ方で働く)

| ファイル | 何を満たすために在るか |
|---|---|
| `保持一覧.json` | ★**機械で導けないから残す物**を、理由付きで宣言する(19.0)── 塊が名指しするが書かない物(`.guardian/踏んだこと` / `audit_at`)。★★現場は `guardian.config.json` の `retain` で **足せる**が、既定から**引けない**(引けると間違いが「消える」側=不可逆に落ちる)。★★★受け付けない指定(広すぎる / 予約 / 理由なし / 引く / 壊れた形)は、言うだけでなく **UNKNOWN** にする |
| `道.cjs` | ★**git から道の一覧を取る唯一の口**(18.0)── ★★道は【行】では割れない。`-z` を下位命令の直後に置き、Buffer で受けて NUL バイトで割り、1件ずつ fatal な UTF-8 復号をする。★★★trim も正規化もしない(道は来たままが正しい)。保存則: 発見 = 読めた + 読めなかった。門は `selfcheck.mjs` の B15 |
| `書き手.cjs` | ★**現場へ書く唯一の口**(17.0)── 走行中に増える物と、作った親フォルダを【書いた事実】で台帳に載せる。★★塊が自分で書き換えた物は指紋も直す(人のせいにしないため)。★★★指紋と台帳の道の正本もここに在る(B14 が写しを赤にする) |
| `台帳.mjs` | ★**外すときのために、入れたときを残す** ── 置いた物を「根の種類(TARGET/BUNDLE)・相対名・指紋・書き手」で記録する。★★台帳に無い物は【残す】(allowlist)。★★★保存は積む(上書きしない ── 一度目が外すときの基準) |
| `外す.mjs` | ★**外す**(★★素の走行は【下見】で、何も消さない ── 消すのは `--外す`)── 台帳(全走行の合併)と実物を突き合わせ、「作った かつ 変わっていない」物だけ外す。★★★台帳に無い物は消さない(allowlist)。retained は【走査 − 消した物】で作る。出口 0=PASS / 1=CONFLICT / 2=UNKNOWN / ★3=引数の誤り。口の一覧は上の【口の表】に在る |
| `pull.mjs --由来` | ★**配る一式の由来を出す**(22.0)── 中身は上の【口の表】に書いた。★★同じ説明を2箇所に書かない(写しは古くなる) |
| `外す.mjs --走査 [--root <道>] [--json]` | ★**台帳が無い現場に当てる**(20.0 / 21.0)── ★★消さない。証拠を【足して】出す(★8型 ── 強い順に 雛形と一致 / 塊が置く道(中身は違う) / 塊が置く道 / 印 / 昔の置き場 / 命令 / 場所だけ / 名前だけ)。★★★排他にしない ── 排他だと、育てられた `docs/CODEMAP.md` のように**証拠が隠れて丸ごと消える**。終端の保存則(見た数 = 候補 + 証拠なし + 読めない)を毎回 出す。`場所だけ` `名前だけ` は所有の証明ではなく、`証拠なし` は「塊の物ではない」証明でもない。出口は常に 0 で、決めるのは人 |
| `hooks/stop.js` | R1(完了を名乗る手前で止まる)── 差戻のときと、★**測れなかったとき**に止め、理由を返す。測れなかったに入るのは: 合否が回らない / 出力が読めない / ★★設定が在るのに読めない / ★★★門の中で例外(24.2)。**不明は合格ではない**。★★★不明のときは止めないが、`decision: "continue_with_unknown"` と 件数・内訳を**機械可読で返す**(26.11)。★また、印より新しい実装が無くて【合否を回さない】ときは `decision: "skipped_unchanged"` と `測った: false` を返す(26.13)── ★★黙るのと、測らないのは別── 人向けの文だけだと、読み飛ばす機械が不明を緑として受け取る |
| `hooks/no-fixed-names.js` | R3(禁じた語を書く手前で拒否)── 禁止語は持たず宣言から読む |
| `hooks/no-reflex.js` | R3の系 ── 器のファイルに反射的な書き込みをさせない門 |
| `hooks/codemap.js` | R7(地図と実装のずれ)── 編集の**直前**に地図の該当項を差し込む |
| `hooks/clock.js` | R8(鳴りすぎない)── 時刻を注入し、**変更で駆動する**催促を出す |
| `hooks/in-loop.js` | R1の系 ── ★**会議の輪に居るのに wait を呼ばずにターンを終えるのを止める**。`.guardian/輪.json` が在る間だけ働き、無ければ素通り。★★**配布一覧には入れていない**(会議の現場でしか意味が無い) |
| `hooks/lib-root.js` | 上の本数ぶんが共通で使う ── 置き場所に依存せずリポジトリの根を探し、設定を読む。★設定は【無い】と【壊れている】を分けて返す(24.2)。★★【見る所】の既定値の正本もここ(24.8)── 直す前は3箇所に在って値が違い、server/ は門が見るのに地図が来なかった |
| `githooks/pre-commit` | **Claude Code でない道具のための代替**(3本ぶん)。コミットの手前で合否を回し、差戻なら止める。有効化: `git config core.hooksPath guardian/githooks` |

★フックは **Claude Code 固有**(3節の境界を参照)。他の道具の現場では入らないが、
  中身はただの Node スクリプトなので、フック相当の仕掛けがあれば同じように呼べる。

---

## 6. 検証済みの事実(主張ではなく実測)

- **塊の自己検査** ── 見本を1箇所ずつ壊して、赤くなることを確かめている(件数は `selfcheck` が出す)
- **`.claude` を丸ごと消した現場**で、検査・自己検査・門・合否・全体走査が全部動いた
- **Linux(GitHub Actions)**で夜間検査が実際に成功している
- **配布先で塊を直したとき**、どのファイルかを名指しで検出できることを実地で確認
- **フォルダ名に空白がある現場**で導入が通ることを確認(直す前は壊れていた)

---

## 7. この文書の扱い

**新しい機能を足したら、ここを更新する。**そして更新は注意ではなく検査で守る:

- 口(`--xxx`)を足したら、**5節の表に足すまで `selfcheck` が赤くなる**
- エンジンのファイルを足したら、**この文書のどこかで名指しするまで赤くなる**
- 満たさないことが変わったら、3節を直す(★ここは機械には測れない ── **人が書く**)

★3節「満たさないこと」だけは機械が見張れない。
**できることは数えられても、できないと言い切ったことは数えられない**からである。
だからここは、監査(`audit.md`)のたびに人とAIが読み直す。

## `pull.mjs` の出口 ── 全17件の棚卸し(26.9、2026-09-03)

★@codex(13:43)「pull の他16出口を含め、**出口ごとの意味を全件棚卸ししてから**変更してください」。
★★**変えていません。まず並べました。**

| 行 | 出口 | いつ | 種類 |
|---|---|---|---|
| 70 | 1 | 口に渡す値が足りない | 引数の誤り |
| 79 | 1 | 知らない口 | 引数の誤り |
| 90 | 0 | `--口一覧` | 問い合わせ(何も書かない) |
| 213 | 0 | `--distributed` | 問い合わせ(何も書かない) |
| 293 | 0 | `--由来` | 問い合わせ(何も書かない) |
| 346 | 1 | `--at` の値が SHA でない | 引数の誤り |
| 356 | 1 | ★**正本を取れなかった** | ★★これは【測れなかった】ではないか(候補) |
| 364 | 1 | その中身が正本に無い | 違反を機械で示せた |
| 402 | 1 | 過去へ戻る道を断る | 断った(設計どおり) |
| 429 | 1 | 依頼と実物が違う | 違反を機械で示せた |
| 473 | 1 | ★**古い受領証を無効にできない** | ★★これは【測れなかった】ではないか(候補) |
| 563 | 1 | この現場で塊を直している | 違反を機械で示せた |
| 613 | 1 | 宣言が両方に書かれている | 違反を機械で示せた |
| 713 | 1 | 降格を承知の口が無い | 断った(設計どおり) |
| 736 | 0 | `--check`(下見) | 問い合わせ(何も書かない) |
| 744 | 1 or 0 | ★**すでに同じ ＋ 受領証が書けない** | ★★★操作は成功・検証だけ失敗(候補) |
| 766 | 1 | ★**取り直した ＋ 受領証が書けない** | ★操作は成功・検証だけ失敗(候補) |

### ★★★見えたこと ── 4件が【操作の成否】と【検証の成否】を同じ出口へ畳んでいる

```
356 … 正本を取れなかった      → ★取りに行けなかった(前提が無い)
473 … 受領証を無効にできない  → ★★書けなかった(前提が無い)
744 … 中身は同じ・受領証だけ書けない → ★★★操作は済んでいる
766 … 取り直し済み・受領証だけ書けない → 同上
```

★この塊は `verdict.mjs` で「**測れなかった** と **違反を機械で示せた** を同じにしない」を掟にしている。
★★`pull.mjs` の出口は、その掟の**外**に在った。

⚠ ★★★**まだ変えていません**(@codex が棚卸しを先に求めたため)。
変えるなら【出口の語の意味が変わる】ので大番号が要り、
`hooks/stop.js` と `selfcheck.mjs` の読み方も一緒に測り直す必要がある。

## 【動かせなかった】は 差戻ではなく 不明(26.0 で直した)

★`verdict.mjs` が「動かせません」と判じる材料は、★★**機械が返す符号だけ**(文言は使わない)。

```
ENOENT / 出口127 / ERR_MODULE_NOT_FOUND … 従来から
★命令の頭の語が【そもそも在るか】     … 26.0 で足した
```

⚠ ★実測(2026-09-03): Windows では上の3つが**一度も当たらない** ── 
`spawnSync("在りません", {shell:true})` は **status 1 / error.code なし**。
★★だから「動かせません」が【差戻】に化けていた ── 
★★★**測れなかったが、違反を機械で示せた、に化ける。**

⚠ ★頭の語を聞く条件は【許す形を並べない】。聞けない形(引用符・パイプ・道の区切り)だけを並べる。
  ★★ASCII だけの網は この塊では必ず嘘をつく(命令名が日本語だと聞きに行かなかった)。

## 道具は【現場に何を書くか】(2026-09-03 に測って足した)

★会議の窓口が「読むだけの道具は、この塊には1つも在りません」と書いた。
★★叩いて数えたら、**そこまで強くはなかった** ── だが宣言が無かったのは本当である。
★★★測って表にする(見本の現場で、走らせる前後の木を比べた):

★**表の1列目は【実名だけ】にする** ── ★★2026-09-03 に、ここへ `(素)` を足したら
SPEC を読む検査(B11)が持ち主を取り違え、★★★別の道具の口として数えた。

★**【条件】の列を足した**(2026-09-03、会議で @kozo が実測して求めた)── 
★★「常に」と「その口を打った時」を分けないと、**空の現場で叩いて表を疑う人が出る**。
★★★実際に @kozo は `--予想` を値なしで叩き(門が正しく弾いて書く所まで行かず)、表を疑った。

| 道具 | 打ち方 | 条件 | 現場に書く物 |
|---|---|---|---|
| `check.mjs` | 素 | ── | ★**何も書かない** |
| | 予想を置く口 | ★値を渡した時だけ(値なしは門が弾く・出口1) | `.guardian/予想.json`(＋台帳) |
| | 上限を下げる口 | その口を打った時 | `guardian.config.json`(＋台帳) |
| `verdict.mjs` | 素 / fast | ★★常に | `.guardian/coverage`(＋台帳) |
| `neighbors.mjs` | 一覧を出す口 | ★近傍が在る時だけ(0件なら手前で終わる) | `.guardian/neighbors.need.json` / `.guardian/neighbors.answer.json` |
| | 門 / 棚卸し / 定義一覧 / 口一覧 | ── | ★★**何も書かない**(実測。台帳も動かない) |
| | 逃した事故を測る口 | その口を打った時 | `.guardian/neighbors.escaped.json` |
| `selfcheck.mjs` | 素 / 変更分 | ── | ★**何も書かない**(押し直す口は塊の `ENGINE_FP` を押す ── 現場には書かない) |
| `外す.mjs` | 下見 / 走査 | ── | ★★**何も書かない** |
| | 名指しで消す口 | その口を打った時 | `.guardian/名指しで消した.jsonl`(★追記・＋台帳。27.6 から) |

★**名指しの口が断る物**(27.7 / 27.9)── ①保持一覧の【削除不可】 ②受け取りそのもの
★★③**区間(guardian:begin/end)が在る道** ── 丸ごとは消せない(台帳の道 `--外す` を使う)。

★★★④**保持一覧そのものが読めないとき** ── その口は【何も消さない】(27.9)。
★守る物が何か分からない状態で消すのは、戻せない側なので。
★★消す物のうち**読めない .json** も、その1件だけ消さずに残す(両方の口で)。
| | 外す口 | その口を打った時 | 消す(★この塊で唯一の不可逆な口) |

★**消える物が【人の言葉】を持つなら、消す前に言う**(27.5、2026-09-03、会議で @codex が求めた)──

```
★check.mjs … .guardian/予想.json に `人が書いた: true` を書く
★★外す.mjs … 消す .json の その旗を【消す前に】読み、一覧の行に警告を足す
            「この中には【あなたが書いた言葉】が入っています/消えると戻せません」
```

★★★**保持一覧には入れない** ── 保持一覧は「塊が【名指しするが書かない】物」の一覧で、
`予想.json` は書き手を通り**台帳に載る**(実測)。入れると PASS が永久に出せなくなる。
★名前をべた書きせず、**中身に宣言させて**読む(この塊は手で並べた一覧を持たない)。
| `install.mjs` | 素 | 常に | 導入する物(＋台帳) |
| `pull.mjs` | 素 | 常に | `guardian/` を取り直す(現場の物は触らない) |
| | 由来を出す口 | ── | ★★★**何も書かない**(★作業木のバイトを読むだけ) |
| `stop.js` | フックとして | ★★合否が JSON で読めた時だけ(★★★測れなかった時は書かずに止める) | `.claude/verdict_at`(＋台帳) |
★**「＋台帳」= 走行中に増えた物を `.guardian/導入台帳.json` に載せる**(共通の書き手を通るため)。
★★だから【何かを書いた道具は、必ず台帳も動く】── ★★★台帳が動いただけなら、それは記録であって仕事ではない。

★**外から測る人へ**: 測るなら**写しを作ってから**叩くこと。
★★上の表で「何も書かない」と書いた道具でも、★★★**その現場が git で追跡していれば mtime は動く**。

## 版の付け方(2026-08-31 依頼主の指示で決定 ── ★決めたら守る)

★**大番号**: 配布先が【読み直す】必要がある変更のときだけ上げる。

```
① 口(--口一覧 が答えるもの)が増えた・減った・名前が変わった
② 出口の語(通過 / 差戻 / 注意 / 不明 / ここでは測れない)の意味が変わった
③ 配る中身の構成が変わった(ファイルが増減した・置き場が変わった)
```

★★**小番号**: それ以外は全部。**繰り上げない** ── `12.9` の次は `12.10`。

```
検査を1本足す        … ★小(赤くなる条件は増えるが、口も語も変わらない)
数を1つ出す          … ★小
記録だけ(WHY/METHOD)… ★小
注釈・地図の更新      … ★小
```

★★★**なぜ書くか**: 2026-08-31、9.99 まで小番号を2桁で使っていたのに、
**10.0 から黙って .9 で繰り上げ**ていた。**規則がどこにも書いていなかったので、気分で変えられた。**
実測: 10.0 以降の29版が、前の規則なら 10.0〜10.28 だった ── **大番号が2回、余計に上がっている。**
★**過去の版は打ち直せない**(版の不変性を B1e が見張っている)。**前向きにだけ直す。**
