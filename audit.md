---
description: CODEMAP.mdと実装の突き合わせ監査(3時間ごとの定期義務・依頼主指示 2026-08-08)
---

docs/CODEMAP.md(機能→全接点の地図)を実装の現実と突き合わせる定期監査を実施する。
改修時の照合(受動)だけでは地図は現実からずれる ── この監査(能動)が対になる。
規模が大きいときはExploreサブエージェントに分担させてよい(worker/gas班・site/companion班・品質班)。

## 観点0 ── まず「もっと良く出来ないか」を問う(2026-08-30 追加)

観点1〜4は**探すものを指定している**(実在するか・網羅されているか・死んでいないか・まとめられないか)。
だから**指定していない形は通り抜ける。**

その手前に、**何も探さない工程**を置く ── 手順は [hunch.md](hunch.md)。

```
段1  違和感を、理由を書かせずに20件出させる        … 10秒
段2  出た全部に、別の1体がサーチライトを当てる      … 4分
```

★**実測**(`research/lab/`): 変更した本人は6つの壊れのうち5つの現場に手を入れて1件しか
指摘しなかった(1/6)。同じコードに「一から書くなら?」と問うた別の1体は **6/6**。
**編集している対象は、判断の対象にならない。**

★**宣言の `context`(運用の文脈)が空なら、運用の欠落は永久に出ない。**
問いをどう変えても動かず、文脈を足した1回だけが動いた(3/6 → 5〜6/6)。
`selfcheck` が「見ていません」と言うので、そこで気づける。

## 監査4観点

1. **実在照合** — 地図に書かれた接点(ファイル・関数・ルート・表)が現在のコードに実在するか。行番号まで確認。消えた接点は地図から削除、名前が変わったものは追従。
2. **網羅照合** — コード側の主要機能(APIルート・GAS道具・siteページ・companion)を俯瞰し、地図に無い機能を拾って項を足す(1項目10行以内の索引形式を守る)。直近の git log / STATUS.md 追記も参照。
   ★**配布物(この塊のような)を使っているなら、その仕様書と地図を突き合わせる。**
   仕様書は機械が見張れても、**地図は索引なので全部は照合できない** ── ここが漏れる。
   実測: この塊自身の監査で、**2回連続**で新しい層が地図から漏れていた(2026-08-28・29)。
3. **重複・死にコード** — どこからも呼ばれない関数・到達しない分岐・意味のない重複・撤去日の無い互換経路(CLAUDE.mdの教訓「旧経路には撤去日か自動掃除を必ず付ける」)を列挙。
4. **メタ化余地** — 個別実装が並んでいて性質表・汎用関数・宣言テーブルに一般化できる箇所を列挙。特に社名分岐が 性質表 の外に散らばっていないか(guardian/check.mjs の検出の穴も含む)。

## 監査の前に、計器を確かめる

判断の前に、**見ている計器が生きているか**を先に確かめる(RULES.md 42条)。

```bash
node guardian/selfcheck.mjs
```

ここが赤いまま監査しても、`check.mjs` の「ずれなし」は信用できない。

## 観点3・4は、まず機械に下ごしらえさせる(2026-08-26)

```bash
node guardian/neighbors.mjs --sweep
```

呼ばれていない記号(死にコード候補)・同名の関数が複数ファイルに(写経の疑い)・
参照の集中点(メタ化・正本化の材料)を、差分ではなく**全体**に対して列挙する。
門(--gate)は変更の局所しか見ない ── 重複と死は距離の現象ではないので、監査のたびに全体を棚卸しする。
**列挙は疑いであって判定ではない**(export の無い古典スクリプトの大域関数などは誤検出しうる)。
判定はこの下の義務2のとおり ── 報告して判断を仰ぐ。

## ★観点3に足す ── 型や語を【改名した版の後】は 迷子を数える(27.30、2026-09-04)

★改名は「足す」より危ない ── 足した物は使われないだけだが、
★★改名は**読む側を静かに外す**(条件が常に false になり、守りが音もなく死ぬ)。

★★★実測(27.25 → 27.30): 型を [印] → [区間]/[印だけ] に分けた時、読む所が1つ残り、
区間の守りが死んで **CLAUDE.md が丸ごと消えた**(人が書いた行も一緒に)。

```bash
# 出す型と読む型を突き合わせる(迷子=出していないのに読んでいる型)
node -e "const fs=require('fs');for(const f of ['外す.mjs']){const s=fs.readFileSync(f,'utf8');const 出=[...s.matchAll(/型: '([^']+)'/g)].map(m=>m[1]);const 読=[...s.matchAll(/型 === '([^']+)'/g)].map(m=>m[1]);console.log(f+': 迷子 '+([...new Set(読)].filter(x=>!出.includes(x)).join(',')||'0件'))}"
```

## 完了時の義務

1. docs/CODEMAP.md を現実に合わせて更新する(観点1・2の結果)。
2. 観点3・4の発見は**勝手に直さない**。監査報告として提示し、依頼主の判断を仰ぐ(開発側は作業手順を決めない原則)。
3. **口が減っていたら、許容(max)を機械に下げさせる**(一方通行を手作業にしない):

```bash
node guardian/check.mjs --tighten
```

4. STATUS.md に監査実施と発見の要点を記録する。
5. 監査時刻を記録する(これでclock.jsフックの期限通知が止まる):

```bash
node -e "require('fs').writeFileSync('.guardian/audit_at', String(Date.now()))"
```
