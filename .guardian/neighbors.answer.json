{
 "range": "HEAD",
 "answers": {
  "install.mjs::added": {
   "判定": "影響なし",
   "理由": "中身を見た。今回 install.mjs で変えたのは、selfcheck を呼ぶ spawnSync の締め切りを 60秒から300秒にした1箇所だけ。呼び方も戻り値の読み方も変えていない。install を見本の配布先で実際に走らせ、手順7の判定(自己検査が通ったか)が前と同じに出ることを確認した",
   "印": "hr6e2m-6271"
  },
  "install.mjs::CC": {
   "判定": "影響なし",
   "理由": "中身を見た。今回 install.mjs で変えたのは、selfcheck を呼ぶ spawnSync の締め切りを 60秒から300秒にした1箇所だけ。呼び方も戻り値の読み方も変えていない。install を見本の配布先で実際に走らせ、手順7の判定(自己検査が通ったか)が前と同じに出ることを確認した",
   "印": "hr6e2m-6271"
  },
  "install.mjs::cfgPath": {
   "判定": "影響なし",
   "理由": "中身を見た。今回 install.mjs で変えたのは、selfcheck を呼ぶ spawnSync の締め切りを 60秒から300秒にした1箇所だけ。呼び方も戻り値の読み方も変えていない。install を見本の配布先で実際に走らせ、手順7の判定(自己検査が通ったか)が前と同じに出ることを確認した",
   "印": "hr6e2m-6271"
  },
  "install.mjs::findInstallRoot": {
   "判定": "影響なし",
   "理由": "中身を見た。今回 install.mjs で変えたのは、selfcheck を呼ぶ spawnSync の締め切りを 60秒から300秒にした1箇所だけ。呼び方も戻り値の読み方も変えていない。install を見本の配布先で実際に走らせ、手順7の判定(自己検査が通ったか)が前と同じに出ることを確認した",
   "印": "hr6e2m-6271"
  },
  "install.mjs::setPath": {
   "判定": "影響なし",
   "理由": "中身を見た。今回 install.mjs で変えたのは、selfcheck を呼ぶ spawnSync の締め切りを 60秒から300秒にした1箇所だけ。呼び方も戻り値の読み方も変えていない。install を見本の配布先で実際に走らせ、手順7の判定(自己検査が通ったか)が前と同じに出ることを確認した",
   "印": "hr6e2m-6271"
  },
  "install.mjs::ROOT": {
   "判定": "影響なし",
   "理由": "中身を見た。今回 install.mjs で変えたのは、selfcheck を呼ぶ spawnSync の締め切りを 60秒から300秒にした1箇所だけ。呼び方も戻り値の読み方も変えていない。install を見本の配布先で実際に走らせ、手順7の判定(自己検査が通ったか)が前と同じに出ることを確認した",
   "印": "hr6e2m-6271"
  },
  "install.mjs::settings": {
   "判定": "影響なし",
   "理由": "中身を見た。今回 install.mjs で変えたのは、selfcheck を呼ぶ spawnSync の締め切りを 60秒から300秒にした1箇所だけ。呼び方も戻り値の読み方も変えていない。install を見本の配布先で実際に走らせ、手順7の判定(自己検査が通ったか)が前と同じに出ることを確認した",
   "印": "hr6e2m-6271"
  },
  "install.mjs::snippet": {
   "判定": "影響なし",
   "理由": "中身を見た。今回 install.mjs で変えたのは、selfcheck を呼ぶ spawnSync の締め切りを 60秒から300秒にした1箇所だけ。呼び方も戻り値の読み方も変えていない。install を見本の配布先で実際に走らせ、手順7の判定(自己検査が通ったか)が前と同じに出ることを確認した",
   "印": "hr6e2m-6271"
  }
 }
}