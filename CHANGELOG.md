# 塊の版

## 9.2 (2026-08-28) ── **公開した**。誰でも1行で入る

正本: **https://github.com/ricordi-studio/guardian**(公開)

- **入れ方が1行になった**(アカウントも認証も要らない):
  ```
  npx degit ricordi-studio/guardian guardian
  ```
- `pull.mjs`(取り直し)も**認証なしで動く** ── 実測: 認証情報を無効にした環境で確認

### 公開の前にやったこと

- 過去のコミットに残っていた個人情報を消すため、**履歴を1つに畳んだ**
- ★force push では**消えなかった**(SHAを知っていれば直接読める。実測)。
  確実に消す唯一の方法は**リポジトリの作り直し**だったので、消して作り直した
- ★**匿名で clone して、到達できる全オブジェクト31個を総なめ**し、
  人名・アカウント名・現場名が1つも無いことを確かめた
  ── 「伏せたつもり」で公開しない

★それ以前の版の記録は CHANGELOG に全部残っている(14版ぶん)。
  履歴を畳んで失うのはコミットの粒度だけで、**何をなぜ変えたかは1つも失っていない**。

## 9.1 (2026-08-28) ── 要件(SPEC.md)を置き、**遅れないことを機械が見張る**

依頼主「新しい機能が追加されたら、その要件定義書も更新されるべき。
そして**それのチェックを自分でやるところまで**。」

- **SPEC.md を新設**: 解く問題 / 満たすこと(R1〜R11)/ **満たさないこと(境界)** /
  動作の前提 / **口の一覧** / 検証済みの事実
- ★いちばん価値があるのは**満たさないこと** ── コードの正しさは見ない / 実行時のバグは見ない /
  セキュリティ検査ではない / 勝手に直さない / 列挙は判定ではない / 他の道具では自動で鳴らない。
  口頭でしか伝えていなかった境界を文書にした
- **更新しないと通れない**(注意書きではなく検査):
  口(--xxx)が SPEC に載っていなければ赤 / エンジンのファイルが名指しされていなければ赤
- ★実測: 書いた直後に**フック4本が名指しされていない**と落ちた(まとめて書いていたため)。
  架空の口 --zz-new-port を足したら**名指しで落ちる**ことも確かめた
- ★**測れない所は、測れないと書いた**: 「満たさないこと」は機械が見張れない
  ──**できることは数えられても、できないと言い切ったことは数えられない**。
  だから監査のたびに人が読み直す、と SPEC 自身に書いた

自己検査 50件 → **53件**

## 9.0 (2026-08-28) ── ルート直下へ引っ越し、どの開発ツールでも使える形に

### ① 置き場を tools/guardian → guardian へ

依頼主「名前を Guardian にしたのに、そのルートが tools なのは気になる。おかしくないですか?」
**おかしかった。**tools は「道具です」以上の情報を持たず、しかも Guardian の本体は
事故の記録(WHY.md)と規律(RULES.md)── **「道具」という括りは中身より狭い**。
8.0 で codemap → guardian を直したのと**同じ問題が、1階層上に残っていた**。

- `guardian/` … 配られる塊(コードと読み物)。**見える** ── 読んでほしいものだから
- `.guardian/` … この現場の作業記録(回答・監査時刻)。**隠れる** ── 機械が書くものだから
- `guardian.config.json` … この現場の宣言。ルート直下のまま(**人が書く**ものだから見える)

★あわせて、門の回答ファイルが `.claude/` に置かれていたのを `.guardian/` へ移した。
  他の道具の現場で `.claude/` に書くのは筋が通らない。

### ② Claude Code でない現場に対応した

`install.mjs` が「この現場に Claude Code の仕掛けがあるか」(`.claude/` か `CLAUDE.md`)を見て、
**無ければ `.claude/` を作らずに、手で回す手順をその場で全部出す**(`--hooks` / `--no-hooks` で上書き)。
どちらにしたかは**必ず言う** ── 黙って片方を選ぶと、なぜ鳴らないのか分からなくなる。

`install.md` に「この道具が Claude Code でない場合」の節を追加。
**本体は1つも減らない**(検査・門・合否・監査は Node だけで動く)。
Claude Code に依存するのは**自動で鳴る部分だけ**、という境界を表で示した。

★実測: `.claude` も `CLAUDE.md` も無い現場で導入し、**`.claude` を作らないこと**と、
  出した手順(検査・自己検査・全体走査・合否)が**全部そのまま動くこと**を確かめた。

## 8.2 (2026-08-28) ── 個人情報を伏せた(公開できる状態に)

依頼主の指示。塊は事故の記録が本体なので、**実機で起きたこと**を具体的に書いてある ──
その具体性が価値であると同時に、個人名が入る所でもあった。

- 人名を**役割の語**に置き換えた(201箇所)。「◯◯の実機で」→「依頼主の実機で」
  ★伏せても**教訓が死なない**ことが条件。「実機で見つかった」まで削ると、
    机上の話に見えてしまう ── それが一番の損
- 家族の名前(3箇所)・秘書の名前(4箇所)・配布先のフォルダ名を伏せた
- **残したもの**: 「この現場(AI秘書)」── 現場の【種類】であって個人情報ではない。
  むしろ「全部実機で起きた」ことの根拠なので残す
- 機械で確認: 人名・アカウント名・現場名が**1つも残っていない**ことを数えた
  (自分の目で「たぶん消えた」と言わない)

## 8.1 (2026-08-28) ── 正本を GitHub に置いた

**正本: https://github.com/ricordi-studio/guardian**(非公開)

これまで正本は「現場A の tools/ 」で、配布はフォルダのコピーだった。
どのコピーが最新かは人の記憶に頼っており、実際に**配った先で直した分が戻らず、
両方の版が同じ番号のまま食い違った**(7.6 の事故)。正本を1つにして、そこから取る形にした。

- **pull.mjs を新設**: 正本から取り直す1行。
  ★取り直す前に **この現場で塊を直していないか**を必ず見る ── 直していたら**止まる**。
    上書きすると、その現場の直りが黙って消えるから(先に --report で1枚を作って還すこと)
  ★何が変わるかを数えて出してから置き換える(黙って上書きしない)
  ★--check なら取らずに差だけ見る
- README の入れ方を「正本から取る」形に。手元のコピーは最新とは限らない
- 指紋の対象に pull.mjs を追加

★これで【配る → 直す → 還す → 配り直す】の輪が、記憶ではなく道具の上で回る。
  ただし**人の承認は残す**(--report の1枚を人が読んでから渡す)。

## 8.0 (2026-08-28) ── 名前を **Guardian** にした

依頼主「この検査ツール全てを Guardian という名称にするのはどうですか?」

**賛成した理由**: `codemap`(地図)は、いまや塊の**一部**でしかない。
実際の中身は5層(合否/門/検査/計器/規律)+ 監査 + 還流で、地図はその1つ。
**名前が中身より狭い = 名前が嘘をついている** ── この塊がずっと直してきたのと同じ形が、
塊自身の名前に残っていた。

### 変えたもの

| 前 | 後 |
|---|---|
| `tools/codemap/` | `guardian/` |
| `codemap.config.json` | `guardian.config.json` |
| `/codemap-audit` | `/guardian-audit` |
| 通行印 `codemap:ok` | `guardian:ok`(器の49箇所を一括置換) |
| `.claude/codemap_audit_at` | `.guardian/audit_at` |

### 変えなかったもの(理由つき)

- **`docs/CODEMAP.md`(地図)** ── 地図は Guardian の**中の1層**。地図という機能の名前として正確
- **`hooks/codemap.js`** ── このフックは「編集の直前に**地図**を差し込む」役。名前が正確
  (一括置換で参照だけ `guardian.js` になっていたのを、7ファイル分ぜんぶ戻した)
- **WHY.md / RULES.md / METHOD.md** ── 中身の名前であって、塊の名前ではない
- **`claude/codemap-audit` ブランチ** ── クラウド側の設定なのでリポジトリからは変えられない。
  文書だけ改名すると**実際と食い違う**ので、実名のまま残し理由を添えた

### ついでに門を直した(改名が炙り出した)

通行印を一括置換したら、**コメントしか変わっていないのに31件の近傍に答えを求められた**。
ロジックは1文字も動いていないので、答えは全部「影響なし・コメントだけ」になる ──
**それは門ではなく儀式である。**答えを埋めれば通る門は、次に本物が来ても同じように埋められて通る。

→ `parseDiff` を作り直し、差分の塊ごとに**消えた行と足された行をコメントと空白を除いて突き合わせ**、
  同じなら数えないようにした。**除いた行数は黙らず表に出す**(何を見なかったか分からない計器は計器ではない)。
  実測: 31件 → **18件**(58行を「中身が変わっていない」と正しく除いた)。

### 配布先へ

**この版から、配布先はフォルダ名ごと入れ替えが要ります。**
`tools/codemap/` を消して `guardian/` を置き、`node guardian/install.mjs` を回してください
(冪等。フックの登録と宣言の名前が新しくなります)。

## 7.7 (2026-08-28) ── 改善の還流(配った先の直りが、元へ還る道)

7.6 で「配られたときの中身と違う」ことは分かるようになった。だが**気づいた後、人が手で運ぶ**
しかなかった。依頼主「監査後にこちらに送る文章を書いて、ユーザーから承認を得られたら、
こちらに送ってきて、それを反映できる、みたいな」── その形にした。

- **指紋をファイルごとに持つ**(`ENGINE_FP`)── 全体で1つだと「どれかが変わった」しか言えず、
  **何を直したのか報告できない**(報告できなければ、還る道が無い)
- **`selfcheck --report`**: 変わったファイルの中身と渡し方の説明を `guardian-report.md` に1枚で書く
- **人の承認を挟む**: その1枚を人が読んで承認し、元の塊のAIへ渡す。**自動送信にはしない**
  ── 中身がそのまま外へ出るので、何を送るかは人が見て決める
- **受け取る側はAIが読んで取り込む**(機械が丸ごと上書きしない ── 元の改善が黙って消えるため)

★サーバーは**いまは要らない**。同じPCの別フォルダなら1枚をコピーで運べば足りる。
  別のPC・他人へ配るようになったら要るが、**流れも人の承認もそのまま**で、
  運ぶ手段が「コピー」から「送信」に変わるだけ。

★実測: 架空の現場を作って塊を配り、そこで1ファイル直し、
  **名指しで赤くなる → --report で1枚ができる**ところまで通した。

## 7.6 (2026-08-28) ── 配った先で直った分が、戻ってこなかった

別プロジェクト(積立と引き出しの金利シミュレータ)へ配って使ってもらった。
**塊自身のバグが2つ見つかり、向こうで直された。だが元の塊には戻ってこなかった。**
しかも両方の `KIT_VERSION` が同じ 7.5 のまま ── **版が嘘をついていた。**

### 逆輸入した2つ(どちらも汎用のバグ)

- **`install.mjs` のパス解決**: ファイルURLから自前で pathname を削っていたので、
  **フォルダ名に空白が入ると壊れる**(URLでは %20 になる)。`fileURLToPath` を使う形へ。
  ★こちらのパスには空白が無いので**一生出なかった** ── 配布先(空白入り)で初めて出た
- **`check.mjs` のセレクタ照合**: `className = ...` の = の前後の空白を許していなかった

### 構造として塞いだ: エンジンの指紋(`ENGINE_FP`)

版の数字は人が上げるので、直した人が上げ忘れれば黙って食い違う。
だから**中身そのものの指紋**で見る(名前ではなく中身で照合)。

- `selfcheck.mjs` がエンジン(mjs とフック)の指紋を計算し、`ENGINE_FP` と照合する
- 違えば **★この塊は配られたときの中身と違います** と赤にし、
  「直したなら元の塊へ戻して版を上げること」と促す
- 意図した変更は `node guardian/selfcheck.mjs --stamp` で押し直す
  (`check.mjs --tighten` と同じ思想 ── 一方通行を人の手作業にしない)
- 文書(WHY.md・RULES.md)は数えない ── 配布先で事故が増えるのは正当

★実際に配布先の塊へこの検査を当てて、**「配られたときと違う」と言えることを確かめた。**

### 使ってもらった評価から(向こうのAIの言葉)

> いちばん価値を発揮したのは「事前に仕込んだ検査」よりも、**「監査という行為そのもの」**でした
> ── 実際に手を動かして地図と実装を突き合わせたからこそ、ズレも、ツール自体の欠陥も見つかった。
> **検査を仕込んで放置するだけでは、ここまでの効果は出なかった**と思います。

この観察は正しく、しかもこの塊の設計そのものを言い当てている ──
だから催促は**時間ではなく変更で駆動する**(`hooks/clock.js`)。
「回す気になったとき」ではなく「実装が動いたとき」に監査を促す仕掛けが、この塊の中心にある。

## 7.5 (2026-08-28) ── **実際に他所へコピーして動かした**(初めて)

「持ち出せる塊」と言いながら、**一度も持ち出して試していなかった**。まっさらなgitリポジトリへ
コピーして端から端まで回したら、**穴が4つ**出た ── どれも「置いてあるだけで誰も読まない/動かない」形:

- **雛形のCLAUDE.mdが書く層の数が古いまま**(1つ足りない)── 導入した瞬間から自己検査が赤かった
- **門(neighbors)の宣言が導入時に入らない** ── `--gate` も `--sweep` も「宣言が無いので回しません」で黙る。
  近傍照合という一番新しい層が、他所では**最初から効いていなかった**
- **証拠(evidence)が空でも案内が無い** ── 合否が【不明】のまま放置される
- **雛形が合否と門を案内していない** ── 他所のAIがCLAUDE.mdを読んでも、その2つを知らない

直したうえで、まっさらな場所で再度確認した(自己検査48件緑 / 門が仕込んだ死にコードを自力で発見)。

★規律に2条追加: **46. 注意書きは検査ではない** / **47. 自分が回した測定にも計器の規律を当てる**

★教訓: **「移植できる」も、実際に移植して確かめるまでは主張であって事実ではない。**
  これは塊自身が説く「無音を成功と読まない」の、塊自身への適用だった。

## 7.4 (2026-08-22) ── 危険駆動の実例を1つ埋めた

- 塊そのものの変更は無し。**危険駆動(RULES.md 43条)で置いた検査の実例**が1件増えた:
  「利用者の表は【誰のデータか】を名乗る口からしか触らない」(onlyIn・max=0)
- 事故を待っていては永久に埋まらない領域(絞り込みが抜けても**例外が出ない**ので、
  事故として現れず WHY.md に1行が書かれない)に、先に検査を置いた例

## 7.3 (2026-08-22) ── いま有効な決定と、履歴を分ける

- **`perSection` を新設**: 節ごとに決めた1行があること(決定記録に**状態**を必須にする用)
- **`citeLive` を新設**: **もう現行でない決定を、いまの規則として引いていないか**。
  履歴として引くのは正当なので、行末の `guardian:ok` で通せる(理由を書くのが通行料)
- どちらも見本で毎回試す(自己検査 48件)

★この2つは、規範文書の**絶対規則**が1か月以上前に改定された決定を語り続けていた事故から。
決定記録は履歴なので古い節が残るのは正しい ── 問題は、古い節と現在の節が**同じ見た目で並ぶ**こと。

## 7.2 (2026-08-22) ── 完了の判定を、AIの外へ移した

- **`verdict.mjs`(合否)を新設**。宣言(`guardian.config.json` の `evidence`)に並べた証拠を集め、
  **通過 / 差戻 / 注意 / 不明** の4語に落とす。**判定は1つも持たない**(理屈は各証拠の出口コード)
  ── ★**不明を緑に数えない**(出口 差戻=1 / 不明=2 / 通過=0)
- **`hooks/stop.js`(Stop フック)を新設**。「できました」と言い終える**手前**で合否を回し、
  **差戻のときだけ止めて理由を返す**。不明・注意は止めないが**必ず本人に返す**
  (実装が変わっていなければ回さない / 二度は止めない / 失敗しても黙って通す)
- **夜間CIを合否1本に寄せた**。証拠の一覧が「CIの yaml」と「宣言」の2枚に割れていたのを1枚に(39条)
- `--fast` で回さなかった証拠は**名前を出す** ── 「速い方だけ回して全部通った」が
  「全部通った」に化けるのを防ぐ
- 道具の有無は**出口コードで**確かめる(`needsCmd`)。文言で当てる実装は、
  日本語Windowsで丸ごと外れた(実測)

## 7.1 (2026-08-22) ── 事故が【どの層まで届いたか】を数える

- **WHY.md の全事故に札**を付けた: `[到達/由来]`。到達=検査/門/計器/**規律(=文章にしかない)**、由来=事故/予防
- **selfcheck が数える**: 札の無い事故があれば赤 / **守り(門か検査)の件数は下げない**
  (`selfcheck.mjs --tighten` で下限を上げる。check.mjs の `max` と同じラチェットで向きだけ逆)
  ── 初回の実測は **事故69件・守り25・計器15・文章だけ29**
- 件数の照合を書き分けた: **総数は「全N条」「全N件」**(実数と一致)/ **参照は「N条」**(実在する条番号か)
  ── 「RULES.md 39条」が総数の主張と読まれて誤検出になった(誤検出は検査ごと外される)
- RULES.md 43条「事故を待てるものと、待てないもの」(事故駆動と危険駆動)
- RULES.md 44条「門には必ず双子を置く」── 門は黙って死ねるので、同じ宣言を読む検査が要る
- WHY.md の「配るときの約束」を落とした(README と重複していた・39条)

## 7.0 (2026-08-21) ── 塊が塊を検査するようになった

**この版から `selfcheck.mjs` が同梱されます。** 塊を配る前に必ず回してください。

- **`selfcheck.mjs` を新設**(塊の自己検査・34件)。見本の小さなリポジトリを一時領域に建て、
  **1箇所ずつわざと壊して**「壊したら赤 / 壊さなければ緑」を確かめる。
  ＋塊そのものの健康診断(版が嘘をついていないか / 一度も試されていない検査の種類が無いか /
  エンジンに現場固有が混じっていないか / 文書が書いた件数が実数と合っているか)
- **`callArgs` を新設**(【既定に任せてはいけない引数】を数える)。
  ★これに伴い、エンジンに直接書かれていた**現場固有の検査を撤去**した
  ── 「`check.mjs` はどのプロジェクトでも同じ中身」がやっと本当になった
- **`check.mjs --tighten`**: 口が減っていれば宣言の `max` を実数まで機械が書き下げる
  (一方通行を人の手作業にしない)
- `noInline`: 見張るはずの語が実装から消えていたら【緑にしない】(永久に0件=緑を返していた穴)
- RULES.md に42条「計器そのものを、計器で確かめる」/ WHY.md に事故を4件追加

## 6.1 (2026-08-20)

- **METHOD.md を新設**(まとめの入口)。弱点6つ・対策4層(門/検査/計器/規律)・情報/作法/器の分け方・人間側の一言
- RULES.md に7条追加(13〜19): 相手の答えを捨てない / 症状を抑えたら原因を問う /
  合格の判定は実際の使い方に合わせる / 情報が足りないときは器で埋めない /
  器は種類の名前すら持たない / 未確認と失敗を区別する / 指示は形と説明を混ぜない
- WHY.md に事故を7件追加(2026-08-20)。合計29件
- hooks/no-fixed-names.js(固有名の門)を収録 ── 禁止語は検査の宣言から読む
- check.mjs: kind ごとの必須の欄が無い検査は【赤】にする(何も見ていない検査を許さない)

配った先の `KIT_VERSION` と見比べて、**どのコピーが古いか**を判断するための記録です。
上げ忘れると、この番号は嘘をつきます(**実際に v4 を上げ忘れました**・下記)。

## v6 — 2026-08-17
- **監査の呼び出し口を同梱**。`templates/guardian-audit-command.md` を `install.mjs` が
  `.claude/commands/guardian-audit.md` に置く → **誰でも `/guardian-audit` の1語で回せる**
  (無人の定期実行はアカウント側にしか置けないが、**呼び出し口はリポジトリに置ける**)

## v5 — 2026-08-17
- **夜間の見張りを同梱**。`templates/nightly-check.yml` を `install.mjs` が
  `.github/workflows/guardian-nightly.yml` に置く。**毎晩01:00・赤ければIssue・緑で閉じる**
- README に「夜中は2段」を明記(①機械検査=付いてくる / ②観点を選んだ監査=付いてこない)
- ⚠ **この版から、コピー先も入れ直しが要ります**(上書きコピー → `install.mjs` を再実行。冪等)

## v4 — 2026-08-17(番号を上げ忘れた版)
- `install.mjs` / `install.md` を追加。**フォルダを置いてAIに一言**で導入が済むように
- 検査エンジンの2つの穴を修正:
  - 宣言した検査が値を拾えなくても黙って通っていた → **落とす**(何も見ていない検査は無い検査より悪い)
  - 地図の雛形の例示を実装への主張と読み、初日に嘘の指摘が出ていた → 囲みコードを拾わない

## v3 — 2026-08-16
- 塊として切り出し。エンジン(記号実在 / same・sameMap・sameSet・shape / noInline / セレクタ)、
  フック2本(置き場所に依存しない)、WHY.md・RULES.md・audit.md・templates を同梱
