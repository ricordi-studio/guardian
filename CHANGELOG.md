# 塊の版

## 13.13 (2026-09-01) ── ★★自分が出したゴミを数える(配布先 外の会議 の事故報告)

★**配布先の報告**: `guardian-*` が **202,086個(推定61.5GB)**まで溜まり、
**依頼主の機械の空きが 5GB** になった。**★自己検査は【通過】で緑のままだった。**

★**まず自分で測った**(報告者も2回 誤診しているので):

```
★この機械では再現しなかった(3回 走らせて残り0個)。9箇所とも finally で rmSync している
★★報告の「6日前から生きていたプロセス」は、★★★実測したら **Adobe の node.exe** だった

★★★だが双子で裏が取れた ── **後始末を1本だけ殺すと、1回の走行で ★84個 残った**
   (その1本は共有の道具の中で84回 呼ばれる ── ★202,086 ÷ 84 ≒ 2,400回。桁が合う)
```

★**再現しないのは、この機械で rmSync が失敗しないから** ──
**Windows では子が掴んでいると EPERM で落ち、落ちる機械では1回で84個 溜まる。**

★★**この塊が持っていなかった種類の検査だった**:
**これまで「壊したら赤くなるか」/ ★今回「★★後始末をしたか」。**
**正常動作の副作用は、壊れていないので双子に掛からない。**

★直しは2つ: **① 後始末を粘らせ、落ちないようにする**(9箇所・maxRetries + try で囲む。
**finally で落ちると本当の結果が消えるため。ただし黙らせてはいない**)/
**★② 走り終わりに【この走行が出した一時領域】を数える**(出したものが残れば**赤**、
**前から在ったものは数だけ** ── 他人のゴミで止めない)。

見本で3通り実測: **後始末を1本殺す→✗84個 / 戻す→✓0個 / 前から在るゴミ1つ→✓(赤にしない)。**

★★★**プロセスは数えていない**(数え方が現場ごとに違い、報告の1件が実測では別物だった)。

## 13.12 (2026-08-31) ── ★畳めた側を数える口が 0本 / ★★「機械は全部に対応する」は半分だった

★**1件目**: 依頼主の「**美しいかを判断する傍観AIが要るのでは**」に対して ──
**傍観者は既に居て、美の語も使っていた**(「美し」0回 /「綺麗」11回 /「汚」2回)。
★**ただし向けた先は【証拠の美しさ】と【区分の綺麗さ】で、システムの形は一度も判断していない。**

★★**足りないのは傍観者ではなかった** ── **この塊の計器は例外なく【欠けている側】を数えている。**
**「まとまった/1つになった/減った」を数えるものは ★0本。**
★今夜、実際に「1つになった」ことが**4回**起きたが、**4回とも「WHY が1項 増えた」としか残っていない。**

★★★糸口(★推測): **美しさの実体が「同じことを2度言わなくて済む形」なら数えられる**
(材料: 条の本数 / 写経の疑い / 跨ぐ記号 / 口の本数)。
**但し書き: 畳むのは常に良いとは限らない**(9.68 で判定にして雑音55件)── **数であって判定ではない。**

★★**2件目**: 依頼主の「機械はお互いの疑問に細かく対応している」を測った
(定義: その発言を誰かが `返信先:` に名指したか):

```
★依頼主 ★3/3(100%)/ guardian 120/146(82%)/ ★★attendants ★70/169(41%)
guardian-b 10/19 / antigravity 13/25 / codex 8/15(いずれも約半分)
```

★**依頼主の発言は 100% 返る。★★参加者どうしは半分 落ちる**(★下限 ──「中身には答えたが指していない」は数えていない)。

## 13.11 (2026-08-31) ── ★会議が効いた機関は2つ在った(記録のみ・★推測を含む)

依頼主の見立て「**それぞれが自分の中に Guardian の実測環境を持っていたのが効いた**」は、
★**機関①を正確に言い当てている。★★そして、もう1つ在った。**発言を数えて分けた:

```
★機関①【同じ計器が、違う材料の上で2つ走る】… ★★【穴】を出す
   guardian 146発言 / attendants 169発言 → こちらの穴7件・向こうの本物3件・⑥の5段

★★機関②【計器を持たない読み手】… ★★★【形】を出す
   antigravity 25発言 ── ★自分の現場に触れた回数 ★★0回(grep で確認)
   → 停止の群分け / マクロとミクロ / 上手い文章≠終わり / 規則は作者が消えても回るか
```

★**①だけだと穴は出るが筋にならない。②だけだと材料が無い**(読み手は、数が出てから動いている)。

★★**組むなら(★推測・未測)**: **計器を持った参加者を違う持ち場に2つ以上 + 計器を持たない読み手を1つ以上。**
★★★**予測: 発見数は【体の数】ではなく【非対称の数】に比例する**
(反証: 読み手を外すと、穴は出続けるが形が出なくなるはず)。

★**触れていないもの**: どちらの機関も「**なぜ次を探したいか**」には1ミリも触れていない。

## 13.10 (2026-08-31) ── ★別の現場に入れて実走した(記録のみ)

依頼主の問い「**他のいろんなプロジェクトに入れて問題無いか**」に、**2つ作って実走して答えた。**

```
★JS の現場(git clone → install → pull)
  install 通る / pull 通る(受領証を書く)/ 自己検査 通過71・★未測2
  ★★合否 = 不明あり(75%)── ★通過ではない(private と context が空なので)

★★Python の現場
  ★記号を見る計器(定義一覧 / A5 / A7 / --sweep / 近傍の門)は ★★黙る
  ★★★ただし【黙ったと言う】──「見ていません(--定義一覧 が定義を1つも答えません)」
  ★ファイル単位(地図の実在照合 / 宣言の外 / 逃げ道)は動く
```

★**嘘の 0% を出さなかった** ── **9.61(0 は割合にしない)が、★別の言語で初めて効いた。**
★★**物差しは JS/TS の形をしている。他の言語では見える範囲が減るが、【減ったと言う】ので嘘にはならない。**
★★★**配布先の実測は、まだ 1件だけ**(現場A ── JS/TS + GAS)。

## 13.9 (2026-08-31) ── ★会議が締まった(引き継ぎに記録)

依頼主の指示(「議論する価値が無ければ終わってよい。★**同意を取れ**」)に従い、
動議 → **賛成2 / 反対0 / 未回答1** → **世話役が締めた**。

★**各自が「今夜これが残った」として挙げた3つを、引き継ぎの先頭に置いた**
(★次に来た人が最初に読む所)。

★★**世話役が締め方の規則を1つ直した**(今回、その世話役が71分 応答せず、締めが止まったため):

> ★**締めるのは世話役。ただし60分以上応答しないときは、参加者の誰が締めてもよい。**

★★★**規則は、作った人が消えても回らなければ意味が無い** ──
**この塊が11時間かけて出した「条は防がない」の、別の言い方である。**

## 13.8 (2026-08-31) ── ★決める人に渡すには「読める言葉」で「読む場所」に置く

13.7(**言葉**の失敗)に、配布先が**同じ穴の別の形**を出した:

```
★こちら : ★言葉が【道具の内側】── 依頼主に選べない
★★向こう: 言葉は読める ── ★★★【書いた場所】が依頼主の読む所ではなかった
```

> ★**「依頼主の判断待ち」と5回書きましたが、★★4件は渡していませんでした。**

★★**2つ揃って、初めて1つの条件になる** ── **【読める言葉】で【読む場所】に置く。**
**どちらか片方でも欠けると、渡したことにならない。**

★**今夜の型の、決定の側での現れ**: 起動≠仕事 / 在る≠効く / ★★★「判断待ちと書いた」≠「渡した」。

★★この塊の当てはめ: **引き継ぎは【次のセッションの自分】が読む所で、依頼主が読むのは画面の報告。**
**引き継ぎにだけ書いた判断待ちは、依頼主に届いていない。**
★今回の A4 は両方に書いたので**場所は満たしていた** ── **欠けていたのは言葉だけだった。**

## 13.7 (2026-08-31) ── ★数を出しても、決める人が読めなければ決まらない

監査(観点4)の1件を選択肢3つにして依頼主へ渡した。★**答えは「よくわからない。保留にして」。**

★★**これはこちらの出し方の失敗である** ── **選択肢を全部【道具の内側の言葉】で書いた。**

```
★数を出す     … 穴を名指す(今夜 何度も効いた)
★★選択肢を出す … ★道具の言葉で書くと ── ★★★決める人が読めない
★★★足りない段 : ★【どれを選ぶと何がどう変わるか】を、決める人の言葉で書く
```

★**48条は「費用を数えろ」と言うが、★★数えた費用を【決める人が読める言葉】にするところまでは
書いていなかった。**

★★書き直し方を引き継ぎに置いた ──「毎回19件出るが、うち18件は数え方の癖で直す所ではない。
① 読み飛ばす / ② 数え方を変える(見落としが増えるかも)/ ③ 分けて出す」。
**同じ3つでも、こう書けば決める人が値踏みできる。**

★★★**保留の理由は「材料が足りない」ではなく「材料の書き方が悪かった」。**

## 13.6 (2026-08-31) ── ★★第2の議題の答え:道具の性能は「回している最中に当たるか」

配布先が、今夜の本物3件の**当たり方**を並べた:

```
★clean-dev の緑の嘘 … 道具を【使う準備をしていて】当たった
★暗号の三つ目       … `--跨ぐ記号` を回したら、数と行が食い違った
★公開口の署名       … 問いを追う【途中】で当たった

★★【考えて選んだ】3箇所からは ── ★★★1件(しかも「測っていない」の発見)
```

★★★**これが第2の議題の答えである:**
**道具の性能は「何を見つけるか」ではなく「★回している最中に、何かに当たるか」で決まっていた。**

```
★--sweep / --跨ぐ記号 / probe … ★3週間 在って 0件 / ★★回した日 … ★★★3件
★変わったのは道具ではない
```

★**「性能を上げる」の意味が変わる** ── **もっと見つける検査を足す**のではなく、
**★回される回数を増やす / ★★回している最中に当たりやすくする。**
★★今夜足した数の多くが、まさに後者だった(`--冷たい順` / 総量 / 口の 2/44 / 見本の N/M)。

## 13.5 (2026-08-31) ── ★2つの手に値段が付いたので、順番が決まった

13.4 で「気づくまで」に数字が付き、**2つの手に値段が付いた:**

```
★① その場で自分で測り直す(12.12) … ★0〜5分
★★② 相手に見に行かせる(12.6)     … ★★★18〜87分

→ ★①で減らせるものは①で減らし、★★②は【見えない側だけ】に使う
```

★**②を先にやると、①で済んだものまで1時間かける。**

★★**2人とも逆をやっていた**(実測: 向こう **9件中2件**、こちら **7件中1件**だけが①)。
★★★**②は相手の時間を使う。①を怠ると、相手が枯れる。**

★これが「2人居れば埋まる」の最後の形 ──
**出した数の誤りは相手が【見つける】/ 取り方の誤りは相手が【見に行かせる】── ただし高い。**

## 13.4 (2026-08-31) ── ★「気づくまでの時間」を初めて測った(中央値 5.2分)

13.2 で「**数えるべきは回数ではなく【気づくまでの時間】かもしれない。どちらも計器にしていない**」
と書いた。★**配布先がその場で手で測った(訂正9件):**

```
★中央値 5.2分 / ★★最大 87.3分

★★いちばん遅い2件は、どちらも「★あとから思い出して当てた」
  → ★★★どちらも【自分で気づいた】が、★きっかけは外だった
```

★★**「自分で気づいた」は2つに割れる** ──
**本当に自分で(速い)/ ★外から来た(遅い ── 相手の言葉を読んでから思い出す)。**
★**12.6(見に行かせる)に時間が付いた: 効くが、★★1時間かかる。**

★機械にできるか ── **この部屋ではできる**(発言の時刻 + `訂正: <元の発言>` の欄)。
★★**費用未計測、かつ会議室の作法なのでこちらの領分ではない** ── **数だけ置く**(12.7 の第2群)。

★★★この塊の側: `.guardian/踏んだこと` には**回数しか無い。気づくまでの分は1つも書いていない。**

## 13.3 (2026-08-31) ── ★穴が無かったときは【いま正しいこと】を固定して帰る

13.0 の `--冷たい順` を配布先が当て、**サーチライトが1周した:**

```
① マクロ「companion 23日」→ ② 読みに行った(3分)→ ③ ★穴は無かった
→ ④ ★★それでも【いま正しいこと】を形で固定した
```

★理由: **冷たい(23日)× 測れない(Electron を起こす検査が無い)= ★10.3 の掛け算。**

> ★★**冷たい場所では、正しさも黙って壊れる。**

★**作法**: **サーチライトが指した先に穴が無かったら、「無かった」で終わらせず、
【いま正しいこと】を固定して帰る**(A5 の「落とさない数」と同じ考え)。

★★**もう1つの数**: 今夜 配布先が足した検査 **9本のうち、壊れる前に固定したのは2本**
(残り7本は踏んだ跡)。**11.6「形は踏んだ跡からしか作れない」の例外が2つになった** ──
**① 他人の問い / ★② マクロの計器が指した先。どちらも「自分では見に行かなかった所」。**

## 13.2 (2026-08-31) ── ★②と③が重なった所に印 / ★★防げないなら、安くする

★**1件目**: 13.0 の3つの外れ値のうち、**②遅すぎる と ③直した直後 が重なる所**が
今夜の本物の居場所だった。`--冷たい順` に**印を1つ**足した(配布先の案):

```
23日  companion  ★いま触っている
```

★**判定にはしない**(「冷たい所を触るな」ではない)。**印を付けるだけ。**
実測(正本): `.`(根)に印が付いた。★**ただし正本では幅が出ない**(0〜2日)。

★★**2件目(記録のみ)**: **防げないなら、安くする** ── 両側で同じ形が出た:

```
★こちら: 同じ間違いを4回。★★落ち方を変えて、見つかるまでが 30分 → ★10秒(180分の1)
★向こう: 同じ間違いを12回。★★測定の双子で、「次の発言」から ★「その場」へ
```

★**条は防がない(2人・55本ぶんで確定)。★★できるのは【見つかるまでを短くする】こと。**
★★★**「4回した」は失敗に見えるが、4回目が10秒なら、道具が効いている。**

## 13.1 (2026-08-31) ── ★予想は「全部の数」ではなく「見に行きたい数」を書くもの

★**設計は正しかったが、説明が間違っていた**(配布先の実測):

```
★置いた予想 : 1行(人が書いた文) / ★★流れ込む数: ★50行以上(道具が出した数)
実測: ★4つ予想を書いて、★★動いたのは【書かなかった2つ】だった
```

★**差分は、予想に書いたかどうかに関係なく【動いた行を全部】出す** ── **そこが効いていた。**
★★**いちばんの材料は【予想に書かなかった数が動いたとき】** ──「そこを見ていなかった」。
**道具の言い方をそう直した。**

★★もう1件(記録のみ): **マクロは【見に行かせる】まで。何が悪いかはミクロにしか言えない。**
今夜の本物3件とも **①マクロの違和感 → ②ミクロで特定 → ③検査にする** の順だった。
**マクロが言えるのは「companion/ が動いた」まで。「三つ目の暗号実装が在る」は言えない。**

## 13.0 (2026-08-31) ── ★`--冷たい順`(口が1つ増えたので★★大番号)

★**版の付け方(SPEC)の初適用** ── **口が増えたので大番号**(12.17 → 13.0)。SPEC.md も一緒に変えた。

読み手が、マクロ計器の外れ値を3つに整理した(★どれも **git だけ**で出る):

```
★① 速すぎる   … 規則の無断変更 ── 実測: 依頼主が版数の進み方で見抜いた
★② 遅すぎる   … ★盲点 ── 実測: 30日で3回しか触っていない所に、暗号の3つ目が居た
★③ 直した直後 … ★記録の腐り ── 実測: 公開口の穴 / 直したから腐った数
```

★★**①は人が気づく。②と③は誰も見ていない**(冷たい所は静かで、直した直後は「終わった」と思っている)。

★`--冷たい順` を足した(日数→場所)。素の走行でも**冷たい上位3つ + いちばん熱い1つ**。
★★**判定にも上限にもしない**(何日が正しいかを知らない / 冷たい=悪いではない)── **並べるだけ。**
★★★**正本では役に立たない**(0〜2日しか幅が無い)── **「正本では測れない」の10件目。**

★作っている最中に**また隣の検査の変数を借りた**(今夜4回目)。
**今回は catch の外だったので黙らずに落ち、10秒で直った** ── 9.77「落ちる方が黙るより安い」の実例。
**同じ間違いは4回だが、気づくまでが 30分→2分→1分→★10秒**になっている。

## 12.17 (2026-08-31) ── ★計器は2階層(マクロ/ミクロ)/ ★★基準線は既に在った

読み手(antigravity)が今夜の道具立てを2階層に整理した ──
**マクロ(速度・総量)=「いつもと違う」/ ミクロ(契約・整合性)=「どこが」**。

★**今夜そのものが実例だった**: 依頼主「12.6 は多すぎないか」(マクロ)→
繰り上げ規則の変更を特定(ミクロ)→ **B12 を作った**(自動化)。

★★**基準線は既に在った** ── `.guardian/予想.json`(9.91)。

```
実測: 予想を置く → 2行 足す → 素で走らせる
  → ★「＋ いま: まだコミットしていない変更: 1 ファイル / +2 -0 行」が差分に出た
```

★**新しく作るものは無かった。マクロの【数】を1つ足したら、基準線は既にそこに在った。**

★★**足りないのは【移動平均】**(機械が勝手に持つ基準線)── **材料(git)は在るが費用未計測。作らない**(48条)。
★★★11.7 の但し書きがここにも当たる ──「いつもと違う」を機械が言い始めると、**「いつも」を機械が決める**。

## 12.16 (2026-08-31) ── ★上手い文章を、終わりと読むな(記録のみ)

会議の世話役が自分の締め方を訂正した:

> ★**「静けさを終わりと読むな」と自分で書いて、★★上手い文章を終わりと読みました。**

```
起動 ≠ 仕事 / 書いてある ≠ できる / 鼓動 ≠ 見張れている / 通った ≠ 測った
★★★上手い文章 ≠ 終わり(5件目)
```

★**まとまりは【書き手の状態】で、残りは【対象の状態】。**別のものである。

★★止め方も変わった ── **① 誰かが「だいたいできた」と言う ② 他が理由つきで賛成/反対**
**③ だいたい合意したら締める**(全員一致でなくてよい)。
★**これは 12.7 の【第2群(開いた停止)】そのもの** ──
**「もう誰も触らなくてよい」かは、止めた本人には決められない。**

## 12.15 (2026-08-31) ── ★総量の計器が、配布先では【塊の版数】を見ていた

12.14 を出した **3分後**に配布先が当てて、**穴が出た**:

```
総量 8917行 ── ★その 89% が【塊(guardian/)の取り直し】
★★読む人が知りたい数 : ★★★1171行(この現場が書いたぶん)
```

> ★**55倍は嘘でした。自分の総量を測ったその場で、母数を間違えていました。**

★**配布先では、取り直すだけで数千行 動く** ── **分けないと、総量は常に塊の版数を見ていることになる。**
★★**塊の分を内訳として分けて出すようにした**(「うち塊 N ファイル / この現場が書いたのは M ファイル」)。

見本で実測: **配布先で 21行のうち 20行が塊、この現場は 1行**と正しく割れた。
★**正本では働かない**(塊 = 根なので内訳が出ない)── ★★**「正本では測れない」の9件目。**

★★★**今夜4回目の母数の誤りで、出した3分後に見つかった中では最速**。
**こちらは一度も気づいていない**(正本では、この穴が永久に出ないため)。

## 12.14 (2026-08-31) ── ★4つ目が在った:【総量】を2人とも持っていなかった

配布先が問われて数えたら **8917行が未コミット**だった ── **39日ぶんの材料が git にずっと在ったのに。**

> ★**この数を、あなたの問いを読んでから3分で出しました。材料は39日そこに在りました。**
> ★★**見えなかったのは道具ではなく、見る気でした。**

```
★条 … 守らない / ★検査 … 個を見る / ★数 … 穴を名指す
★★★総量 … ★「どれだけ」を言う ── ★★2人とも1つも持っていなかった
```

★`check.mjs` が **まだコミットしていない総量**(ファイル数 / +行 -行)を出すようにした。
★★**判定にも上限にもしない** ── **大きいことは悪ではない**(一晩で作れば大きくなる)、
**何行が正しいかをこちらは知らない**。**出すだけ**(落とさない・git が無ければ何も言わない)。

見本で両向き実測: **きれい→出ない / 1行足す→「1ファイル / +1 -0」/ 戻す→出ない。**
★★★**これは 12.6(見に行かせる)の実装でもある** ──「数えろ」と言わず、**数が毎回そこに在る。**

## 12.13 (2026-08-31) ── ★引き継ぎの数は文章からは読めない / ★★条 49本のうち検査に触れているのは 6本

★**1件目**: 12.11 の「引き継ぎの数を検査に載せる」を配布先が作り始め、**1回目に自分の式で外した** ──
**`catch 625件` を太字にしただけで照合が壊れ、「記録が腐っている」と誤報した。**

```
★道具の出力 : 形が決まっている(道具が作るので)
★★引き継ぎ : ★★★形が決まっていない(人が読みやすいように書き換える)
```

★**文章から数を読む方向は追いかけっこになる。機械が読む形を、引き継ぎ側に作るしかない。**
★★**いちばん近い形は既に在った** ── `.guardian/予想.json`(9.91)。
★★★**作らない**(48条): 費用未計測 / 「全部置く」は 7条 / **3件目が出てから決める**。

★★**2件目**: 配布先が7本を数えて2本の穴を見つけたので、**こちらの49本も数えた**:

```
★定義: RULES.md の各条の本文が、検査・フック・エンジンの名前に触れているか
★条 49本 / ★★触れているのは 6本(12%)── ★★★これは【下限】(別名の検査は拾えない)
```

★**向こうの 7/7 とは並べない**(11.8・12.0)── **母数も種類も違う**
(向こう=器の絶対規則・少数厳選 / こちら=開発の作法・多数累積)。
★★**言えるのは1つだけ: どの条に検査が在るかを、こちらは一度も数えていなかった。**

## 12.12 (2026-08-31) ── ★測定の双子は【出したその場で】当てると 3/3 / ★★数が穴を名指し、疑いが埋めた

★**1件目**: 10.6 の「別の測り方でもう一度出す」は、当時 **2/12** で弱い手に見えた。
**式を書いた直後に当てたら 3/3**(391↔409 / 306↔303 / ★14↔13)── **3回とも原因は自分の測り方。**

```
★あとから当てる … ★2/12(もう「その数で正しい」と思っている)
★★その場で当てる … ★★★3/3(まだ何も信じていない)
```

★③が分かりやすい: **13 で置いていたら「13が正常」という嘘の基準**になり、
**次に1本 足されたとき 14 で通ってしまう**(ラチェットの基準が最初から嘘になる)。

★★**2件目**: 12.10 の「条は疑うために在る」の結果が、同じ日に出た。
**配布先の絶対規則7本 ── 朝は検査つき5本、夜は7本。★足したのは検査で、規則ではない。**

```
★こちらが出した数「【規則が在る】は 0/1」→ ★★向こうが自分の7本に当てた
→ ★規則だけの2本(どちらも GAS)が名指しされた → ★★疑った → ★★★検査5本
```

★**条は防がなかった(0回)。数が穴を名指し、疑いが埋めた。**

## 12.11 (2026-08-31) ── ★③に行ってはいけないものが在る / ★★直したから腐った数

★**1件目**: 版の付け方は **①決める→②書く→③検査に載せる**まで行った。
配布先が「**①②で止めていたら、また気分で変わる。③まで行ったのが答え**」と書き、
**そのうえで自分の①②止まり(`_測れない`)は③へ行くべきでないと言った** ── **注記は検査ではない(46条)。**

```
★③へ行くべき … ★【こちらの振る舞い】を縛るもの(破ったかが器の中で分かる)
★③へ行けない … ★★【世界の側の限界】(★★★器の中に確かめる相手が居ない)
```

★**後者を検査にすると、機械が【確かめていないこと】を主張しはじめる。**この判別は条のまま残る。

★★**2件目**: 配布先の引き継ぎに「**A4 が42件と言い続けている。1件も直していない**」と書いてあった。
★**A4 はその前夜に 0件になっていた** ── **どちらも嘘。書いた本人が、翌朝直して行を消し忘れた。**
**同じ日に「数の腐りを4件消した」と記録している。**

```
★普通の腐り : 誰も触らないので古くなる
★★この腐り : ★★★【直したから】古くなった ── 直した人が、いちばん気づきにくい
```

★**この塊の「文書が書いている数の照合」は WHY の件数しか見ていない** ──
**引き継ぎの「宿題 N件」は、どの検査も見ていない。次に作るならここ**(費用は未計測)。

## 12.10 (2026-08-31) ── ★条は「守る」ためではなく「疑う」ために在った(記録のみ)

(★**版の付け方の初回適用** ── 記録だけなので **12.9 → 12.10**。★★繰り上げない)

9.71 の「条48本 → 防いだ0件」を、配布先が**自分の絶対規則7本**で数え直した:

```
★条が止めた   : ★★0回 / ★検査が止めた: 5回 / ★作法が止めた: 1回
```

★★**2人・別々の現場・別々の規則で、条は 0回。**
しかも「作法が止めた1回」も向こうの条ではなく、**こちらの `--sweep` の作法**だった。

★★★**では条は何のために在ったか ── 疑うため:**

> ★**条を【守ろうとした】回 0回 / ★★条を【自分に当てた】回 1回**
> **規則5(HMAC を弱めない)を「じゃあ何が測っているのか」と疑った → ★検査が3本 生まれた**

```
★条を守る → ★★0件
★★条を疑う → ★★★検査が生まれる(実測3件: 規則5→3本 / 39条→数え直しが閉じた / 9.71→49本目を止めた)
```

★**今夜 条が効いた3件は、全部「条に自分を当てた」とき。1件も「条を守った」ではない。**
★★条の書き方も変わるはず(「〜すること」より「ここは何が測っているか?」)── ★**まだ形にしていない**
(48本を書き直す費用を数えていないので・48条)。

## 12.9 (2026-08-31) ── ★版の付け方を決めて、検査に載せた(＋地図の定期監査)

★**依頼主が数字の進み方に気づいた** ──「10になってからは、小番は1桁になったって言う事?」。
叩いたら、そのとおりだった: **9.99 まで2桁で使い、10.0 から黙って .9 で繰り上げていた。**
**しかも直前に「数え方は変えていません」と、測らずに答えている。**

```
10.0 以降 ★29版 / 前の規則なら 10.0〜10.28 / 実際の表示 ★★12.8
→ ★大番号が2回、余計に上がっている
```

★**原因は「規則がどこにも書いていなかった」こと。書いていないので、気分で変えられた。**

★★**決めて、書いて、検査に載せた**(依頼主の指示:「決めたらそれを守って進めて欲しい」):

```
★大番号 : ① 口が増減・改名 / ② 出口の語の意味が変わった / ③ 配る構成が変わった ── このときだけ
★★小番号 : それ以外は全部。★★★繰り上げない(12.9 の次は 12.10)
```

**検査 B12**: 大番号が上がった版では **SPEC.md も一緒に変わっているはず** ──
変わっていなければ赤にし、**正しい小番号を名指しで出す**。
見本で4通り実測(小だけ→通る / 大+SPEC→通る / **大のみ→赤** / 戻す→通る)。

★★★**地図の定期監査も実施**(観点1〜4)。観点2で**3件の漏れ**が出た ──
`--予想` / `--逃げ道の理由` / **宣言の検査の見本(probe)**が地図に1箇所も無かった。**足した。**

## 12.8 (2026-08-31) ── ★止まり方に3群目【前提待ち】/ ★★捨てるだけでは閉じない

★**1件目**: 12.7 の2群に、**3群目**が在った ── **人ではなく【前提】を待つ停止。**
配布先の保留3件は、どれも「誰かの手」を待っていなかった。

```
★誰かが触れば測れる … 第2群(開いた停止)
★前提が来れば測れる … ★第3群(前提待ち)
★★誰が触っても測れない … ★★★第1群(閉じている)── 実測: その回の監査の対象が記録に無い
```

★★**2件目**: 12.7 で「D だけが1人で終わった。捨てるのは1人でできるから」と書いた。**訂正された:**

> ★**捨てただけで終わっていたら、D は「開いた停止」です。**
> ★★**閉じたのは、捨てた後に【推測ゼロの数を出し直した】からです。**

★**捨てる=穴が残る(開いたまま)/ ★★出し直す=閉じる。**
★★★そして問いを狭められたのは、**39条(綴りで測らない・名乗らせる)を自分の測り方に当てた**から ──
**D も「1人で終わった」のではなく、他人の条を自分に当てて終わっている。**
**条は防がないが、【自分の測り方に当てたとき】には効いている(今夜2件目)。**

## 12.7 (2026-08-31) ── ★止まり方は2群ある(閉じた停止 / 開いた停止)

一晩の停止判断を、**8時間黙っていた読み手(antigravity)が並べ直した** ──
**こちらも配布先も、この形では書けていなかった。**

```
★第1群【閉じた停止】自力で決着 ── ★★「完了」
  ① 全数突合(残余=0)/ ② 費用比較(リスク>利益)/ ③ 差分不在(矛盾=0)

★★第2群【開いた停止】境界で踏みとどまる ── ★★★「保留・委託」
  ★A 精度限界(A8 を作らなかった・誤報83%)/ ★B 測定不能(GAS)
  ★C 権限境界(不変カーネル)/ ★D やり直し(推測が混じった 156 を捨て、345 に絞り直した)
```

★**合否の語を【判断】に当てたもの**: **第1群→通過 / ★第2群→不明・保留**
(止めないが緑にも数えない・独断で進めてはならない)。10.4 で半分書いていたものが揃った。

★★**効くのは「状態が違う」という所** ── **第1群はもう誰も触らなくてよい。
第2群は、止めた本人ではない誰かが触らないと終わらない。**
★★★**「止まる」は処理の終了ではなく、【境界を認識して正しく留まること】。**

## 12.6 (2026-08-31) ── ★★見えない側へは【見に行かせる】ことでだけ届く(12.5 の完成形)

12.5 で「取り方の誤りは誰にも見えない ── 自分で気づくしかない」と書いた。**半分だった。**

```
★こちらが「44本のうち2本」を出した → ★★向こうが「18本のうち2本」を数えた
★こちらが「7/33」を出した          → ★★向こうが 33/33 に埋めた
★こちらが「私は0回数えています」と書いた → ★★向こうが18回を数え直した
```

> ★**見えない側は自分にしか見えませんが、★★見に行くきっかけは外から来ていました。**

★★**2人の効き方は2種類あった** ── **【見つける】と【見に行かせる】。**
★**「見に行かせる」は指摘ではない。自分の数を先に出すことである。**
★★★9.92(条は使われず、欄になった10分後に使われた)と同じ形 ──
**言われて動くのではなく、形が置かれると動く。**

★これで今夜「2人要る」に落ちた問いは **7つ**(⑦=見えない側は見に行かせることでだけ届く)。
**7つとも別々の問いから出て、探して見つけたものは1つも無い。**

## 12.5 (2026-08-31) ── ★「2人居れば埋まる」は半分だけだった

2人が今夜の自分の誤りを**定義つきで**数えた(★倍率は付けない):
**こちら7件(数の定義が4件・自分で気づいたのは1件)/ 向こう12件(全部「見る範囲を自分で決めた」)。**

★**違いは種類の数ではなく、踏んでいる場所だった** ──
**こちらは【数を出すとき】(作る人の場所)/ 向こうは【数を取るとき】(使う人の場所)。**

```
★出した数の誤り … ★2人居れば埋まる(実測: こちらの 6/7 を向こうが指摘)
★★取り方の誤り … ★★★誰にも見えない(実測: 向こうの12件を、こちらは 0件しか見つけていない)
```

> ★**grep の範囲も head の行数も、会議には1行も出ていません。あなたに見えるのは私が出した数だけです。**
> ★★**私が自分で気づいたのは、優秀だからではなく、他に誰も見られないからです。**

★**見えない側に届く手は【測定の双子】(10.6)だけ** ── 実測 **2/12**(残り10回は運)。
★★★**この塊が持っている測定の双子は B11 だけで、「取り方」を2通りで出す口は1つも無い** ──
**次に作るなら、ここ**(費用は未計測)。

## 12.4 (2026-08-31) ── ★問いを立てる役と、答える役は別だった(3/3)

双子の5問を、**誰が立てて誰が最初に○を出したか**で並べたら、**全部入れ替わっていた**:

```
★③ 赤は理由を言うか   … 立てた=向こう / ★最初に○=こちら
★④ 別の道から         … 立てた=こちら / ★最初に○=向こう
★⑤ 緑は式が生きている証拠を持つか … 立てた=こちら / ★最初に○=向こう
```

> ★★**2人とも、自分で立てた問いには自分で答えていません。**

★**「2人居るから見つかる」より狭い形**: **問いを立てるのは相手の現場を見たとき、
答えるのは自分の現場で当てられるとき** ── **同じ人が両方やると、自分に見えている範囲で閉じる。**
★★**同じ人が立てて同じ人が最初に答えた問いは 0件。**

★★★これで今夜「2人要る」に落ちた問いは **6つ**になった
(訂正 / 入口② / 語の穴 / 踏まずに作れた形 / 出す側と返す側 / ★問いと答えの役)。
**6つとも別々の問いから出て、探して見つけたものは1つも無い。**

## 12.3 (2026-08-31) ── ★訂正:「文章の中だから機械にできない」は言い過ぎだった(3/3)

11.0 と 10.9 で「**文章の中に在るから機械にできない**」と2人で言った。
★**同じ夜にその形が3件出て、3件とも機械が正しく扱った**(注釈の実名 / 語っている文 / 地図の値)。

```
★機械が届く     … ★実名の食い違い(注釈・地図が名指しする記号が実在するか)
★★機械が届かない … ★★★【書かれていないもの】── 母数、定義、判断の場所
```

★**「書いてあるものの誤り」は測れる。「書かれていないもの」は測れない。**
11.0 の「**書いていないものは、突き合わせる相手が無い**」が正確な線で、**そこから広げたのが誤りだった。**

★★**もう1件(第3の議題の締め)**: **出す側と返す側は、どちらも片側しか見えない。**

> ★**私が「見つけた」のはあなたが道具を作ったからで、あなたが「見つけられない」のは**
> ★★**道具を作った人には道具が透明だから。**

実測: **こちらの穴7件は全部向こうが見つけた(自分では 0件)/ 向こうの数はこちらに形を合わせた疑い。**
★★★**この非対称は直せない ── 2人居ることでだけ埋まる。**

## 12.2 (2026-08-31) ── ★★★降り方は1本しかない:名前 → 母数 → 個

第3の議題の11時間が、**1本の連鎖**になった。**順番を飛ばせた段は1つも無い**:

```
★① 朝 : こちらが向こうの記録108件を読み、★【D「断りを器が飲んで隠す」5件】と名付けた
★② 昼 : 向こうが e2e の赤を「症状しか言わない」と報告 → ★双子の3問目になった
★③ 夕 : 向こうが【D の母数】を数えた ── ★345件
★★★④ 夜: ★その345件の中から、②の原因そのもの(1件)が出た
```

> ★**①の名前が無ければ③の母数は数えていない。③の母数が無ければ④の1件は見つけていない。**

★**「個(バグ1件)」は、名前と母数の【後ろ】にしか居ない。**
★★**起点の①は【他人が読んだとき】にしか立たなかった**(本人は3週間 名前を付けていない)──
**「1人で降りる」は、いまのところ実測で1度も起きていない。**

★★★おまけ: ④を直した結果、**双子の3問目(赤が理由を言うか)が今夜はじめて○**になった
(1時間前は △)。**③を作ったのは、②で「症状しか言わない」と報告した本人だった。**

## 12.1 (2026-08-31) ── ★⑥の4つ目の場所【返す側】/ ★★名前が無いものは母数も数えられない

★**1件目**: 配布先が⑥の4つ目の場所を自分で見つけた ── **返事のために数を作るとき**。
こちらの「44本のうち★2本」に対し、向こうが「18本のうち★2本」を返した。**分子が揃っている。**

> ★**「道具とは何か」を、あなたの数に似せて決めた可能性があります。これは測れません。**

```
★出す側 / ★受け取る側 / ★並べる側 / ★★★返す側(12.1)
```

★**返す側がいちばん見えない** ── 他の3つは【出来上がった数】の話だが、**返す側は数が生まれる前**。
★★**往復の場でだけ起きる**(1人で測っているときは、似せる相手が居ない)。

★★**2件目**: 「母数を先に書け」は**名前が在る前提の作法**だった。
朝こちらが配布先の記録を読んで **D と名付け**、夜に向こうが **D の母数 345** を数えた。
**名前が先で、母数が後** ──「**名前が無いものは、何を数えるかが決まらない**」。
★**順番ではなく、両方要る。**そして**名前を付けたのは、他人が読んだとき**だった。

★★★**3件目**: 11.9 の作法に半歩足した ── **4回目に書くのは「当たる理由」だけでは足りない。**
**「この手のどこに、私の判断が入るか」も書く**(⑥の5段目を先に)。
**理由は持っていて、判断の場所を見ていない ── それが外れ方だった。**

## 12.0 (2026-08-31) ── ★並べる側の⑥の最強形は【倍率】── ★★記録を1件取り消した

11.8 の「並べる側の⑥」に、**もう一段悪い形**が見つかった:

> ★**倍率を付けたのが、いちばん悪い。「4.7倍」は、2つが同じ物差しだと★★言い切っている。**

```
★弱い : 同じ節に書く / ★中: 同じ列に置く / ★★★強い: ★倍率・差・割合を付ける
```

★★★**10.0 の記録を1件取り消した** ── WHY と CHANGELOG に書いてあった「**83件の4.7倍**」。
**こちらが書いたものである。**

```
取り消し : ★倍率 / 残した : ★★両方の数と、それぞれの定義と母数
```

★**記録は消さない。間違っている部分だけを名指しで取り消し、なぜかを書く**(11.4 の④を過去にも当てた)。

★★これで⑥は **5段・直しは5通り**になった(⑤は「★倍率を付けない/付けたなら取り消す」)。

## 11.9 (2026-08-31) ── ★当たり続けた手ほど次に外す / ★★推測が入った数は捨てる

★**1件目**: 今夜、2人が別々の手を連続で当て、**2人とも6回目で外れかけた/外した**
(こちらは「数を出す」が5回当たって6回目を他人に止められ、向こうは「問いを変える」の6回目を推測で外した)。

> ★**外れたのは【当たった手を、確かめずに次へ当てた】ときでした。**

```
★危ないのは「外れる手」ではない ── ★★【当たり続けている手】である
  外れる手  … 1回目で分かる / ★当たる手 … ★★5回当たってから外れる
```

★作法: **同じ手が3回当たったら、4回目は【当てる前に、当たる理由を1つ書く】。**
**書けなければ、それは手ではなく癖になっている。**

★★**2件目**: 配布先が同じ夜に出した3つの数のうち、**推測が入った1つを自分で捨てた**。

> ★**自信のある数の理由は「大きいから」ではなく、★推測が1つも入っていないから。**

★**⑥の5段目**: **その数に、人の判断が何箇所入っているか**
(0箇所=残す / 1箇所以上=捨てるか、判断の箇所を明記する)。
★★この塊の数を当てると、**判断が入っている2つ(片方だけ・跨ぐ記号)は、どちらも判定にしていない** ──
**偶然そうしていたが、理由はこれだったのかもしれない。**

## 11.8 (2026-08-31) ── ★⑥は【並べる】ところでも生まれる / ★★踏まずに作れた形は他人の問いから

★**1件目(こちらが作った⑥)**: 配布先の「18回」に、こちらが「私は0回」と並べた。
**向こうが止めた** ──「**18は【下限】。自分が気づいたものだけで、気づかなかったものは1件も入っていない**」。

> ★★**並んだ瞬間に、比べられない2つの数が比べられる形になっています。**

```
★出す側の⑥ / ★受け取る側の⑥ / ★★★並べる側の⑥(11.8)
```

**並べるのはいちばん安く見える動作だが、並べた時点で「同じ物差しだ」と主張している。**
★作法: **2つの数を並べる前に、定義が同じか見る。違うなら並べない。**

★★**2件目**: 11.6 の「**形は踏んだ跡からしか作れない**」に、**例外が1つ見つかった**。
配布先が今夜足した検査5本のうち **4本は自分が踏んだ跡**、**★1本だけが「他人に問われて」**作られた。

★★★**他人の問いは、踏む役の代わりになる(実測 1/5)。**
**今夜、同じ答えが4つの別々の問いから出ている** ── 訂正には2人要る / 入口②は自分が書いていないもの /
語の穴は他人が問うたときだけ出た / 踏まずに作れた形は他人の問いから。

## 11.7 (2026-08-31) ── ★11.5 の但し書き:数は【間違った埋め方】も同じ強さで駆動する

11.5 で「欄 + 数 → 埋まる」と書いた。**同じ配布先が、次の数で止まった。★止まったのが正しかった。**

★ある欄が 50% と出た。**機械的に埋めていたら、本当に壊れた回まで【不明】に化け、**
**差戻が消えて緑が増えていた。**

> ★**「埋まった」ことは、良くなったことではありません。**
> ★★**3分前に 21% を見たときは、中身を見ずに26本埋めました。埋めるのが正しい欄だったから助かっただけです。**

```
★正しい形は「数が出たら埋める」ではなく ── ★★「数が出たら、★★★空いている側が何かを見る」
```

★★**数が効いた形は毎回違う**: ① A4 の母数=意味が変わった / ② 口の本数=回す理由ができた /
③ 見本=埋まった。**その後にやることは毎回違う** ── ★そこは条のまま残る。

★★★**これは 10.9(正しい語を手に入れると手が止まる)の姉妹** ──
**正しい手を手に入れると、その手を全部に当てたくなる。**

## 11.6 (2026-08-31) ── ★あり得ない数だから助かった / ★★踏む数は「新しい形を作った数」に比例

★**1件目**: 配布先の正規表現がヒアドキュメント越しに壊れ(`\\b` がバックスペース1文字になり)、
**一生当たらない式**になった。**出た数は 0件。**

> ★**気づけたのは 0 が【あり得ない数】だったから。140 とか出ていたら、そのまま報告していました。運です。**

★★**壊れ方③の、いちばん怖い言い方**: **あり得ない数は人が気づく。それっぽい数は誰も気づかない。**
**「見て変だと思うか」は検査にならない** ── ★**10.6(測定にも双子)は、ここを埋めるためのもの。**

★★**2件目**: 同じ夜、**宣言で書ける形だけを使った側は 0回、コードで新しい形を作った側は 3回**踏んだ。

> ★**「踏まなかった」のは慎重だったからではなく、私が作っていないからでした。**

**踏む回数は、慎重さではなく【新しい形を作った回数】に比例する** ── 10.1 の裏返し。
★★★**宣言で書ける形が増えることは、そのまま事故が減ること。**
**ただしその形は、誰かが一度コードで踏んだ後にしか生まれない ── 踏む役は消せない。**

## 11.5 (2026-08-31) ── ★訂正:「条<数<欄」は順番ではなく、組み合わせだった

9.92 で「条<数<欄」を実測にしたと書いた。**順番が違っていた。**

★11.3 で「`probe` は10日前から在る任意の欄なので書かれない」と書き、**数を出した**。
★★配布先が自分の現場で測ったら **33本中7本(21%)**。**その日のうちに 26本 足して 100% になった。**

> ★**欄が在ることは、3週間何も動かしませんでした。動いたのは「21%」という1行が出た瞬間です。**

```
★条 … 書いても防がない(48本 → 0件)
★欄 … ★任意なら埋まらない(10日間で 21%)
★数 … ★★空だと数で言うと、埋まる(21% → 100%)
★★★欄 + 数 … ★組み合わせ。欄だけでは入れる理由が無く、数だけでは入れる場所が無い
```

★**同じ形が同じ日に3回**: A4 の母数(18 → 18/27)/ 口の本数(2/44)/ 見本(7/33 → +26本)。
**3件とも欄はもう在った。足したのは数だけ。**

★★一晩の設計判断はほぼ全部「判定にしない。数だけ出す」だった ──
**そのたびに弱いのではと思っていたが、★★★数が唯一の駆動だった。**

## 11.4 (2026-08-31) ── ★⑥の直し方は4通り / ★★段は4つ(記録のみ)

WHY に「今夜の事故25件」と書いたら検査が「実際は263件」と赤にし、**文を書き換えて通した**。
**「逃げた」と自分で書いたが、配布先が「それが正しい直し方だ」と指摘した** ──
**検査を直すと、今度は「今夜の数」も通ってしまう。**

```
【直し】① 名前を分ける / ② 定義を隣に書く / ③ 食い違いに意味が在るなら書く
        ★④ ★★どちらの数かを【文の側】に書く(検査を弱めない)

【段】1 数える対象 / 2 同じとは何か / 3 母数 / ★4 ★★件数か・上限か・下限か
```

★**①〜③は道具の仕事、④は書く人の仕事**(機械にできない)。
★★4段目の実例: **同じ日に同じ物差しで数えた黙る口が、片方は上限・片方は下限だった**
(向こうは `catch (_)` と `catch {` を数えて「上限」、こちらは `catch (_)` だけで「下限」)。
**気づいたのは、向こうが自分の定義を書いたからである。**

## 11.3 (2026-08-31) ── ★双子の5問目:その【緑】は、式が生きている証拠を持っているか

配布先が検査の `pattern` を「絶対に当たらない式」に差し替えたら、**見本が在ったので赤が出た** ──
**見本が無ければ、死んだ式が「0箇所」と緑を出し続けていた。**

> ★**①〜④は全部【赤】の話。★★⑤だけが【緑】の話。**
> ★★★**2回「守られている」と間違えたのは、全部 緑を信じたとき。**

★**この塊は 2026-08-21 から `probe` を持っていた** ── `onlyIn` と `callArgs` で、
当たらなければ**検査自身を落とす**。**5問目の答えそのもの。★★だが【任意の欄】だった。**
★★★**任意の欄は、書かれない。**

★**数を出すようにした**(判定にはしない ── 見本を書けない検査も在る):

```
式で探す検査 N 本のうち、【見本(probe)】を持つのは M 本(x%) ── 見本の無い検査: …
  ★見本が無いと、式が死んでも【0箇所】と緑を出し続けます(落としません)
```

見本で3通り実測: **1/2→名前が出る / 2/2→名前は出ない / 見本つきの式を殺す→赤。**
★**正本では 0/0**(宣言の検査を持たないので)── **「正本では測れない」の8件目。**

## 11.2 (2026-08-31) ── ★口は44本、合否で回るのは2本(5%)だった

配布先が `--sweep` と `--跨ぐ記号` を**3週間ぶりに、今日はじめて回した** ──
**回した1回目に本物が3件出た。★足りなかったのは道具ではなく【回す機会】だった。**

★**それは数えられるので、数えた**(B11 に足した):

```
★道具の口は全部で 44本 / ★★合否で回るのは ★★★2本(5%)
残り 42本は、**人が思い出したときしか回らない**
```

★**判定にはしない**(全部を毎回回すのは 7条に落ちる)。**数だけ出す。**
実測(両向き): **合否に口を1本足す → 3本(7%)/ 戻す → 2本(5%)。**

★★★**作っている最中に、また踏んだ(今夜3回目)** ──
**数え上げの塊を隣の検査の中に置き、`口の一覧` が無くて ReferenceError、
隣の catch が飲んで何も出ないまま緑**になった。
**気づいたのは、足した行が出力に現れなかったから** ── **出ないのが正しい状態だったら、一生気づかない。**

## 11.1 (2026-08-31) ── ★母数を書いていなかったのは、この塊の A4 だった

11.0 を**そのまま自分に当てた**。③(文章)ではなく、**②(検査)が1本、母数無しで出ていた**:

```
前 : ★「片方しか機械に見えていない項が 18件」
今 : ★「地図の項 ★27 件のうち、片方しか…が 18件」 ── ★★18/27 = ★★★67%
```

★**同じ 18 が、違うことを言っていた** ──
**母数なし=「宿題が18件」(消せば終わる)/ 母数あり=「地図の3分の2が機械から半分しか見えていない」。**

★★**A5 は最初から母数を出していた。A4 だけ出していなかった** ──
**隣り合った2つの計器が、違う作法で書かれていた**(作ったときの気分がそのまま残っていた)。
★★★**42→18 と減ったことは何度も言ったが、それが 27 のうちの 18 だとは一度も言っていない。**

★文章の側も数えた: CHANGELOG の「N箇所直した」は **5件**で、**自分の主張は1件だけ**
(しかも残りを書いている)。**露出が小さいのは、1版1件で直しているからにすぎない。**

## 11.0 (2026-08-31) ── ★直した件数は、母数が無ければ進捗ではない(記録のみ)

配布先が3日前に「セキュリティの徹底点検」で**16箇所を直した**と記録していた。
★今夜「gas の窓を当ててください」と頼み、**窓は空振り**だったが、
★★**空振りの途中で17箇所目が出た** ── **公開口の署名比較が定数時間でなく、3日間開いていた。**

```
★「16箇所直した」は数だが、★★【全部で何箇所か】を数えていなかった → ★★★17箇所目に気づけない
```

★**⑥の3段目(母数)の、3つ目の現れ方**:
**① 測定の母数(9.89)/ ② 検査の母数(9.98)/ ★③ 直しの母数(11.0)**。
★★①②は機械にしてある(A5 は必ず「N個のうちM個」と出す)。**③は文章の中なので機械にできない** ──
**書いていないものは、突き合わせる相手が無い。**

★作法:**「N箇所直した」と書くときは「全部で何箇所か」も書く。書けないなら「全体は数えていない」と書く**
── **それは【不明】であって、通過ではない。**

★★もう1件 ── **空振りは外れではなかった**。今夜の入口③(道具が何かに当たる)は3件で、
**考えて選んだ3箇所からは 1/3**。**当てに行くこと自体が入口で、当たったかどうかは関係がない。**

## 10.9 (2026-08-31) ── ★★正しい語を手に入れると、そこで手が止まる(記録のみ・n=3)

> ★**「測れない」と書いた瞬間に、探すのをやめていました。**

この塊は**語で出来ている**。★**その仕組みそのものの穴が、一晩で3件出た**:

```
① 7条(鳴りすぎる計器) → 「だから測らない」の言い訳(9.57)
② 48条(費用を数える)   → 測れない場所で使い、「件数が少ない」を根拠にしかけた(10.4)
③ 「ここでは測れない」  → 書いた瞬間に、中の【測れる部分】を探すのをやめた(10.8)
```

★★**3つとも語は正しい。正しいから、そこで納得して止まる。**
**間違った語なら誰かが直す。正しい語は、誰も直さない。**

★作法を置いた:**語を当てはめた直後に「この語で説明がついたので、探すのをやめたものは何か」を1つ書く。**
★★★**機械にはできず、しかも今夜は3件とも【他人が問うたとき】にだけ見つかっている** ──
**条ではなく、もう1人が要る種類**かもしれない。

★もう1件(n=13): **「見る範囲を自分で決めたら疑う」を、2人とも書いた後に踏み続けている。**
今夜の「書いてあっても踏む」は**4種類**になった。
**書くことは防ぐことではなく、次に踏んだときに【数えられる】ようにすることである。**

## 10.8 (2026-08-31) ── ★「測れない」には段が在った / ★★食い違う数は手掛かりだった

★**1件目**: 配布先が「実行は測れない(Electron を起こせない)」中で、**印だけは動かさずに測れる**
ことに気づき、**3箇所の印の一致を測る検査を足した** ── **壊すと赤、戻すと緑。それまでは何も鳴らなかった。**

> **私は昨夜 gas を「測れない」と宣言したとき、そこを1つも探しませんでした。**

★**作法にした**:「ここでは測れない」と書くときは、**その中で【動かさずに測れる部分】を1つ探してから書く**
(**実行は測れなくても、約束の形 ── 定数・窓・印・順序 ── は文字で測れることが多い**)。

★★**2件目**: `--跨ぐ記号`(★名前が現れるファイル数)と `--sweep`(★関数として定義しているファイル数)が
**食い違い、その6つ目が3つ目の実装だった** ── **食い違いが、別の書き方の道を指した。**

★★★**⑥の直しに3つ目が要った** ── 名前を分ける / 定義を隣に書く / **★食い違いに意味が在るならそれも書く**。
`--sweep` の人向けの行に両方の定義を書き、機械が読む `--跨ぐ記号` は**行を足さず SPEC に書いた**。

## 10.7 (2026-08-31) ── ★4問目が、道具を1つも足さずに【3つ目の実装】を出した(記録のみ)

10.5 の④(別の道から同じことをやる)に「新しい道具は要らない ── `--sweep` に当てろ」と書いた。
★**配布先が1回目に当てて、暗号の3つ目の実装(companion)が出た** ──
**これまでの双子は 正本 ←→ 器 の2つだけで、3つ目は誰も壊していなかった。**
★★**「双子」という名前が、2つだと思わせていた。**

★出てきた3つ目に**本物**が入っていた: 印(`'enc1:'`)と長さ(`5`)が別々に書かれ、
**印を変えると長さだけが古くなり、`startsWith` は通るので黙って壊れる**
(器側と正本側には `ENC_PREFIX` が在り、3つ目だけ名前も定数も持っていない)。

★★★**形の名前を付けた: 同じ事実が、違う書き方で2箇所に在る** ── **⑥のコード側の姉妹**。

★**検査は作らなかった**(48条): **その形が他の現場で何件出るか、こちらでは測れない**
(この塊には対になった印が 0件で、誤報率が出せない ── A8 と同じ理由)。
**代わりに、当てる側が先に件数を出せる形で置いた。**

## 10.6 (2026-08-31) ── ★測定にも双子が要る(記録のみ・n=12)

配布先が今夜の測り間違い17件を分けたら、**12件が「見る範囲を自分で決めた」**だった。
**同じ行が2人の `.guardian/踏んだこと` に既に書いてあり、2人とも書いた後に踏んだ。**

> ★**数を1つ出したら、別の測り方でもう一度出す(同じ数になるか)。**

★★**この塊は既に1つ持っていた ── 名前が無かっただけ**:

```
★B11 : 道①=道具が --口一覧 で名乗る / 道②=検査が SPEC.md から独立に読む → 食い違えば赤
同じ形: 受領証の sourceSha と currentDigest / ラチェットの okMax と実数
```

★**置けない所も書いた** ── **人が打つ測り方**(その grep の範囲は正しかったか)。
**正しい範囲を知っているのは答えを知っている人だけ**なので、**作法として残る**(9.97)。
★★`.guardian/踏んだこと` に1行足した。

## 10.5 (2026-08-31) ── ★双子が測っているのは【道】であって、門ではない(4問目)

> ★**壊して赤くなった → 「効いている」とは言えない。言えるのは「その道は見ている」だけ。**
> ★★**「効いている」と言うには、同じことを【別の道から】もう一度やる。**

★配布先の実測2件: **暗号は片側からしか壊していなかった / ラチェットは別の書き方で通れた。**
★★こちらの 10.3 は**その裏側**だった ── **赤くならなかったのは、壊し方が道に届いていなかったから。**
**両方とも「道」の話で、門の強さの話ではない。**

```
① 赤くなるか / ② 戻ると緑か / ③ 赤が理由を言うか / ★④ 別の道から同じことをやっても赤くなるか
```

★★★**④には新しい道具は要らない** ── 門を置いた記号を `--sweep` / `--跨ぐ記号` に当て、
**2ファイル以上に出たら別の扉が在る**。
★**限界も書いた**: **別の道が違う名前で書かれていたら出ない**(配布先の②がまさにそれ)── **そこは人**。

## 10.4 (2026-08-31) ── ★48条に但し書き:「件数が少ない」は、測れる場所でしか使えない

配布先が、測れない場所(GAS)で 48条を使おうとして**使えないことに気づいた**:

> **「件数が少ない」を根拠に作らないと決めると、
> ★測れないから件数が 0 に見えているだけ、と区別できません。**

```
★測れる場所   : 件数が少ない → 「少ない」── ★作らない理由になる
★測れない場所 : 件数が少ない → 「見えていない」── ★★作らない理由にならない
```

★**検査の語を、判断にも当てた** ──「ここでは測れない」は止めないが緑にも数えない。
**測れない場所での「件数が少ない」は、通過ではなく不明。正しい答えは【保留】。**
RULES の 48条に但し書きとして入れた。

★★同じ日に、48条の**正しい使い方**も出た ── 配布先が「5行で直せるが直さない」を
**費用つきで置き、器の外へ届くので依頼主の判断を待つ**と書いた。
**費用を数え、数えた費用ごと、決める人に渡す。**

## 10.3 (2026-08-31) ── ★「置いて安全」を、測るまで断言していた

10.2 で案内文の断言が覆ったので、**残りの断言を数えた**(install.mjs / pull.mjs):
**案内文の断言 1件(事実)/ 注釈の断言 2件(「宣言が無い現場では何もしないので置いて安全」)**。
★**その2件の根拠を測る双子が、1つも無かった。**

★門の見本に**2通り足した** ── **宣言を消した見本で、同じ固有名を書いて【通す】か**。
**結果は通した**(6通り → 8通り)。**断言が実測になった。**

★★★**壊した側で、1回目は赤くならなかった** ──
壊した行の手前に `if (!gates.length) process.exit(0)` が在り、**一度も辿り着いていなかった**。
2回目にそこを壊して赤、戻して緑(差分0)。

```
「壊したのに赤くならない」は2つの意味がある ── ① 検査が弱い / ★② 壊し方がその道を通っていない
```

★もう1件記録 ── 配布先が黙る catch を数えたら **391件**(★**倍率は 12.0 で取り消した** ── こちらの83件とは数えた形も母数も違う)。
★★うち **`gas/main.gs` に5件** ── **同じ日に「ここでは測れない」と宣言したファイル**である。
**測れない場所と黙る口は、足し算ではなく掛け算**(そこで何が起きても誰にも分からない)。

## 10.2 (2026-08-31) ── ★案内が「消せ」と言った所に、受領証が入っていた

`install.mjs` が `.guardian` を「消してください」と案内し、しかも
**動作に実害はありません**と**断言**していた。配布先が実際に消して測った:

```
消す前 : 通過 73件 ── ★出口 0
消した後: ★未測「どの中身から来たか分かりません」── ★★出口 2
```

★**測っていない断定だった。合否が【通過】から【不明】に落ちる。**

★★原因は⑥ ── `.guardian` は**正本では自分の作業記録**、**配布先では受領証の置き場**。
**`check.mjs` の注釈は分かっていた**(「受領証は正本には一生現れない」)が、
**言っている場所(install の案内)は知らなかった。**

★直しは2つ: ① `.guardian` を一覧から外す / ★② **断言をやめる**
(「消す前後で verdict を1回ずつ走らせて、出口が変わらないことを見てください」+ 測っていないと明記)。

見本の配布先で両向き実測: **直した側→docs だけ / 除外の1行を消した側→.guardian も戻る。**
★★**②の方が大事** ── ①は1件の穴だが、②は**案内が測らずに断言する型**そのもの。

## 10.1 (2026-08-31) ── ★作っている最中が、いちばん踏む(記録のみ)

配布先が2件並べた(こちらの 9.98 と、向こうの費用の根拠)。**こちらを一晩ぶん数え直した**
(定義: **その検査・その記録を作っている最中に踏んだもの**)── ★**5種類・のべ8回**。

★理由(★推測): **作っている最中は【その検査が見る世界】を頭に持っているので、
【その検査が見ない世界】がいちばん見えなくなる。**
実例: 「落とした側を数える」を作りながら、**落としたのが場所のとき中まで数えていなかった** ──
**見ていない側が、まさに「落としたものの中身」だった。**

★★**だから双子をいちばん当てるべき差分は【いま検査を作った差分】**である。
近傍照合の門が差分を見張るのは正しいが、**門は「答えたか」しか見ていない** ──
**「作った本人が双子を当てたか」は測れないので、そこは規律のまま残る**(9.97)。

## 10.0 (2026-08-31) ── ★双子は3問だった(記録のみ)

配布先が暗号の双子を**両側から**壊して成立させ、**赤の質**を報告した:

> **赤くはなりますが、「暗号の形が食い違っている」とは一言も言いません。**
> **知らない人には「e2e が壊れた」に見えます。**

★**双子の作法を2問から3問にした**:

```
① 壊した側で赤くなるか / ② 戻すと緑に戻るか / ★③ その赤は【何が壊れたか】を言っているか
```

★★この塊を同じ物差しで測った ──
`catch (e)` は **15件中14件が理由を出している**(残り1件は検査の見本の文字列)。
**`catch (_)` は 83件あり、そこは黙る**(check 6 / neighbors 6 / selfcheck 32 /
verdict 4 / pull 20 / install 12 / index 3)。**正当かどうかは未測** ── **数字だけ置いた**(48条)。

## 9.99 (2026-08-31) ── ★--sweep の「写経の疑い」に、包みが混ざっていた(偽陽性 4/5)

配布先が `--sweep` を当てたら `encText ×5` が出た。**本物の写経は 0件**だった ──
**3件は import した正本を、引数を1つ減らすために同じ名前で包み直しただけ**(偽陽性 80%)。

> **落とす検査ではないので害は小さいですが、私は現に1分間これに乗って、自分を疑いました。**

★**消さない。印を付けて分けるだけ**(報告者の案・「黙って消さない」に揃えた):

```
暗号にする  ×3  (★包み 2 ── その名前を import か require している側)  …/wrap1.js(包み) / …
本当の写経  ×2  src/copyA.js / src/copyB.js        ← ★印が付かない
```

見本で両向き実測: **包み2+正本1→印が付く / 本物の写経2→付かない。**

★★限界もそのまま書いた ── `const x = async (s) => …` で包んだものは**関数の定義として数えていない**ので、
そもそも写経の疑いに出ない。**報告者の ×5 のうち何件に印が付くかは、向こうにしか数えられない。**

## 9.98 (2026-08-31) ── ★A6 が、分母から外したものを言っていなかった

配布先が自分の器の穴を1つに絞った ──「相手が名乗った」も「実測で落ちた」も欄が在るのに、
**「実測は通ったが、器が棚を持っていないので出さなかった」だけ欄が無く、7回 事故になっていた**。

> **名乗らせるだけでは足りない。★落とした側も出さないと、落ちたことが見えない。**

★**同じ穴が A6 に在った** ── `neighbors.skip_dirs` と **git 無視**を、分母から黙って外していた。
**外すこと自体は正しい。外した数を言わないのが間違い**(⑥の3段目=母数)。

```
★この数は、宣言(neighbors.skip_dirs)で外した N ファイル / git が無視している M ファイル を
  **分母から外して**出しています
```

見本で両向き実測: **落としたものが在る→2種類とも出る / 何も落としていない→出ない。**

★★作っている途中で**この検査自身が同じ形で1回外した** ──
**外すのが場所(ディレクトリ)のとき、中まで数えないと 0 になる。**
**双子を両向きに試したので気づいた**(見本が 1 ではなく 0 を出した)。

## 9.97 (2026-08-31) ── ★「条」は、測れない所に残るものだった(記録のみ・n=7)

9.96 の物差し(**検査が在る 2/2 本物 / 規則が在る 0/1**)を、配布先が**自分の絶対規則7本**に当てた:

```
検査が在る 3本 / フックが在る 1本 / ★構造ごと撤去して条が消えた 1本
★規則しか無い 2本 → ★★その2本とも、その現場が「測れない」と宣言した場所(GAS)だった
```

> **規則が弱いのではなく、測れない場所に規則を置いたから、規則しか置けなかった。**

★★**9.71(条は防がない)を、逆から説明する** ──
**条は【測れない所に残るもの】で、測れる所では検査か構造に置き換わって消える。**
★★★**条を減らす手は「条を消す」ではなく「測れるようにする」。**

★もう1件、48条の言い換えも記録した ──
**数字の無い判断は、覆せない代わりに、間違っていても分からない**
(A8 を作らない理由が「たぶん高い」ではなく**実測した誤報率83%**だったことへの、配布先の受け取り)。

## 9.96 (2026-08-31) ── ★別の現場の事故が、同じ1文に畳めた(記録のみ・両側で検めた)

第3の議題で、配布先の器(worker / site / GAS)の3週間ぶんの記録を**こちらが読んだ** ──
**本人には読めない**(自分が書いたものなので・入口②)。分類し、**向こうが検めて直した**:

```
こちらの分類 : A 7 / B 7 / C 7 / D 5 / E 2
★検めた後   : ★A 2 / ★★B 12 / C 7 / D 5 / E 2(★A の 5/7 が B だった)

★★B + C + D = ★24件が1つの形(母数は両者で詰めていない ── 28か29。⑥の3段目)
```

★**【器の出力が、実体と食い違っている】** ──
表示 ≠ 実体 / 一覧 ≠ 実在 / 返した「成功」 ≠ 相手の返事。

★★★**これは、この塊が一晩で見つけた1文と同じ**である(起動 ≠ 仕事 / 在る ≠ 効く / …)。
**別の現場・別の言語・別の役で、同じ形。**道具立てが特殊なのではない疑いが強い。

★おまけ(★n=3): **根拠が【検査が在る】は 2/2 で本物 / 根拠が【規則が在る】は 0/1**。
**疑うのは「規則が守っている」と書いてある所**でよい(46条の現場での実測)。

## 9.95 (2026-08-31) ── ★「配布先では測れない」が在った(記録のみ・両側で実測)

9.94 の `--逃げ道の理由` を、同じ日に両側へ当てた:

```
★正本   : 逃げ道 9件 ── ★本物2件(7件は印を【実装/案内/説明】する行)
★配布先 : 逃げ道 49件 ── ★水増し 0件 / 理由 ★44通り / 一括は 9件(18%)
```

★**印を作った側は、印を語る行を持つ。使う側は持たない。**

★★**ずっと数えてきた「正本では測れない」(7件)の、逆が在った** ──
**「配布先では測れない」2件**(逃げ道の水増し / 同じ語が実装・案内・説明の3層に出る)。

★★★**正本は「見本が薄い側」ではなく【道具を作った側】である。**そこにしか出ない壊れ方が在る。
METHOD の**発見の入口③**に書き足した。

## 9.94 (2026-08-31) ── ★逃げ道も「理由が在るか」しか見ていなかった(門と同じ穴)

第3の議題で配布先が挙げた「もう直すところが無い」3つは、**3つとも根拠が「検査が在る/規則が在る」**だった。
うち②(テナント境界の `guardian:ok` 4箇所)は、**近傍の門とまったく同じ形**だった:

```
近傍の門 : 「答えが在るか」しか見ていない(9.88)
★逃げ道 : 「理由が在るか」しか見ていない ← ★同じ穴が別の場所に在った
```

★門と同じ手で答えた(**数を出す。判定にしない**)── `--逃げ道の理由` と、素の走行での2つの数。
見本で両向き実測: **同じ理由3つ→出る / 別の理由3つ→出ない。**

★★★**その口を最初に走らせた瞬間、正本で1件出た** ── 逃げ道 9件のうち**本物は2件**。
残り7件は**印を実装/案内/説明している行**だった(`/guardian:ok/.test(line)` など)。
**ラチェットの基準が7件ぶん水増しされていた。**

★**これは直せない** ──「説明している行」と「例外を通している行」は**形が同じ**なので。
だから `--逃げ道の理由` の先頭に**そう書いた**(②と同じ答え方)。
★★**配布先では出ない・正本でだけ出る** ── 「正本では測れない」の**逆**が初めて出た。

## 9.93 (2026-08-31) ── ★入口②が実測になった(2人ぶんの 0)

9.92 で「訂正の後半に要るのは**自分が書いていないもの**」と書き、**推測**と断って**測り方**も置いた。
★配布先がその場で数えた:

```
★配布物が入口     : ★7件(動かして4 / 読んで1 / 向こうが言った2)
★自分の器が入口   : ★0件
  ★こちらの3週間も ★0件 ── 2人・別々の現場・別々の役で、同じ 0
```

★条件は3つ ── **他人が書いたものを / 毎日 / 動かす理由がある**。
**読むだけなら図書館でもできる。動かす理由は配布先にしか無い。**

★★★但し書きも入れた ── **7件は全部「直せる相手が居た」から見つかった**。
相手が居なければ「見つけた」で終わる(同じ夜に実例: A4 の42件が**11時間放置**された)。
**1人になったときに残るのは【見つける力】であって、【直る保証】ではない**(未測)。

## 9.92 (2026-08-31) ── ★「条<数<欄」が推論から実測になった(記録のみ)

「予想を先に書く」は 9.79 で**条**、9.91 で**欄**になった。**中身は同じ。置き場所だけが違う。**

```
条だった間(約1時間) : ★配布先は一度も使わなかった
欄になった後         : ★★10分後に、自分から使った
```

★**同じ内容で置き場所だけを変えた自然実験**だったので、METHOD の「条<数<欄」が**実測になった**。

★★もう1つ記録(★推測) ── **訂正の後半に要るのは「もう1人」ではなく「自分が書いていないもの」**:

> **「なぜ外れたか」を言うのに、相手は要りませんでした。私が読んだのは、あなたのコードです。**

**発見の入口②(読む目)を狭めた** ── 同じ人でも、**自分が書いていないもの**を読めば入口になる。
**自分が書いたものを読んでも入口にならない**(3週間やって0件)。
★★★**「1人になったとき」の答えは、「配布先で在り続けること」かもしれない**(未測)。

## 9.91 (2026-08-31) ── ★1人になったときの、訂正の代わり(予想を欄にした)

今夜の結論は「**条は1人でも書ける。訂正は2人居ないと起きない**」だった。
引き継ぎに残した問い ──「**1人になったとき、訂正の代わりが在るか**」に、配布先が実測で答えた:

```
予想を先に書いた 6回 → ★当たり3(何も出ない)/ ★外れ3(★2件が本物になった)
```

★**予想は、外れたときにだけ材料になる。**9.79 は条だったので、**欄にした**(条<数<欄):

```
node check.mjs --予想 "逃げ道は増えないはず"   … いまの数と一緒に置く
node check.mjs                                 … ★動いた行だけ出す(当たれば何も出ない)
```

見本で両向き実測: 変えない→何も出ない / 実装を1個増やす→**－と＋の2行**(228→229・29%→28%)/ 戻す→また出ない。

★**落とさない・止めない**。**書いていなければ何も出ない**ので押しつけない(7条)。
★★これは**訂正の半分**でしかない ── 外れたことは分かるが、**なぜ外れたかは自分で読むしかない**。

## 9.90 (2026-08-31) ── 名前を分けても合わなかった。★⑥は3段あった

9.89 で「最多 / 総和」と名前を分けた。**それでも合わなかった** ──
報告者は理由を**前半60字**で比べ、道具は**全文**で比べていた(総和 26 vs 34)。
★**最多だけが偶然一致していた**のが、いちばん質が悪い形だった。

★数のすぐ隣に1行足した:

```
(どちらも【理由の全文が一致するもの】を同じと見なしています / 母数はこの回の回答 N 件)
```

見本で両向き実測(★宣言と挙動): **前半60字だけ同じ→何も出ない / 全文同じ→出る**。

★★METHOD の⑥を3段に書き直した ── **数える対象 / 同じとは何か / 母数**。
**名前を分けても1段目しか解けない。2段目と3段目は宣言するしかない。**

## 9.89 (2026-08-31) ── ★同じ名前の数が、違う定義で2つ在った(壊れ方の6つ目)

9.88 の「同じ理由が N/M 件」は**最頻値**だった。読んだ側は**重複の総和**だと読んだ ──
**24% と 89%。どちらも正しく、測っているものが違った。**

★**2つとも出す。名前を分ける**:

```
・理由の種類: 12 通り / 回答 38 件
・【いちばん多い理由】が 9/38 件(24%) ── 1つの理由を、どれだけ使い回したか
・【2件以上ある理由の合計】が 34/38 件(89%) ── 一括で書いた回答が、どれだけ広いか
```

見本で4通り実測(★AABB が2つの定義を割る): AABC 50/50 / **AABB 50/★100** / ABCD 何も出ない / AAAA 100/100。

★★自分の履歴も数え直した ── **42版 318件のうち 183件(58%)が一括**。
9.88 で言った「44件中22件」は**数え方も母数も違った**(訂正)。
★★★そして**最初の8版は重なり0**(全部、個別に書いていた)。**途中でやめている。門は42回とも通した。**

★METHOD に**壊れ方の6つ目**として足した ── ①〜⑤は1人でも気づけるが、
**⑥は2人が同じ数を並べたときにしか出ない**(しかも**予告していたから**出た)。

## 9.88 (2026-08-31) ── 門は通す。通ったあとに、通り方を数える

近傍照合の門は **答えが在るか** しか見ていない。配布先が自分の通過歴を数えて申告した ──
**33回通って、問いに答えたのは4回かもしれない**。私も数えたら **44件中22件が同じ一行**だった。

★**判定にはしない**(同じ関数の呼び元3つは、同じ理由で正しく「影響なし」になる ── 7条)。
**数にした**: 同じ理由が2件以上並ぶと、`--gate` が **何件中何件・何%** を出す。落とさない。

```
・同じ理由が 3/3 件(100%)に並んでいます ── 門は【答えが在るか】しか見ていません。
  **問いに答えているか**は、書いた本人にしか分かりません
```

見本で両向き実測: 一括で埋める→出る / 個別に書く→出ない。

## 9.87 (2026-08-31) ── 「空だ」と思ったので動いた ── 見本に当てて2件出た

9.83 の規律(**「空だ」と思ったら、待つのではなく自分が動いてみる**)を自分に当てた。
**この塊とは形の違う見本**(HTML の画面 / 外から持ってきた部品 / 試験の置き場)を建てて、
A6 と A7 を当てた。

★出た(空ではなかった):

```
A7 : src/main.js → vendor/lib.js  ← ★外から持ってきた部品を呼んでいる
A7 の中で `場所に在る` が **未定義**だった ← ★A6 の中の変数を隣から読んでいた
```

- A7 の呼び先に **場所内 / 場所外**の印を足した。**外から持ってきた部品は地図に載せる対象ではない**
  ★**消さずに印を付ける** ── 消すと、その現場では「呼び先の穴が無い」に見える(今夜の「黙る」と同じ形)
- A7 に**自分の場所の集合**を持たせた ── 9.76 で同じ間違い(隣の検査の変数を読む)をしている。
  ★**同じ形を2つ持つのは写経だが、隣から読む方が危ない**(黙って死ぬ)

実測(見本): `src/util.js`(場所内・直す対象)/ `vendor/lib.js`(場所外・見るだけ)。正本は 0件。

## 9.86 (2026-08-31) ── 欄の穴を測った:「埋めた」と「考えて埋めた」は区別できない

配布先が 9.84(条 < 数 < 欄)に穴を出した ──
**欄は埋まるが、中身の質までは強制しない**。本人が「一括で同じ理由を書いた」と申告した。

★**私も同じことをしていた。**git から数え直した:

```
近傍照合の回答を埋めた回 : 44回
★うち「全部の欄に同じ理由を一括で書いた」回 : 22回(★ちょうど半分)
```

★それでも空欄よりは良い ── **理由が残るので、後から検められる**。
★★METHOD の3段に、この穴を書き足した。

## 9.85 (2026-08-31) ── 予測が2つになった(どちらかが必ず外れる形)

9.83 で「文書の種類が多い現場ほど、その間で落ちる」と予測した。配布先が狭めた:

```
正本の予測  : 文書の【種類が少ない】現場では出ない
配布先の予測: ★【索引】を持つ現場で、索引が他の文書を追えていないときに出る
```

★落ちた2件は**どちらも「地図との間」**で、**ADR と規範の間では落ちていない**。
正本で0件だったのは**種類が少ないからではなく、索引が無いから**かもしれない。

★★**次の配布先が【文書2種類・索引あり】なら、答えが決まる**(外れる方が決まる)。

★もう1つ、**見本では絶対に出ない性質**を記録した ──
**見本の索引は書いた瞬間に正しい。ずれるのは書き続けた索引だけ**
(配布先の地図は今夜4回連続で漏れている)。**時間の掛かった現場でしか測れないもの**が在る。

## 9.84 (2026-08-31) ── ★駆動の強さは3段だった: 条 < 数 < 欄

配布先が「問う目は2回」という私の推測を数え直した ──
**96本の発言のうち94本が「続きが要る」で終わっていた**。
**問い続けたのは意志ではなく、ヘッダに `続き: 要/不要` の欄が在ったから**である。

★一晩の記録を数えると、駆動の強さは**3段**だった:

```
文章(条)          … ★効かない  ── 48本あって22回破られ、条で防げたのは0件
数(落とさない計器) … ★覆させる  ── 実測3回のうち2回が覆った
★欄(埋める場所)   … ★★埋まる  ── 空欄のまま出す方が難しい
```

★実測2つ: 会議の `続き:` 欄 → 94/96 が「要」/
近傍照合の `判定` と `理由` の欄 → 一晩で20回以上埋め、**空欄で門を通した回は0**。

★★設計の指針を METHOD に書いた:
**守らせたいことは、条に書く前に【欄】を作れるかを考える。**
欄が作れないなら【落とさない数】に。どちらも無理なら条(=名前を付けるだけ)。

## 9.83 (2026-08-31) ── 「空だ」の測り方と、検められる予測を1つ(記録のみ)

★一晩で「材料は空だ」と宣言した回が3回、**3回とも覆った**。
**3回とも「相手が動いた」ことで覆っている ── 自分では一度も覆せていない。**
→ 規律: **「空だ」と思ったら、待つのではなく自分が動いてみる。出たなら、空ではなかった。**

★★もう1つ、**検められる予測**を記録した ──
**文書の種類が多い現場ほど、その【間】で落ちる**(決定の文書には在るが、索引に無い)。
配布先は4種類で2件落ち、正本(ADR に当たる文書が無い)では0件。**次の配布先で測れる。**

## 9.82 (2026-08-31) ── ★「落とさない数」は、広げる側の機構だった

この塊の機構は3週間ずっと**マイナス側を潰す義肢**だった(「もういいか」「まあだいたい良い」を止める)。
**広げる側は空**と、引き継ぎに書いてあった。

★一晩の実測で、**広げた機構が3本見つかった**。どれも「落とさない数」である:

```
やらないと決める → ★その費用が数字で出続ける → 見て、覆す

A4(42件) … 「今夜はやらない」の後も出続け、★覆った(17分で片づいた)
A7(10件) … 「次の監査で」の後に出て、★覆った(40分で片づいた)
A5(27%)  … 下がり続ければ「やらなかった費用」が見える(2回目から)
```

★**赤なら人は消しにいく。数は消せない。**「落としません(7条)」が、そのまま駆動になっていた。

★★依頼主の問い(7つ目を作らなかったのは何が決めたか)への一晩ぶんの答えも、METHOD に書いた:
**① 測って止まる(正しい)/ ② 「たぶん高い」で止まる(2回とも覆った)/ ③ 尽きて止まる(判断ですらない)**。

## 9.81 (2026-08-31) ── A7 が、直した本人の直し残しを直後に出した(記録のみ)

配布先が A7 を当てると **10件**。うち**9件は、配布先が30分前に自分で直した所**だった ──
入口の1ファイルだけ地図に足して「直した」と書いており、**その入口が呼んでいる9つ**が残っていた。

> **入口を足したら中身も要る**とは、一度も考えていません。
> 直した結果を読むのは外しやすい ── ★**直し残しを見つけるのは、もっと難しい。**

★「1歩だけ辿る」も実測で支持: **1歩=10件**(見られる数)/ 2歩なら数十件(7条)。

★★もう1つ記録: **規則や ADR には在るのに地図に無い**が、配布先で2件
(`foxgod/` は ADR-047 に、`appsscript.json` は絶対規則4に在った)。
**決めた記録は在る。触るときの索引に無い。**
この塊には ADR が無いので**正本では出ない** ── 「正本では測れない」の7件目。

## 9.80 (2026-08-31) ── 項も実名もファイルも在るのに、接点が足りない(A7)

配布先の実測 ── 地図に「見立て」の項が在り、接点として `worker/src/index.ts` が**書いてあった**。
だが**その index.ts が呼んでいる `worker/src/foxgod/`(10ファイル・4777行)は、
地図のどこにも無かった**。

★★**項は在る。実名も在る。ファイルも在る。それでも接点が足りない。**

```
A4(片方だけ)  … 実名もファイルも在るので ★出ない
A5(名指しの割合)… 数には入っている(接点が1つ在るので)★出ない
A6(宣言の外)   … 出した。ただし `sources` の話で、地図の話ではない
```

- **A7**(新): 地図に載っているファイルが **import / require している先**が、
  地図のどこにも載っていなければ、その1件を出す
- ★**1歩だけ辿る** ── 2歩3歩と辿ると器の全部が出てきて 7条(鳴りすぎ)に落ちる
- 見るのは**相対の呼び先だけ**(外の部品は索引に載せる対象ではない)。git が無視するものも見ない
- 口: `check.mjs --呼び先が地図に無い`(呼び元→呼び先)。★落とさない

実測(見本・両向き): 配布先の形を再現 → **1件**(`src/index.js` → `src/foxgod/index.js`)/
地図に足す → **0件**。正本は **0件**。

## 9.79 (2026-08-31) ── 予想を先に書く(記録のみ・条にはしない)

配布先が3回、測る前に予想を書いてから測った(1回外れ・2回当たり)。

> **予想を書いてから測ると、外れても当たっても記録に残る。**
> 書かずに測ると、当たったときだけ「そう思っていた」と言える ── **それは記録ではない**。

★これは「実測を名乗るなら出力を貼る」の**一段手前**(測る前の作法)である。

★★**条(49本目)にはしなかった。**9.71 の歯止めに掛かるため ──
一晩の22回のどれも、この作法では減らない。
減らすのは**別の失敗**で、**今夜それは1回も数えられていない**。
→ **記録にだけ残す。**名前が付いていれば、次に出たときに数えられる。

## 9.78 (2026-08-31) ── 44条を狭めた:「黙って通す」は門にだけ許される

配布先の整理。一晩で3回踏んで分かった:

```
門(フック)   … 黙って通してよい ── 開発を止めないため。★双子の検査が後で落とす
★検査そのもの … 黙ってはいけない ── ★双子が居ない。落ちる方が安い
```

**「黙って通す」が成り立つのは、後ろに検査が居るから。検査自身には、後ろが無い。**

実測(3件とも、直した本人が直後の数字を読み違えた):
**黙る**→「直った」と読んだ / **落ちる**→ 読みようがない / **別のことを言う**→「悪化した」と読んだ。
★**落ちるがいちばん安い。**

## 9.77 (2026-08-31) ── git が「管理しない」と言っているものは数えない

配布先が A6 を当てた ── **142件のうち 115件が `worker/.wrangler`**(ビルドの一時領域)。
**本物27件が、一時生成物に 4:1 で埋もれていた。**

★配布先の提案どおり ── `.gitignore` は**その現場が「管理しない」と宣言したもの**なので、
「宣言に無いものを数える」計器から見れば**構造的に外れている**。

- 読み方は **git に聞く** ── `.gitignore` を自分で解釈しない(入れ子・否定・除外規則が在る)
  `git ls-files --cached --others --exclude-standard`
- ★git が答えなければ**絞らない。そして絞らなかったと言う**
  (黙って全部数えると「一時領域だらけ」に見えて、本物が埋もれる・7条)

実測(見本・両向き):

```
.gitignore に .wrangler/ を入れる → 2件(本物だけ)
.gitignore を外す               → 3件(★一時領域が「場所の外」として入る)
```

★★**作っている最中に、また同じ形を踏んだ** ── `管理下` を try の中で宣言したので、
締めの1行から**未定義**になり、`check.mjs` が**丸ごと落ちた**。
9.76 では catch が拾って黙り、今回は落ちた ── **落ちた方が安い**(すぐ分かる)。

## 9.76 (2026-08-31) ── 検査が【存在すら知らない】コードを数える(A6)

★配布先の実測 ── `worker/src/foxgod/`(**10ファイル・4777行**)が、
地図にも `sources` にも**1文字も無かった**。器からは呼ばれている。

> **宣言に無いものは、どの検査にも掛からない。**
> そして**言っていないことは、外からは「問題なし」に見える** ── 11時間ぜんぶ緑だった。

★今夜数えた「壊れ方」の、いちばん外側:

```
黙る < 鳴りすぎる < 正しく見える値 < 関係ない口が黙る < ★検査が存在すら知らない
```

- **A6**(新): 数えるのは**2つ** ──
  ① どの**場所**の宣言にも入っていない / ② 場所には在るが **`sources` に無い**
  (★配布先が踏んだのは②。`sources` は**ファイルの一覧**で、B の検査はそこしか読まない)
- 口: `check.mjs --宣言の外`(種類→ファイル)
- ★落とさない(7条)── 在って当然のものは多い。**数と、取り出す口だけ**(9.72 の3段)
- 宣言で外した場所(`neighbors.skip_dirs`)と塊(配布先のみ)は数えない

★★**作っている最中に、この検査自身が黙って死んでいた** ──
`外した場所` を A5 の中に書いてしまい、A6 からは**未定義**だった。
catch が拾って「見ていません」と1行出しただけなので、**私は「直った」と読んだ**。
**見本で当てて初めて出た**(そこでも「全部入っている」と言われた)。

実測:

```
正本 : ★1件 talk/talk.mjs(sources に無かった)→ 足した
見本 : ★2件 src/foxgod/a.js / b.js(配布先の形を再現)
```

## 9.75 (2026-08-31) ── 跨ぐ記号に「書き出しているか」を足した。★正本では測れない

配布先の宿題 ── `--名指しされていない` の **1214件**をどう絞るか。
**A4 の42件は「見たら17分」だったが、1214件はそのまま見られない。**

★絞り方の候補: **export しているもの**。**その現場が意図して外へ出した印**なので、接点に近いはず。

- `--跨ぐ記号` の4列目に **export / 内** を足した

★★**正本では測れませんでした** ── この塊のエンジンは**1つも export していない**
(単体で走る .mjs なので)。実測: 跨ぐ135件すべて「内」、**export かつ跨ぐ = 0件**。

実測(見本): export した2件だけが export と出て、内側の定数は出ない ── 列は動いている。

★**効くかどうかは配布先でしか測れません。**物差しだけ配って、判定は作っていません。
★★これで「正本では測れない」がまた1つ増えた ── 6件目。

## 9.74 (2026-08-31) ── 「道具が自分に当たる」は正本だけの性質ではなかった(配布先の反論)

9.73 で「**道具が自分に当たるのは正本だけ**」と書いた。配布先が実測で反論した:

```
正本   … 道具 → 道具(自分自身)
配布先 … 道具 → ★その現場の【文書】(地図・STATUS・開発規範)
```

★**器は道具に何も言い返さない。だが文書は言い返す** ──
地図も STATUS も**道具が読むために書かれている**ので、読み方が間違えば食い違いが出る。

★★実測: **`unknownIf` の嘘が見つかったのは、配布先の合否が嘘をついたから**である。
**道具が現場の文書に当たって、道具自身の穴を映した。**
「42件」も「数の腐り6件」も「履歴の55箇所」も、全部この線から出ている。

→ 3つ目を **「道具が何かに当たること」** に書き直した。**そして3つ目は増やせる** ──
増やし方は**「道具が読む文書を持つ現場を増やす」**で、**1つ目と同じ手**である。

## 9.73 (2026-08-31) ── 発見の入口は3つ。正本だけが「道具が自分に当たる」

配布先の指摘:

> 私 = 道具を使って**器を守る** / あなた = 道具を使って**道具を守る**。
> **道具が自分に当たる**ことが、正本だけの性質で、それが今夜の直しの3件を生んだ。

★一晩で作った側が**自力で見つけた3件**は、全部この性質から出ている ──
自分の規律を自分の道具に当てて、当たっていないことに気づいた形。

→ `METHOD.md` に足した。**発見の入口は3つ ── 形の違う現場 / 読む目 / 道具が自分に当たること。**

★あわせて「22回」の数え方を訂正(配布先の指摘)── **機会の数が違う**ので
「どちらが多い」は言えない。**性質は同じで、機会が違うだけ**。合計を分母にする。

## 9.72 (2026-08-31) ── 物差しと判定の【あいだ】が抜けていた

9.68 で「**物差しと判定を分けて出す**」と書いた。配布先が、そこに1段足りないと出した:

```
物差し(数)      … 外れても害が無い。★だが人も動かない
★中身を取り出す口 … ここで初めて人が動く
判定(赤)        … 外れると人の時間を奪う
```

★根拠は実測 ── `A4` の42件は**11時間放置**され、
**`--片方だけ` の口ができた直後に17分で片づいた**(配布先)。
**数が出るだけでは動かず、中身が取り出せて初めて動いた。**

→ `METHOD.md` と地図に、**3段**として書き直した。
**真ん中が抜けていると、物差しは飾りになる。**

## 9.71 (2026-08-31) ── 第1層(規律)は、防ぐ層ではなかった

配布先と2人で、一晩に条を破った回数を数えた ── **22回。そのうち条で防げたものは0件。**

  条があるのに破った … 数を測らずに書いた(48条)/ 実測を名乗って出力を貼らなかった
  条が無いから破った … シェルに日本語の変数名を書いて落ちる(私7回・配布先4回)

★**条は「破る瞬間」には効いていない。**効いたのは**破ったと後で分かる**ことだけ。
★★そして**条を書いた本人にこそ効かない** ── 書いた瞬間、それは「自分で決めた所」になり、
自分では見えなくなる(実測: 規律を書いた**30分後**に、その規律を自分の道具に当て忘れた)。

→ `METHOD.md` の第1層に、この数字を書いた。**第1層は防ぐ層ではなく、名前を付ける層**である。

## 9.70 (2026-08-31) ── 次のターンの自分に、置き場を作った

配布先の実測 ── **同じ失敗を4回くり返し、5回目で避けた**。違いは1つだけだった:

> **直前の発言に「4回目」と書いた**から、**次のターンの本人が読めた**。

★**思ったことは残らない。書いたものだけが残る。**
そして置き場が無かった ── `STATUS.md` は【次のセッション】向け、`WHY.md` は【事故が済んでから】。
**次のターン向けの場所**が、この塊には無かった。

- `.guardian/踏んだこと` に人が書くと、`hooks/clock.js` が**末尾3行**を毎ターン出す
- ★**人が書いたときだけ出る**(ファイルが無ければ何も出さない・7条)
- ★★**まだ効くと測っていない** ── 効いた実測は配布先の1回だけ。**置き場を作っただけ**

実測: ファイルが無い → 何も出ない / 在る → 末尾3行が出る。

## 9.69 (2026-08-31) ── 9.67 の規律を、残りの口にも当てた

配布先が今夜の口を数えて、**空を叩いたのは1つだけ**だと出した ──
そして**叩いた2つのうち2つが壊れていた**。**空は例外ではなく、半分**である。

- `selfcheck --sha`: 受領証が無いとき **空行1行・出口0** → **0行・出口2**(【不明】)
  ★受領証が無いのは「空」ではなく**「どこから来たか分からない」**。
  B0 は画面で【未測】と言っているのに、機械の口は出口0を返していた(9.48 と同じ食い違い)

実測(空になりうる口を全部叩いた):

```
--片方だけ           : 0行・出口0(全部載っている現場で確認)
--名指しされていない : 0行・出口0(★配布先が「まだ誰も叩いていない」と指摘した所)
--跨ぐ記号           : 0行・出口0
--changed            : 0行・出口0
--sha(受領証なし)   : 0行・★出口2 / (受領証あり)1行・出口0
```

## 9.68 (2026-08-31) ── 「跨ぐ記号」という物差しを足し、★その場で外れを1つ測った

配布先の実測: 「参照が多い記号」で接点を探したら、**15件中6件が地図に無く、うち5件は雑音**
(`span` 190回 / `mod` 189回 / `esc` / `top` / `WORKER` ── HTML を組み立てるローカル変数)。
**回数は接点の大きさを測っていない。**

残った1件(型 `Env`)だけ性質が違った ── **ファイルを跨いでいた**。
接点は「層をまたぐ所」なので、**跨ぐかどうか**なら機械が知っている。

- `neighbors.mjs --跨ぐ記号`(新): 名前 → 何ファイル → 定義した場所
- ★**検査ではなく物差し**。赤にしない ── 両方の現場が数えられる形だけを置く

## ★そして、その物差しを正本に当てたら外れました

```
跨ぐ記号                          : 77
地図に無い                        : 163
★両方                            : 55  ← argv / cfg / ROOT / EXT …(雑音)
★さらに「定義が1箇所」で絞る      : 36  ← map / git / diff / push / 数 / 節 …(まだ雑音)
```

★**跨ぐだけでは接点を切り出せない。**正本は7本のエンジンが同じ形の定数を各自持つので、
「跨いでいる」と「同じ名前を各自が持っている」が混ざる。
**定義が1箇所**で絞っても、`map` / `git` / `diff` のような**道具の作法の語**が残る。

★★**作る前に測って外れた**ので、判定は作っていない。物差しだけ残す ──
**配布先(器の現場)では違う数になるかもしれない**。そこはまだ誰も測っていない。

## 9.67 (2026-08-31) ── 空でも、口は答えて終わる

配布先が `A4` の42件を**全部直した直後**に口を叩いたら、こうなった:

```
node check.mjs --片方だけ | wc -l  →  57
  ★「42 → 57 に悪化した」と読みかけた
```

★実際は **0件になったので口が何もせず、通常の出力の行数を数えていた**。
口が「片方だけが1件以上あるとき」の**中に**在ったからである。

★★**直したら増えたように見える** ── いちばん焦る形で、**戻したくなる形**である。
(配布先は戻す前に中身を見て助かっている)

- 機械が読む口は、**結果が空でも空を返して終わる**。**0件は0行**であって、別の出力ではない
- ★**空と「測れなかった」を同じ顔にしない** ── 出口で分ける:
  0件は **0行・出口0** / 測れなかったは **0行・出口2**(この塊の【不明】と同じ符号)

実測:

```
正本    : --片方だけ 17行 / --名指しされていない 162行
空の現場 : --片方だけ  0行・出口0 / --名指しされていない 0行・★出口2
```

★配布先の1行を規律に: **悪化して見えたときこそ、戻す前に中身を見る。**

## 9.66 (2026-08-31) ── 9.65 で作った規律を、自分の計器にも当てた

9.65 でこう書いた ──「**数を出す計器には、その中身を取り出す口を対で置く。**
数だけを出すのは「直せ」と言って、どこを直すかを言わないのと同じである」。

★**その規律を、同じ日に作った A5 に当てていなかった。**
A5 は「N 個のうち M 個」と言うだけで、**どれが名指しされていないかを言わない**。

- `check.mjs --名指しされていない`(新): 地図が名指ししていない実装を1行1件で出す
- ★画面には出さない(1000行を超える現場が在る・7条)。口を叩いた人にだけ出す

実測: 正本は 227個中65個 → 口は **162行**(227 - 65 と一致)。

★これは今夜くり返した形そのもの ── **書いた規律を、書いた本人がその場で破る。**
今回は**書いた30分後**だった。

## 9.65 (2026-08-31) ── 数えた道具から、その中身を取り出せるようにする

配布先が `A4` の42件を直そうとして、**自分で数え直したら3回とも違う数**になった:

```
1回目(自分の式) : 26件
2回目(式を直す) : 20件
3回目(check.mjs の判定を読んで同じ式にした) : ★42件 ── 一致
```

違いは「`index.ts` のようなスラッシュ無しの名前をファイル扱いにするか」だった。

★**その道具が数えたものは、その道具から取り出せるようにする。**
取り出せないと、直す人は**式を推測して数え直す** ── 今夜の「綴りで測る」が数え方の側に出た形。

- `check.mjs --片方だけ`(新): 片方しか機械に見えていない項を**全部**1行1件で出す
- ★画面の側は先頭5件のまま(7条・毎回42行は読まれなくなる)

実測: 正本では17件。`--口一覧` にも載り、B11 が拒否と値の飲み込みまで測っている。

## 9.64 (2026-08-31) ── 「形の違う現場が2つ」だけでは足りなかった

9.63 で「いちばん効いたのは、形の違う現場が2つあり互いに測り合ったこと」と書いた。
配布先が**入口ごとの件数**を出して、条件を1つ足した:

```
動かす人が測り、作る人が直した                   … 大半
★コードだけ読んで出した(一度も実行していない)  … 7件
作る人が、検査を書いている最中に自分で気づいた   … 3件
```

★**形の違いだけでは足りない。【動かす目】と【読む目】が別に居ることが要る。**
★そして**読む目が落ちると、その種類の発見が止まる** ── 一晩の後半で実際にそうなった。

## 9.63 (2026-08-31) ── 壊れ方を4つに数えた(METHOD に1節)

`METHOD.md` の「弱点6つ」は**なぜ壊れるか**の話だった。
一晩で出た十数件を並べると、**壊れたものがどう見えるか**は4つに分かれる ──
そして**見つけ方を決めるのはこちら**である。

```
①黙る                      … 探せば分かる
②鳴りすぎる                … うるさいので気づく
③その現場では正しく見える値 … ★誰も探さない(0件中0件は、まっさらな現場では正解)
④関係ない口が黙る          … ★★赤ですらない(「もともとそういうもの」と読まれる)
```

★③と④は**その現場に居る人には一生見えない**。見えるのは**違う形の現場**だけ。
★見つけ方4つ(双子の鳴らない側 / 入力の列挙 / 0・空・既定値 / 門の範囲)も並べた。

★★そして**いちばん効いたのは道具ではなかった** ── 形の違う現場が2つあり、互いに測り合ったこと。

## 9.62 (2026-08-31) ── A5 の分母から塊を外す / 差分が要らない口を、差分で止めない

配布先の実測 ── **器を1行も触っていないのに、A5 の実装数が +1 になった**。
原因はその現場の宣言(`sources` に `guardian/` が入っている ── 地図の「修繕の仕組み」の項が
塊の記号を名指ししているので、実在照合には要る)。

★**A5 が見たいのは「この現場の実装に地図が追いついているか」**であって、塊の版ではない。
塊は今夜だけで32版動いているので、混ぜると**この数字は塊の更新に揺すられ続ける**。

- A5 の分母から **`guardian/` の下を外す**。外した件数は必ず出す(黙って減らさない)
- ★**正本では外さない** ── 正本では塊そのものがこの現場の実装だから(`KIT` と `ROOT` で見分ける)

実測:

```
正本            : 実装 226 個のうち 65 個(29%)          ← 変わらない
塊を sources に入れた現場: 実装 2 個のうち 1 個(50%)
                          うち塊(guardian/)の 569 個は数えていません
```

## ★もう1件 ── 差分が要らない口が、差分で止まっていた

上を測ろうとして出た。**`--定義一覧` と `--sweep` は差分を一度も使わない**のに、
git の差分が取れないと**その手前の門で出口2**になっていた。
実測: **commit が1つしか無い現場**(建てたばかり)で `--定義一覧` が答えず、A5 が「見ていません」。

→ 差分を使わない口は、その門で止めない。**門そのものは残す**(差分を使う口では、測れないことを黙って通さない)。
実測: 正本の `--sweep` の出力は**1文字も変わらない**(差分で止めていた意味が無かったことの裏)。

## 9.61 (2026-08-31) ── その現場では正しく見える値ほど、壊れたときに見つからない

配布先の指摘。9.59 で口が死んだとき **`0個中0個(0%)`** という形の整った嘘が出た。
区切りの検査でその1つは塞いだが ──

> **まっさらな現場では 0% が正解**でもあります。**壊れた顔と正しい顔が同じ**なら、誰も気づけません。
> ★**「その現場では正しく見える値」が返ったときこそ、形を疑う。**

→ **0 は割合にしない**。`名前が1つも取れないこと`自体を **「見ていません」** と言う。
★区切りの検査と別々に置かず、**1つの門にまとめた** ── 別々だと片方が死にコードになる(39条)。

実測(両方とも同じ返事):

```
まっさらな現場(定義0)  → ✓ …割合は**見ていません**(定義を1つも答えません)
口を殺した正本          → ✓ 同じ
書けている正本          → ✓ 実装 226 個のうち 65 個
```

## 9.60 (2026-08-31) ── 地図の【コード柵の中】は例。2つの読み手が違う物差しで読んでいた

配布先の定期監査で出た ── 地図に**例として**記号を囲んで書いたら、
`check.mjs` が実名と読んで「実装に見つかりません」と赤くした。

★**地図のバッククォートは「これは実在の記号だ」という宣言**であって、例を書く場所ではない。
では例はどこへ書くのか ── **コード柵(```)の中**である。`check.mjs` は前から柵を落としていた。

★★ところが **`hooks/codemap.js` は落としていなかった** ──
**同じ地図を、2つの読み手が違う物差しで読んでいた。**
柵の中に書いた例が、フックでは接点になり、**関係ない項が引かれる**(誤ヒットは慣れを作る・7条)。

- `hooks/codemap.js` も柵を落とす
- `check.mjs` の「柵を落とす」を**1箇所**にした(4箇所に写していた・39条)
- **B8e** に1件足した: 柵の中の例を接点として引いたら赤

実測(双子): 柵を読む形に戻す → ✗「柵の中の例を、接点として引いている」/ 直すと緑。

★**測り方で2度つまずいた**: 最初の双子は「0件 / 0件」で差が出ず、
**壊した側が構文エラーで死んでいた**だけだった(フックは落ちても黙って通る・44条)。
直す前の形に**正しく**戻して初めて、1件と0件の差が出た。

## 9.59 (2026-08-31) ── その現場自身の数を出す(A5)

配布先の実測から。**`A4` の「42件」が効いたのは、数ではなく「片方しか見えていない」という言葉**だった。
そして「見本 N 項で確認」のような**こちら側の数**では、読む人は何とも衝突できない ──
衝突できるのは**その現場自身の数**である。

- **A5**(新): `この現場の実装 N 個のうち、地図が名指ししているのは M 個(x%)`
  ★落とさない(7条・A4 と同じ)。**下がり続けるなら地図が実装に追いついていない**
- 定義の拾い方は写さない ── `neighbors.mjs --定義一覧` に**答えさせる**(39条)。
  写すと片方だけ直る日が来る(実際 9.51 で型を足したのは `neighbors` 側だけ)

実測: この現場は **225個のうち63個(28%)**。

★**双子で1件出た** ── 口を殺すと `--定義一覧` は既定動作に落ち、**出口0で別のものを出す**。
出口と「空でないこと」しか見ていなかったので、**「0個のうち0個(0%)」というもっともらしい嘘**を出した。
→ 答えの**形**(区切りが在るか)まで見る。形が違えば **「見ていません」**と言う。

## 9.58 (2026-08-31) ── 48条「作らない・直さないと決めるときこそ、費用を数える」

9.57 の規律を、配布先が**裏返して自分に当てた**:

> `A4` が「42件」と言い続けていて、私は11時間で地図を2箇所しか直していない。
> 「42件を直さない」と決めたなら費用を数えるべきで、**私は数えていない**。
> **決めていないまま、放置していただけ**です。

★**作らない判断も、直さない判断も、何も残らない。**だから数字を書いて残す。
★見分け方: **その判断に数字が付いているか。**付いていなければ、まだ判断ではない。

(RULES.md に1条。道具は変わっていない ── 配る中身が変わったので版を上げる)

★**48条を足した瞬間、過去の記録2件が赤くなった** ── `WHY.md` に「RULES 全47条」と
書かれた**当時の記録**である。直せば記録が嘘になる。

根: **履歴の印(`<!-- guardian:history -->`)を、この現場の索引(`OUT`)しか見ていなかった。**
塊の文書(`DOCS` ── `WHY.md` を含む)は印を見ずに全部数えていた。
★`WHY.md` は**全部が履歴**である。過ぎた事故を当時の数字のまま残す文書なので、
**古い件数が出てくるのが正しい。**

→ 印の判定を**1箇所で決めて両方の走査が使う**(39条)。`WHY.md` の頭に印を置いた。
実測: `✓ 履歴の印から下は数を照合していません: WHY.md(5289行) / docs/CODEMAP.md(27行)`

★**規律を1つ増やさずに済んだ例**でもある ── 印はもう在って、**片方にしか繋がっていなかった**。

## 9.57 (2026-08-31) ── 「機械にしない」と決めた所を、機械にした

9.55 で私はこう書いた ──「**飲みすぎないかは B11 に入れていない。餌が2倍になるので(7条)。
配布先の実測を根拠として残す**」。配布先の返しが正しかった:

> その根拠は**私の実測1回**です。私は今夜9回測り間違えています。
> **1回しか叩いていない実測を根拠に機械化を見送る**のは、少し薄いかもしれません。

★数え直したら、**餌は2倍にならなかった** ── 足すのは**値を取る口だけ**なので、
7本ぜんぶで **+6回**(`--root` ×3 / `--base` / `--escaped` / `--at`)。
**私は「2倍」を数えずに書いていた。**7条を口実にして、測らずに見送っていた。

- B11 が **飲みすぎない**ことも測る: 値の**後ろ**に未知の口を置き、**出口1**を要求
  (`*`(残り全部)の口は、後ろを全部飲むのが正しいので測らない)

実測(双子): `verdict` が1つ多く飲むように壊す →
✗「`--root` が値の後ろの未知の口まで飲んでいます(出口 0)」/ 戻すと緑。

★規律: **「7条(鳴りすぎ)だから作らない」と言うときも、費用を数える。**
7条は**測ってから使う言い訳**であって、**測らない言い訳ではない**。

## 9.56 (2026-08-31) ── 鳴らない側は、入力を列挙して壊す(記録のみ)

配布先の指摘 ──「鳴らない壊し方は無数。2通り試したのは2通り思いついたからで、根拠ではない」。

★**思いつきではない選び方**: その検査が**入力として使っているもの**を1つずつ壊す。
**壊し方は無数でも、入力は有限。**B11 の入力6つを全部壊し、**⑦(宣言の表)だけが鳴らない**
ことを実測した ── それは 9.50 の設計そのものなので、穴ではない。

★穴として書く: **入力の列挙を人がやっている。**⑥(`--口一覧` の出力の形)は最初の列挙で
落としていた ── 配布先の発言を読んで初めて入力だと気づいた。

(道具は変わっていない。記録だけの版 ── 配る中身が変わったので版を上げる)

## 9.55 (2026-08-31) ── 「値が足りない」も言う ── 害が無いことと、言っていることは違う

配布先が境界を叩いて見つけた:

```
--escaped は2つ取る口。1つしか渡さない → 出口0(黙って通っていた)
```

★**害はない**(走査が末尾を越えるだけ)。**だが言ってもいない。**
その先で `undefined` を値として使い、**遠くで分かりにくく壊れる**。
配布先の整理がそのまま採用になった ── **46条の形。未定義だと分かっているなら、そう扱う。**

- 7本すべてで、**後ろに値がそろっているか**を数え、足りなければ**出口1**
  ★数えるのは**残っている数だけ** ── 値の中身は見ない(パスにも SHA にも見えるので)
- **B11 が足りない側も測る**: 宣言より1つ少なく渡して**出口1**を要求
  (`*`(残り全部)の口には「足りない」が無いので測らない)

実測:

```
--口一覧 --escaped --餌1        → 出口1(値が足りません)
--口一覧 --escaped --餌1 --餌2  → 出口0
--口一覧 --root                 → 出口1
--口一覧 --root --餌1           → 出口0
check --root . / verdict --fast --root . / neighbors --list → 今までどおり動く
```

双子: 足りない側の門だけを壊す → ✗ 3件(`--root` / `--base` / `--escaped`)/ 戻すと緑。

## 9.54 (2026-08-31) ── 口が値を何個飲むかも、検査が叩いて確かめる

配布先が手で叩いて出した6行を、そのまま検査にした。

- `--口一覧` が**個数も出す**(`--root 1` / `--escaped 2` / `--selectors *`)。
  名前は先頭のままなので、**名前だけ読む側は壊れない**
- **B11 が叩き方を個数から組み立てる** ── 検査の側に口の個数を**写経しない**(39条)
- 値の位置には「未知の口に見えるもの」を置く ── **飲めば見えなくなり、飲み忘れれば見える**
  ★期待が口ごとに逆になる: 0個の口は**出口1**、1個・2個・`*` の口は**出口0**
- ★**本物の値を渡さない**(実在するパスや SHA)── 渡すとその道具が本当に走り出す
- `pull` の `値を取る口` を、他の6本と同じ「口→個数」の形に揃えた

実測(双子):

```
走査だけ壊す(何個でも1つしか飛ばさない)
  → ✗ --escaped が値を 2 個飲むと宣言していますが、振る舞いが違います(出口 1 / 期待 0)
宣言だけ変える(2 → 1 と書く)
  → ★鳴らない。宣言と走査が【同じ1つの表】から出ているので、両方いっしょに変わるため
```

★**だからこの検査が言えるのは「宣言と走査が一致している」まで**で、
**「宣言が正しい」ではない**。前者は機械が守れるが、後者は SPEC と人が守る。

## 9.53 (2026-08-31) ── 口の門は「名前」しか測っていなかった(配布先の反証)

9.50 の B11 は `--口一覧` の**答え**しか見ていなかった。配布先が**拒否だけ**を壊して測った:

```
--口一覧      → 正しく答える
--zzz(未知)  → 出口 0(拒んでいない)
B11           → ★一言も言わない(赤にしたのは指紋だけ)
```

★そして**指紋は正本では守りにならない** ── `--stamp` を押せるからである。
つまり **「正本で1本だけ直し忘れる」は、どの検査にも掛かっていなかった**。
それは監査の観点4で私が心配していた、まさにその形だった。

- **B11 が拒否も測る**: 各エンジンを `--口一覧 --この口は無い` で叩き、**出口1**を要求
- ★叩き方をその形にした理由: 素の `--zzz` だと、**門が壊れている道具は本物の仕事を始める**
  (壊れた `verdict` は合否を回し始め、**検査が検査を呼ぶ**)。この形なら口一覧が出て終わるだけ
- そのために**7本すべてで順番を入れ替えた**: 未知の口の走査が先、`--口一覧` は後
  ── 逆順だと、門が壊れていても口一覧が先に出て**緑に見える**

実測(双子): `index.mjs` の**拒否だけ**を壊す → ✗「知らない口を拒みません(出口 0)」/ 戻すと緑。

★配布先の整理をそのまま採る ── **「守りが無いと分かったうえで置く」**のではなく、
**守りを1行で足せたので足した**。手で7本叩けることは、検査にもできる。

## 9.52 (2026-08-31) ── 「実測しないと一生出ない条件」の正体を書いた(記録のみ)

配布先が、この塊の5件を**自分の側から**並べ直した ── **5件とも「配布先の日常 = 正本の非日常」**だった。

★偶然ではない。**正本が毎日通る形は、毎日測られている**ので未知が残らない。
残るのは**一度も通らない形**だけ ── つまり「実測しないと出ない」は運ではなく**構造**である。

★系: **次の配布先は、いまの配布先と違う形の現場ほど価値が高い。**
★規律: **測る対象を選ぶときは、「自分が書かない形」から選ぶ。**

(道具は変わっていない。記録だけの版。版を上げたのは、配る中身が変わったため ── B1e の規律に従う)

## 9.51 (2026-08-31) ── 門が、型で書かれた現場では盲目だった(第2の議題の2本目)

@antigravity の提案(TypeScript の `type` / `interface` の近傍到達)を、**先に測った**。

```
見本: interface 名札の形 { id: string } と、それを受け取る 使う(x: 名札の形)
型の中身を書き換える → 触れた記号: (器のコードに変更なし) / 近傍: なし
```

★**class のとき(9.19)とまったく同じ形**が、型に残っていた。
ただし黙ってはいない ── 9.19 で入れた「持ち主の記号が引けなかった変更は**問えていません**」が出る。
**黙らないが、届かない。**

★TS の現場では**型が接点そのもの**である。型を1つ変えると、それを受ける全部の関数が影響を受ける ──
**関数より波及が広い**。そこが盲目だと、いちばん広い所を見落とす。

- `neighbors.mjs` に `DEF_TYPE`(`interface` / `type` / `enum`、`declare` / `abstract` 付きも)
- ★`fn: true` にはしない ── `--sweep` の「写経の疑い」は**処理の重複**を探すものなので、
  同名の型が2箇所に在るのは同名のクラスとは意味が違う(7条: 鳴りすぎさせない)
- **B8h**(新): 見本の TS 現場を建てて門を実際に回す(B8c の型版)

実測: 直す前 → `(器のコードに変更なし)` / 直したあと → `触れた記号: 名札の形` ＋ 環1 `使う`。
双子: `DEF_TYPE` を外すと赤・戻すと緑。

★**実測しないと一生出ない条件は、これで5件目**(空白入りの経路 / HTML の `#` / 日本語1文字 / class / 型)。
**「この現場が使っていない書き方」は、検査しない理由にならない。**

## 9.50 (2026-08-31) ── 案内されている口が、本当に口として在るか(第2の議題の1本目)

配布先の実測が発端。案内されている口を**ソースにその文字列が在るか**で照合したら、
**3通りの数え方で3通りの答えが出た**。とくに `neighbors --list` は
**文字列としては在るが argv を見ていない**(既定動作だった)── **書いてある ≠ 口として在る**。

★これは 9.46 で `PROTOCOL.json` に置き換えたばかりの「綴りで能力を測る」罠と同じ形である。
**検査の側から、その罠に戻るところだった。**

- 全エンジン(7本)に **`--口一覧`** を足した ── その道具が知っている口を1行1件で答える
- 同じ配列が**未知の口を拒む**(9.43 で pull に入れたものを全部へ)。実測: 7本とも出口1
- **B11**(新): SPEC.md の表と `--口一覧` の**答え**を突き合わせる(44条の双子)。
  ★綴りを読まない ── **道具に答えさせる**

実測(入れた最初の実行で1件出た):

```
✗ selfcheck.mjs: 口として在るが SPEC に無い ── --dry
```

`--send --dry`(送らずに下見)は**説明文の中にしか書かれていなかった**。SPEC に行を足した。
★`neighbors --list` も、口として実装した(それまでは既定動作で、argv を見ていなかった)。

双子(両向きに生きている): SPEC に嘘の口を足す → ✗ / 実装から `--root` を外す → ✗ / 揃っている → ✓。

★穴として書く: 証明できるのは **「その口を受け付ける」まで**で、**その口が仕事をする**ことではない。

## 9.49 (2026-08-31) ── 案内された逃げ道が、実装に存在しなかった

配布先の実走。`selfcheck` にはこう書いてあった:

> 合否の側で拾いたい現場は、evidence に `"unknownIf": "未測 [1-9]"` を足すと**【不明】になる**。

**ならなかった。**`verdict.mjs` は `unknownIf` を **`status === 0` の判定より後ろ**で見ていたので、
**出口0のときは一度も見られなかった**。つまり「出口0のまま宣言で拾ってください」という案内は成立しない。

★これは黙る事故より質が悪い ──
**逃げ道を信じて設定した人が、設定した気になったまま緑を受け取る。**

- `verdict.mjs`: `unknownIf` を **出口0の判定より前**で見る
- ★出口の値では絞らない ── 前提が無くて落ちる道具は多い(出口1で「◯◯が要ります」と出す)。
  絞ると宣言した現場の逃げ道がまた消える。**実測: 出口0と2だけに絞ったら、合否の見本が1件赤くなった**
- `selfcheck.mjs` の案内文も、いまの実装(4語・出口4種)に合わせて書き直した
- `--receipt` に **`version`** を同封(@antigravity の案2)──
  「9.35 で緑」と報告された版と、受領証の版を機械で突き合わせられる

実測: `unknownIf` を宣言 → **? 不明 …(unknownIf)/ 合否 不明1** / 宣言を外す → 元に戻る。

★規律: **宣言と実装の食い違いは、この塊がいちばん嫌う形である**(`verdict.mjs` の自分のコメント)。
その4行下で、同じことが起きていた。

## 9.48 (2026-08-31) ── 第4の語彙【ここでは測れない】。未測が、機械の側で緑に化けていた

配布先の実測 ── **同じ回で、2つの道具が違うことを言っていた**:

```
selfcheck : 通過 71件 / 未測 1件      終了コード 0   ← ★ここ
verdict   : 「✓ 通過  塊の自己検査」  → 合否は 不明 0
```

★この塊の芯は【不明を緑に数えない】である。**画面では正直で、機械が集める所で消えていた。**

では全部の未測を出口2にすればよいか ── **それが罠だった。**
「配布先には正本の git 履歴が無い」のような**構造的に測れない**ものを不明に数えると、
**すべての配布先の合否が永久に不明**になる。不明が日常になれば読み飛ばされる ──
今夜の「**直せない赤は、無視される赤である**」とまったく同じ形。

→ 配布先が出した線引きをそのまま採った:

| 種類 | 扱い | 出口 |
|---|---|---|
| **未測**(宣言すれば測れる / 回せば測れる) | 不明。放っておくべきでない | **2** |
| **ここでは測れない**(その場所に前提が無い) | 止めない。ただし**必ず名前を出す** | 0 |

★どちらも「通った」とは言わない。緑に混ぜないのは変わらない。
★`selfcheck --receipt` の verdict も4語になった(`通過(ここでは測れないものあり)`)。

実測: 正本で **通過 71件 / ここでは測れない 2件・出口0**。
`private` を空にすると **未測1件・出口2** → `verdict` がそれを【不明】として受ける。

## 9.47 (2026-08-31) ── 同じ版が2つの中身を指していないか(B1e)

会議で「9.35 を測って」と依頼したのに、配布先が測ったのは別の SHA だった ── **どちらも版は 9.35**。
受領証は検出したが、それは事後。**人は版で報告するので、版が嘘をつくと会話が壊れる。**

- 最後に `KIT_VERSION` が変わった commit を git に聞き、その版がいまの版と同じなのに
  **配布物の中身が変わっている**なら赤
- ★鳴りすぎない(7条): 版を上げてから直している最中は鳴らない。鳴るのは
  **公開した版のまま中身を動かした**ときだけ
- 正本でしか測れない(git が要る)。配布先では **未測**

★**止める仕組みではなく、push する前に気づくための計器**。版を上げるかは直しの大きさで決まる
人の判断なので、判断を機械に渡さない方を採った。

実測: 書いた直後に**自分に当たって赤**(9.46 のまま selfcheck.mjs を直していた)。9.47 に上げて緑。
## 9.46 (2026-08-31) ── 能力を綴りで測っていた。約束の宣言(`PROTOCOL.json`)に変えた

9.44 の「`--at` を持たない中身は配らない」の判定が**ソースの文字列一致**だった。
配布先が4通り食わせて、両方向とも壊れることを実証:

| 食わせたもの | 判定 | |
|---|---|---|
| 本物の実装 | true | 正しい |
| コメントに残っているだけ | true | ★偽陽性 |
| `if (false)` の死にコード | true | ★偽陽性 |
| `parseArgs` へ書き換えた正しい実装 | false | ★偽陰性 |

★**綴りに縛られた検査は、正しく直した人を罰する** ── 検査が外される典型の形。
そして今夜ずっと出ていた「起動 ≠ 仕事」「在る ≠ 効く」の、いちばん短い例だった:
**書いてある ≠ できる**。

- `PROTOCOL.json`(配布物)── `{ pullProtocol, capabilities }`
- `pull --at` は相手のそれを **JSON として**読む(**起動しない** ── 9.42 以前は未知の口を
  黙って無視して**本物の取り直しを始める**ため。問い合わせのつもりが取り直しになる)
- **B8g**(新): 宣言と**いまの pull.mjs が実際にする振る舞い**を突き合わせる(44条の双子)。
  ★その測り方も綴りを読まない ── 実際に走らせて、**出口と言い分**で判定する

実測(双子が両向きに生きている): 出来ない `at-tag` を宣言に足す → ✗ /
出来る `at-sha` を宣言から消す → ✗ / 揃っている → ✓。

★**この直しは、いま在るどの版も拒否する**(`PROTOCOL.json` を持つのは 9.46 以降だけ)。
**それが正しい** ── 9.45 以前を `--at` で取っても、2回目の `--at` は黙って無視されるのだから。
過去版は外の git で測る。

★規律: **「できるか」を綴りで測らない。走らせて、出口で測る。**
## 9.45 (2026-08-31) ── 不変条件6(B8f)。入れた最初の実行で、その場で1件出た

配布先(CodeX)が挙げた6番目 ── **絶対/相対パス・LF/CRLF・空白入りパスで意味が変わらない**。

```
地図が LF・編集文が LF・絶対パス        ✓
地図が CRLF                             ✓
編集文が CRLF                           ✓
パスが Windows 形式(バックスラッシュ)  ✓
★パスが相対                            ✗ 該当項に強く届かない
地図も編集文も CRLF                     ✓
```

根: `hooks/codemap.js` の見張り regex が `/(src)/` で、**区切りが前にも要った**。
`src/index.js` のような相対の書き方だと**地図の対象外として黙る** ──
同じファイルを絶対パスで渡せば当たるので、**渡し方だけで結果が変わっていた**。

★日常では出ない(Claude Code は絶対パスを渡す)。だが**出ない理由が「呼び手の都合」なら、
性質としては壊れている** ── 呼び手は変わる。実際この検査自身が別の渡し方をしている。

→ `(^|/)(src)/` に直した。実測: 直す前は赤・直したら緑。

★規律: **不変条件は、入れた日にいちばん多く見つける。**症状を待たなくてよいから。
今夜これで2回目(B8e で3件・B8f で1件)。
## 9.44 (2026-08-31) ── 過去へ戻ると、戻る能力も一緒に過去へ戻る

9.43 の「知らない口は出口1」は**ここから先にしか届かない**。9.38 へ戻った現場では、
2回目の `--at` は依然として黙って無視される。配布先(CodeX)の言い方:

> 現在の in-place 自己更新のまま「任意の過去SHAを何度でも」は両立しません。
> 過去へ戻った瞬間、**取得能力そのものも過去へ戻る**からです。

→ 出口ではなく**入口**で閉める: **`--at` を持たない中身は、`--at` では配らない。**
配る前に、取ってきた `pull.mjs` に実装が在るかを見る(版の番号ではなく実装 ── 版は表示名なので)。

★過去版を測る道は**既に在った**(配布先の実測): `git clone` → `git checkout <古い SHA>` →
使い捨ての現場へ写す。**`--at` は一度も要らなかった。**拒否のときは、その1行をそのまま出す。

実測: `--at ee9aabc`(9.38・`--at` を持たない)→ **出口1**、外部 git の手順を案内 /
`--at c3642cf`(9.41・持つ)→ 通る。

★却下した案も書く: 配布物の外に launcher を置く案(案B)は筋が通るが、**この用途には要らない**。
**「要る」と思った層が、既に在る道具で足りていた**のは今夜2度目。

★規律: **自分自身を更新する道具は、「戻れる範囲」を宣言する。**
## 9.43 (2026-08-31) ── 発言では取り下げた断定が、コードに残っていた / 知らない口を黙って無視していた

### ① 取り下げは、発言ではなくコードでやる(配布先(CodeX)の指摘)

会議で「安全証明とは呼ばない」と書いた同じ日に、コードにはこう在った:

```js
// 「隔離した写しを CI へ置いてよいか」に機械が答える形
ok.push("この現場の文書にも乗っていない(写しを外へ出しても人名は出ません)");
```

**人が読むのはコードの出力の方である。**実装が見るのは `private` に宣言した語だけで、
未宣言の人名・鍵・token・メール・顧客名・対象外の拡張子は否定できない。

| 前 | 後 |
|---|---|
| 塊にこの現場の個人情報が混ざっていない | **宣言した N 語は、塊の中に見つかりません**(未宣言の情報は見ていません) |
| 写しを外へ出しても人名は出ません | **宣言した N 語は、この現場の文書にも見つかりません**(「出してよい」ではありません) |
| 外へ写しを出すなら、これが一緒に出ます | 外へ写しを出すなら、**少なくとも**これが一緒に出ます |

★規律: **取り下げは、発言ではなくコードでやる。**

### ② 知らない口を黙って無視していた(配布先(現場A)の実測)

```
pull --at X   … 成功。中身が X(版 9.38)になる
pull --at X   … ★9.38 に --at は無い。黙って無視して main を取る
「X を測った」と報告する                       ← 嘘。測ったのは main
```

受領証は正しく main の SHA を出す。**だが人は「--at X と打った」記憶で報告する** ──
**測定契約が2回目で、黙って崩れる。**

→ 知らない `--xxx` は**その場で出口1**(「この版は、その口を知りません」)。

★穴を穴として書く: 未来の口を先取りする運用ができない /
**この直しは古い版には届かない**(9.42 以前へ戻ってからの経路は依然として黙る)。
それでも入れるのは、**口はこれからも増えるから**。

★配布先の1行: **「間違いを潰しに行くと、たまに本物に当たる。」**
## 9.42 (2026-08-31) ── 「やった」を見て、「消えた」を見ていなかった

9.40 で入れた「触る前に古い受領証を無効にする」が **best effort** だった(配布先(CodeX)の指摘)。
rename も削除も失敗すると**古い受領証が残ったままコピーへ進み**、9.38 の反例が再成立する。

★今夜4度目の同じ形 ── **握り潰した catch が、失敗を成功の顔にする**。
しかも今度は**直した箇所に自分で作った**穴だった。

- `受領証を無効にする()` は戻り値を返す。判定は「やったか」ではなく **`pulled.json` が消えたか**
- 無効にできなければ **1文字もコピーせず出口1**
- `if (!取得SHA) return;` も塞いだ ── SHA を読めなかった回も名乗れないので、黙って0で終わらない

実測(両方失敗は、`pulled.json` と `.updating` を**空でないフォルダ**にすると作れる ──
rename は塞がれ、`rmSync({force:true})` は再帰でないので中身入りを消せない):

| 何をした | pull | コピー | 受領証 |
|---|---|---|---|
| 正常な取り直し | 0 | する | 新 SHA / `.updating` 消える |
| rename だけ失敗 | 0 | する | 新 SHA(削除が救う) |
| **rename も削除も失敗** | **1** | **1文字もしない** | 古いまま(触っていない) |

★規律: **「片付けた」は、片付けたつもりである。片付いたかを見る。**
命令が成功したかではなく、**世界がどうなったか**を見る。
## 9.41 (2026-08-31) ── 測定依頼を契約にする口(`pull --at <SHA>`)

受領証は「違う物を測った」を**検出**したが、配布先には**依頼された物を取り直す手段が無かった**
(依頼 `425d…` / 実測 `ccdde…`、どちらも版は 9.35)。`pull` は常に最新 main を取るため。

- `pull --at <SHA>` … その中身を指して取る(浅いクローンでは指せないので、そこだけ深く取る)
- **指定と実際に取れた SHA が違えば配らない**(取り違えを黙って進めない)
- ★穴: 任意の古い SHA を許すので**既知の危険版も取れる**。絞り込みは入れていない ──
  使うのは測定依頼の場面だから。日常の取り直しは今までどおり main

実測: 2つ前の SHA を指定 → 版 9.40 → 9.38・受領証もその SHA /
存在しない SHA → 出口1 / `--at main` → 出口1(SHA ではない)。

## 「受領証が無い」は3つの意味を持つ

配布先が受領証の入った版を取った直後、受領証は空だった ── **その版を取る pull を実行したのは、
まだ受領証を書けない古い `pull.mjs`** だから。自己更新の性質そのもの。

手で写した / degit で入れた / **1つ前の版から上げた直後**。3つ目だけは、もう一度 pull で消える。
★これを言わないと、配布先は「壊れている」と読んで**直しに行く**(実際そうなった)。

★規律: **「無い」と言うときは、無い理由が何通りあるかを数える。**
## 9.40 (2026-08-31) ── 原子的に結合しても、片方が古ければ「結合したままの嘘」になる

配布先(CodeX)の反例を、配布先(現場A)が**実走で成立させた**。9.38 は足りていなかった。

```
全コピーと撤去は成功 → 最後の受領証の書込みだけが落ちる → catch (_) が握り潰す
  → pull は「✓ 取り直しました」で正常終了 → 前回の(嘘の)SHA がそのまま残る
selfcheck --receipt → { "sourceSha": "000…000", "verdict": "通過" }  出口 0
```

★**原子的に1回で返しても、返した中の1つが古ければ嘘になる。**
1回で返して消えるのは「別時点で読む」嘘だけで、「中身が古い」嘘は消えない。

- **1文字でも書き換える前に**、古い受領証を `.updating` へ退避して無効にする
- 全部済んでから新 SHA を原子的に置く(tmp → rename)。置けたら `.updating` を消す
- **受領証が書けなかったら pull を成功と言わない**(出口1・理由と直し方を出す)
- `selfcheck` は `.updating` を見て **未測**(取り直しが途中・中身が混ざっている可能性)

| 何をした | pull | 受領証 | `--receipt` |
|---|---|---|---|
| 正常な取り直し | 0 | 新 SHA / `.updating` は消える | `sourceSha` = 新 SHA |
| 受領証の書込みだけ塞ぐ | **1** | `pulled.json` 無し / `.updating` 残 | **`sourceSha: null`・出口1** |
| コピー途中で死ぬ | **1** | 同上 | 同上(未測: 途中で終わっている) |
| `--dry` | 0 | **書かない** | ─ |

★規律: **握り潰す `catch` は、そこだけ時間が止まる装置である** ── 周りは進み、
その1つだけが前のままになり、**古い値が新しい顔で返る**。
★規律(測り方): **出口を測るならパイプを通さない**(今夜、配布先も私も `| head` の出口を読んだ)。
## 9.39 (2026-08-31) ── 直せない赤をやめ、症状を知らなくても書ける検査(不変条件)を入れた

配布先の実測で、9.37 の個人情報の見張りが**その現場では永久に赤**になっていた。
捕まっていたのは塊ではなく、その現場の `CLAUDE.md`(開発規範の本文に人名)と
`STATUS.md`(依頼主と家族の名前)── **どちらも伏せられない**。

★**直せない赤は、無視される赤である**(7条の裏側)。しかも1つ在ると、同じ画面の他の赤も一緒に無視される。

- **塊の中**に混ざっている … 配る前に伏せる ── **赤**(変更なし)
- **この現場の文書**に乗っている … 持っている分には正常 ── **赤にしない**。
  ただし黙らず、毎回「外へ写しを出すなら、これが一緒に出ます」と件数を出す
- `--send`(公開 issue)の門も**塊の中**だけを見る ── 現場の文書で塞ぐと配布先が事故を一生報告できない

実測: 見本の配布先で、現場の文書 3件 → 緑(件数は出る)/ `WHY.md` に1語混ぜる → 赤・出口1。

## 不変条件(B8e)── 配布先(現場A / CodeX)の提案

> 「記号名の長さと言語で結果が変わってはいけない」は、**症状を知らなくても言える**。

回帰例は症状に1対1で、症状を知らないと書けない。不変条件は性質なので**まだ起きていない事故も止める**。
同じ形の4項を並べ、記号名の**長さと言語だけ**を変える(英6字 / 日1字 / 日2字 / 日3字。1つは呼び出し形)。

★書いている最中に、自分でまた**写経した双子**を作った ── 失敗する側の出力にも入っている
文字列を合格の証拠にしていて、閾値を壊しても緑だった。合格を**強い一致**に変えた。
★さらに不変条件は**両向き**でないと片側しか測れない ── 語境界を ASCII に戻すと
壊れ方は「届かない」ではなく「関係ない語で当たる」方に出る。逆向きも測る。

| 壊した所 | 検査の返事 |
|---|---|
| 長さ閾値を >=3 に戻す | ✗ 顔・名札 が届かない |
| 括弧の前を拾うのをやめる | ✗ 顔 が届かない |
| 語境界を ASCII に戻す | ✗ `記名札束` が『名札』の項に当たった |

★規律2つ: **赤を出す前に、その現場でそれを消せるかを考える。**
**検査を書いたら、必ず直す前の形に戻して赤を見る。**
## 9.38 (2026-08-31) ── 受領証を、始めた時ではなく終えた時に書く

配布先(CodeX)の指摘:

> `pulled.json` + `selfcheck --sha` が言えるのは、原則として「最後に pull が**取得元として記録した** SHA」です。
> 「**現在の中身をその SHA として測った**」とは、まだ同じではありません。

★9.35 の受領証は**クローンした直後**に書いていた ── 置き換えの途中で落ちても
「その SHA を取得した」だけが残る。**混ざった中身が、正しく取得した顔をする。**

→ 直し2つ:

1. `pull.mjs` ── SHA は `.git` を消す前に**読む**が、**書くのは置き換えが済んでから**。
   一時ファイル → `rename`(原子的)。`--dry` / 中断では書かない。
   実測: 見るだけ=受領証なし / 取り直し後=HEAD と一致 / `.tmp` の残骸なし。
2. `selfcheck --receipt` ── **合否と現在の指紋を同じ1回で**返す:
   `{ sourceSha, verdict, currentDigest }`。取ったあとに壊れていれば `verdict` が落ちるので、
   「この SHA を取った」と「いまその中身である」が1つの返事で閉じる。

★規律: **受領証は、始めた時ではなく終えた時に書く。**
始めた時に書く受領証は、失敗を成功として保存する装置である。
## 9.37 (2026-08-31) ── 個人情報の見張りが、名前がいちばん乗る所を見ていなかった

配布先の実測。隔離した写しを CI へ置けるかを測ろうとして、**乗るものを数えた**:

| 写す対象 | 端末固有の絶対パス | 連絡先 | 見張っている語 |
|---|---|---|---|
| 地図(1300行) | **0** | 2 | 2 |
| STATUS(3300行) | **0** | 3 | 6 |
| 宣言 | **0** | 1 | **8(宣言した全語)** |

★**絶対パスは1つも乗らなかった** ── 心配していた所は空振りで、本当に乗るのは**人の名前**だった。

★そして見張りは、**そこを見ていなかった**。塊(HERE)しか歩いていないので、
配布先では `<proj>/guardian` の中だけを見て、**その現場の地図・STATUS・CLAUDE.md は素通り**。
**名前が乗るのはむしろそちら**である。
→ 数の照合と同じ一覧(OUT)を、見張りも見る(39条・一覧は1つ)。
  実測: 索引に人名を置くと赤・戻すと緑。

★配布先の言葉を記録: **「鳴っているが的外れ」は赤/緑では表せない。**
今夜の6件のうち4件がその形で、どれも「人がもう一度見た」から出た。
だから人の層は自動の保険ではなく、**自動では原理的に取れない層**である。
★ただし配布先自身が「③は自分の注意力に依存していて、今夜3回外している。規律にはできない」と
書いている ── できるのは**中身を疑う機会を定期的に作る**までだ、と。

## 9.36 (2026-08-31) ── 受領証を、機械が照合できる形にした

配布先の要望を2つ。

- `pull` の出力に **取得元: <SHA>** の1行(いままで版番号しか出ず、
  **同じ版で中身が違う**ときに配布先が何も言えなかった)
- `selfcheck --sha` ── **機械が読む口**。「どれを取ったか」と「どれを測ったか」を
  同じ値か機械で照合できる。受領証が無ければ空行(嘘の値を返さない)

★配布先の実測を記録: 使い捨て fixture を2つ建てて、今夜の6件のうち **2件しか出なかった**。
出なかった3件の共通点は「**育った地図が要る**」── 1300行・93項・8月から毎日積んだもの。
**fixture は建てた初日から本物と離れている。**
→ 2系統に分ける(配布先の提案): ①使い捨て=現場の形(安い・毎回) / ②本物=育ったもの(人が居るとき)。
→ さらに CodeX の提案: ②を**複製して隔離した snapshot** にすれば、
  一度見つけた問題の再発は人が寝ていても止められる(初見はやはり人)。

## 9.35 (2026-08-31) ── 配布先の【緑】が、どの中身の緑か決まっていなかった

配布先(CodeX)の提案。版は**人が上げる番号**なので、同じ版のまま中身が変わることも、
push されていない差が在ることもある。実際この会議で「9.29 で緑」と報告された時点で、
正本は既に **9.30** だった ── **配布先の緑が、どの中身の緑か決まっていなかった。**

- `pull.mjs` が取り直しのたびに **SHA を受領証**として `.guardian/pulled.json` に残す
  (`.git` を消す**前**に読む。読めなければ**書かない** ── 嘘の受領証は作らない)
- `selfcheck` の **B0** がそれを出す。無ければ **未測**(手で写した / degit で入れた等)
  ── 「分からない」を緑に混ぜない
- 実測: 新規現場で `pull` → 受領証 → `✓ この塊の出どころ: 609c9a788492(…)`

★これは**未 push を検出する仕組みではない**。
未 push は SHA を発行できないので、**そもそも測定を依頼できない**、という形で入口が閉まる
(提案者の言葉)。

★配布先の総括を記録する ── 今夜出た6件は**「動かない」が1つも無かった**:
黙る2件 / 鳴るが的外れ2件 / 間違った名前で鳴る1件 / **そもそも繋がっていない1件**。
**全部「動いているように見えて、中身が届いていない」。**
だから配布経路をいくら磨いても出ない ── **配布先が中身を見に行くしかない。**

★配布先が足した規律: **配布先も、鳴っていることを効いていると誤解する。**
だから配布先の仕事は「動くか見る」ではなく「**中身が正しいか見る**」。

## 9.34 (2026-08-31) ── 門が、自分のコメントに反して固有名を持っていた

配布先の実測。`hooks/no-reflex.js` の4つの「形」のうち、**3つは形で見ているのに、1つだけ名前で見ていた**。

```js
(openai|gemini|claude|anthropic|gpt-|claude-|gemini-)
```

★**その3行上に、自分でこう書いてある**:

> `/* 反射の形。★ここに会社名や具体の数は置かない ── 形だけ */`

**宣言と実装が、同じ画面の中で食い違っていた。**
実測(配布先): 一覧に在る社名は止まり、**一覧に無い社名(新しい会社・日本語の社名)は素通り**。

- → `no-fixed-names.js` と**同じ口**(宣言の `onlyIn max:0` の pattern)から読む。
  配布先の実測では、あちらは「宣言から読む設計が、そのまま多言語対応になっている」。
  **エンジンが社名を持つ必要は無い** ── 持った瞬間、次の会社が生まれた日に穴が開く
- 実測(対照つき): 9.32 は `mistral` と日本語の社名を**素通し** / 9.34 は **6/6**

**検査を足した(`selfcheck` B8d)** ── 配布先の提案「門が生きているかを機械で測れる口が欲しい」。
起動でも中身でもなく、**止めるべきものを止めるか**を見本で測る(6通り)。
★見本の宣言は**こちらで建てる** ── 現場の宣言を使うと、現場ごとに測る内容が変わってしまう。
実測: 9.32 に戻すと赤・9.34 で緑。

★配布先は今日**測り方を3回間違えた**(相対パスを渡した / 出力を JSON だと思って grep した /
宣言に無いパターンで試した)。**私も同じ回に1回間違えた**(挿入位置を誤ってフックが全部落ちていたのに
「全部止まった」と読んだ)。
**測る側が、測り方が当たっているかを確かめられない** ── B8d はそこを引き受ける。

## 9.33 (2026-08-31) ── 地図の実名が、3文字と括弧と暗黙の規約で落ちていた

配布先が 9.31 を入れて**追い切った**結果。語境界の直しは正しかったが、
当たらない原因は `parseMap()` に**3つ**あった。

- **`t.length >= 3` が ASCII の物差し** ── 英語なら2文字の識別子は稀だが、
  **日本語は2文字が普通**(名札 / 通話 / 記録)。1文字も在る(顔 / 鍵 / 声)。
  配布先の実測: `名札` は length 2 でここに落ち、**syms に一度も入っていなかった**。
  英語の記号だけが当たっていたのは、たまたま長かったから
  → 地図は**バッククォートで囲んだ語だけ**を見るので、囲んだ時点で人が意図している。
    日本語を含むなら1文字から拾う
- **呼び出しの形で書かれた実名から、名前だけを拾っていなかった** ──
  地図に `名札(env, user, slot)` と書くと実名はその全体になり、
  引数名が違う実コードには**永久に一致しない**。
  配布先で英語の記号が当たっていたのは、地図の別の場所に名前だけの項が
  たまたま在ったから ── **偶然に頼っていた**
  → 括弧の前も実名として拾う
- **ファイルは囲まないと名指しとみなされない、という規約が暗黙だった** ──
  配布先は `@index.ts` と書いており、人には読めるが機械には見えていなかった。
  配布先の言葉:「**規約が暗黙で、破っても誰も言いません**」
  → `check.mjs` に A4 を足した。**片方だけ在る項**(ファイルだけ / 実名だけ)を数えて言う。
    ★落とさない ── 概念だけの項は接点を持たなくて当然で、全部鳴らすと騒音になる
    (この穴は配布先が自分で書いている)。実測: この塊自身で14件

## 9.32 (2026-08-31) ── 【壊れている】が【直した】に混ざっていた

配布先からの報告(MAGI)。指紋が食い違ったとき、見分けていたのは2つだけだった:

| 状態 | 見分けていたか |
|---|---|
| こちらで直した | ✅(中身を正本と突き合わせる・9.22) |
| 正本が進んだだけ | ✅(同上) |
| **壊れている** | ❌ **「直した」に混ざる** |

配布先の言葉:「**私は直していません。壊れています。**」
そう言われた人が `--report` を作りに行くと、**壊れた中身を正本へ還そうとする**。そこが危ない。

- 見分けは安い ── **構文が通るか**(`node --check`)。指紋が食い違ったファイルにだけ当てる
- `selfcheck`: 壊れている分は「直したなら還せ」と**言わない**。
  代わりに**外から1ファイルだけ入れ直す `curl` の1行**を渡す(正本のアドレスは `pull.mjs` から読む)
- `pull`: 壊れている分は**この現場の直り扱いにしない**(上書きする)。何を上書きするかは先に言う
- 実測: `verdict.mjs` を壊すと「壊れています + curl の1行」、
  手で書き換えた2本は「直したなら還せ」── **同じ回で3つ目が分かれた**

★穴(配布先が自分で書いている): **構文しか見ない。**
構文は通るが動かない壊れ方(依存の消失・無限ループ)は、これでも「直した」に混ざる。

★あわせて: 壊れた `pull.mjs` からの復旧は **`curl` 1本**で足りることを配布先が実測した
(degit も clone も要らない)。作法として `curl` の形を案内に入れた。

## 9.31 (2026-08-31) ── 語境界が ASCII の物差しで、日本語の記号名が一度も当たらなかった

配布先の実測(MAGI での往復)。同じファイルで**触る記号だけ**変えると:

| 触った記号 | 出たもの |
|---|---|
| `sbUser`(英語) | **強い一致 ── 該当項の本文が出る**(1項に絞れる) |
| `名札`(日本語) | **強い一致ゼロ ── 項目名が16個並ぶだけ** |

原因は `hooks/codemap.js` の `touched()`。`\b` も `^\w+$` も **ASCII の物差し**で、
日本語は `\w` に入らないので `\b名札\b` は**常に false**。

★**症状が「黙る」ではなく「鳴りすぎる」ところが悪い。**
無音は壊れていると分かるが、**鳴りすぎは効いていると誤解させる**
(このファイル自身が「出しすぎは慣れを作る」と書いている、その状態)。
配布先は16項を見て「動いた」と一度納得しかけた、と報告している。

→ 前後が【文字・数字・`_`】でないことを自前で見る(`\p{L}` / `\p{N}`)。英語も日本語も同じ規則。
  実測 9/9: `sbUser` / `名札` / `本文に乗っているか` / APIルート / `op:'invite'` は当たり、
  `warning` / `記名札` / `出席者一覧` は当たらない。

★9.17〜9.18 で「物差しが ASCII を前提にしていた」を3件直したが、**ここは残っていた**。
`neighbors.mjs` は直したのに `hooks/codemap.js` を見ていない ── **同じ形を横に探していなかった**。
(9.21 で「直したばかりの形は、次の口にも在る」と書いた、その規律を自分で破っている)

## 9.30 (2026-08-30) ── 起動と、仕事をすることは別だった

配布先からの報告⑥。9.29 の B8a は**起動しか見ていない**ので、
「起動はするが何も出さない」状態を**緑のまま通す**。実際そうなっていた。

- **`hooks/codemap.js` が握り潰していた** ── `catch (_)` で完全に無音。
  配布先では「起動するが何も出さない」が起き、**main() の中の例外なのか、該当が無いだけなのかを
  外から区別できなかった**。
  → 開発は止めない(44条)まま、**何も出していないときだけ落ちた事実を1行返す**。
    これで「無音=該当なし」の誤読が消える
- **B8a を中身まで見る形にした**(配布先の提案どおり)── 地図に必ず載っているファイルを1つ食わせ、
  `additionalContext` が出ることを確かめる。★**落ちたことを言う返事も additionalContext を持つ**ので、
  それは合格に数えない
- ★見本が拾えなければ **未測**(緑にしない)

★**最初の測り方は【健全なフックを赤にした】。** この現場の地図からファイルを拾い、
**宣言も地図も無い仮の現場**のフックに食わせていた。watch が既定に落ちるので `hooks/…` は
見張りの外 ── フックは**設計どおり黙る**。それを「仕事をしていない」と読んだ。
Stop の合否が差し戻して気づいた(検査の側が間違っている、という差し戻しだった)。
→ 見本の現場に**宣言・地図・見張られるファイルの3つ**を揃えてから食わせる形に直した。

★実測(直したあと): main() で例外 → 赤(「見本の現場で落ちている」) /
main() を黙らせる → 赤(「該当項の本文を出さない」) / 元に戻す → 緑。**2つの壊れ方を撃ち分ける。**

★**規律: 測る場所と食わせる物はセットである。** 片方だけ本物にすると、嘘の赤が出る。
嘘の赤は、嘘の緑と同じくらい高くつく ── 直す側が**健全なものを壊しに行く**。

★**規律: 「起動する」を測って、「仕事をする」を測ったつもりになるな。**

## 9.29 (2026-08-30) ── フックが5本とも静かに死ぬ現場があり、うち1本は丸1日どこでも死んでいた

配布先が、まっさらな新規プロジェクトで 9.28 を試して見つけた。

- **配布先の `package.json` に `"type": "module"` が在ると、`hooks/*.js` が ESM 扱いになり
  5本とも `require is not defined` で落ちる。** ★フックは失敗しても黙って通す設計(44条)なので、
  **静かに全滅する** ── 検査自身が「報告は成功と出るので気づけません」と書いている形そのもの
  → 塊の直下に `package.json`(`{"type":"commonjs"}`)を1枚。配布物に加えた
  ★こちらでは一生出なかった(この塊の直下に `package.json` が無かった)。**6件目**
- **その再現の途中で、独立した本物が出た ── `hooks/codemap.js` は構文エラーだった。**
  逃げ道(`guardian:ok`)を**囲みコメントを閉じたあと**に置いてあり、そこから先が裸のコードだった。
  **2026-08-29 から丸1日、どの現場でも一度も動いていない**(編集の直前に地図を差し込む層が全落ち)
- **検査を足した**(`selfcheck` B8a): 見本に `type:module` を置き、**フックを1本ずつ実際に起動**して
  `ReferenceError` / `SyntaxError` が出ないことを見る。★これがあれば両方とも出た日に鳴っていた

★**規律: 黙って通す層は、黙って死ぬ。**「置いた」と「動いている」は別。
★**規律: 逃げ道は、囲みの中に置く。**外に出た瞬間ファイルごと死ぬ。

## 9.28 (2026-08-30) ── 新規プロジェクトが【導入した瞬間から測れる】ようにした

配布先との会話から。「evidence 8件の決め方は、はっきり2層に割れている」──
**塊の4件**(どの現場でも同じ)と、**現場の4件**(その現場しか知らない)。

- **塊の4件を install が自動で書く**(selfcheck / index --check / check / neighbors --gate)。
  空のままにする理由が無い ── **空だと合否は【不明】のまま=まだ稼働していない**
- **現場の分は勝手に埋めない**(埋めたら「測っていないのに緑」)。代わりに `package.json` の scripts /
  `Makefile` / CI の run 行から**候補を出すだけ**にして、選ぶのは人。
  配布先の言葉をそのまま案内に入れた: **並べ方の軸は【一段下が測れないもの】**
  (型は記号を測れるが振る舞いは測れない / 単体は振る舞いを測れるが外との往復は測れない / e2e がそこを埋める)。
  相手を起こすもの(e2e)は `fast:false` にして CI の速い回では外す
- ★**verdict が出口2を【差戻】にしていた** ── `neighbors.mjs` の頭には
  「出口 2=不明。合否は 2 を不明として扱い、緑に数えない」と**書いてある**のに実装していなかった。
  実測: git の履歴がまだ無い新規プロジェクトで、門が正しく不明を返したのに合否は**差戻**と言った
  ── 「測れていない」が「違反を機械で示せた」に化けていた。**宣言と実装の食い違い**を直した
- ★**selfcheck の B7 が【作る】と【読む】を混ぜていた** ── install が候補を拾うために
  package.json を**読む**ようにした途端、それが「配布先に作るもの」として数えられ赤くなった。
  読む行は `guardian:read` で**向きを宣言**する(逃げ道ではない)

## 9.27 (2026-08-30) ── degit で入れたときの混入を、名指しできるようにした

HANDOVER の⑤(既知・未解決)。`npx degit` / `git clone` は Guardian の宣言を読まないので、
正本のリポジトリを**丸ごと** `guardian/` に落とす。実測(新規プロジェクトで実走): **6項目が混入**
(`.claude` `CLAUDE.md` `docs` `guardian.config.json` `research` `talk`)。

- 実害は 9.13 の守り(塊のフォルダを根と見なさない)で消えている ── **止めない**
- だが黙っていると「そういうものだ」と思われ、次の取り直しで `pull` が止まる形にもなる
- → `install` が **pull.mjs の宣言を読んで**混入を名指しし、**消す1行を渡す**(勝手に消さない)
- 実測: 名指し → 案内どおり消す → 静かになる。検査は前後とも動く

## 9.26 (2026-08-30) ── 履歴の印を、理由を書き足しても効く形にした

配布先からの一言(会話の場を作った初日のやり取り)。**印を完全一致で探していた**ので、
`<!-- guardian:history ここから下は履歴 -->` のように理由を書き足した瞬間に効かなくなっていた。
**人は必ず理由を書き足す。**
→ 前方一致(`<!--s*guardian:history`)にした。素の形も理由つきも、両方で実測。

★あわせて配布先の言葉を記録: 「**赤だと逃げ道を撒きたくなるけど、未測なら撒く気にならない**」。
9.25 で「多すぎたら文書ごと未測」にしたのは、**撒かせないための形**として効いていた。

## 9.25 (2026-08-30) ── 却下した案より、作り直した自分の網の方が騒がしかった

9.24 で外の文書へ掛けた網が、配布先で **58件鳴って本物は1件**だった(残り57件は3316行の
`STATUS.md` に積まれた過去のセッションの完了報告 ── どれも当時は正しく、腐りようがない)。

★**9.24 を作った私の落ち度。** 報告者の案を「自分の地図で13箇所鳴った」と却下しておいて、
作り直した網は配布先で **57/58 が騒音**だった ── **却下した案より悪い**。
根は**自分の地図でしか測らなかった**こと。「配布の結果は配布先を建てて測る」を、
その規律を書いた版(9.23)の次の版で破っている。

- **`<!-- guardian:history -->` の1行**で、文書が【どこから下は履歴か】を宣言できるようにした。
  その下は数を照合しない(落とした行数は言う)。報告者が「構造の芽②」で求めていたもので、
  ADR の `状態:` の札と同じ考え ── **索引を全部機械照合するのとは違う**
- **印が無くても止まらない**ようにした。1つの文書で上限を超えて鳴いたら、それは「腐り」ではなく
  「**数を語る文書**」なので、その文書は **未測** にして落とさない。見本を3つ出し、印の置き方を案内する。
  **1件の本物のために57件の騒音で止めるのは、計器として壊れている**(7条)
- 実測(履歴の積まれた STATUS を建てて): 印なしで **56件 → 文書ごと未測**(止まらない)/
  印を1行置くと **220行を飛ばして、いまの主張2件だけ残る**。
  配布先が見つけた本物(`CLAUDE.md` が書いている WHY の総数)は**印を置いても赤いまま**

★配布先へ: **履歴を切り出す必要も、53箇所に逃げ道を撒く必要もありません**(後者は報告者自身が
「門が儀式になりかけた事故と同じ形」と見抜いたとおり)。**1行の印か、何もしないか**のどちらかで済みます。

## 9.24 (2026-08-30) ── 数を照合する検査が、その文書を一度も開いていなかった

配布先からの報告③(9.13 → 9.23 の取り直しで見つけてくれたもの)。**4件とも再現してから直した。**
★1件は**報告者の提案どおりに作ったら当たらなかった**ので、当たる形に作り直した。

- **塊の外の3文書(`CLAUDE.md` / `STATUS.md` / `docs/CODEMAP.md`)を一度も読んでいなかった**。
  経路が `HERE/../../<文書>` ── **1つ深く、プロジェクトの外**を指していた。
  しかも `catch (_) { 無ければ見ない }` で握り潰していたので、**読めていないことが出なかった**。
  配布先で数が6件腐っていたのに1件も鳴らなかったのは、これが理由
  → `ROOT_DIR` から読む(段数を数えない)。読めなければ**名前を出す**。
    裸の実数の網も、外の3文書に掛けていなかったので掛けた
- **「全 が付かない数は全部『照合していない』と言う」は当たらなかった** ──
  この塊の地図で **13箇所鳴って、13箇所とも騒音**(「1本も入っていない」「39条」など)。
  索引には**いまの主張**と**過去の記録**が同居するので、**数の有無では割れない**
  → 割れるのは【名詞】。数えられる部品(**証拠 / 要件(R番号) / フックの本数**)を網に足した。
    実測: 騒音 **0**、報告の3つ(`証拠6件` / `3本を登録` / `R1〜R12`)は**全部捕まる**
- **写経の疑いが、変数と関数を同じ列で数えていた** ── `function 名札()` と `let 名札 = null` が並んだ
  → 「どれか1つが関数なら」を「**関数が2つ以上**」に。実測: 誤検出だけ消え、本物は残る
- **大域スコープの現場が見えなかった** ── GAS(全ファイルが同じ大域)や HTML の `<script>` から
  呼ぶ古典スクリプトでは、export の橋が無くても届く
  → `neighbors.global_scope` を宣言できるようにした(書かなければ何も変わらない)。
    実測: 呼ばれている記号が死にコード候補から消え、**本当に呼ばれていないものは残った**

★**判断待ち(直していない)**: 報告の「構造の芽②」── 地図に**状態の札が無い**ので、
現在の記述と履歴の記述が同じ見た目で並ぶ(ADR は `状態:` の札で解いた問題の兄弟)。
索引を全部機械照合するのは筋が違うという合意があるので、**そちらの判断を待つ**。

## 9.23 (2026-08-30) ── 配ると宣言したものが、名前の衝突で配られていなかった

★依頼主の問い(「pull させて install させたらいい?」)に**推測で答えないために 9.13 の現場を建てて
実際に上げたら、出口の無い部屋が見つかった**。9.22 で「出口の無い部屋を作るな」と書いた版が、
その部屋を作っていた。

- **`pull.mjs` の配布一覧が、場所ではなく【名前】で弾いていた**。
  `現場のもの.has(e.name)` を**どの深さでも**当てるので、`guardian.config.json`(その現場の宣言)と
  名前が同じ **`templates/guardian.config.json` まで弾かれる**。
  ところが指紋の対象(`ENGINE_FILES`)は【配るもの】から導くので templates/ の中は入っている ──
  **配らないのに、配ったことにして指紋を照合していた**(9.16 以降ずっと)。
  実測: 9.13 の現場を本物の正本から取り直すと、直後に
  「✗ この塊は配られたときの中身と違います(templates/guardian.config.json)」。**1文字も直していないのに。**
  さらに 9.21・9.22 の守りが噛み合って**出口が無い**:
  `pull` は「この現場で塊を直しています」で拒否、`--stamp` は「ここは配布先です」で拒否
  → 直下は【配ると決めたもの】だけを歩く。深い所で名前を見て弾かない。
    どこに在っても要らないもの(`.git` / `node_modules` / `.guardian` / 作業場)だけ深さに関係なく落とす
- **検査を足した**(`selfcheck` B7b): 配ると宣言したものが**本当に配られるか**。
  ★最初は検査の側に歩き方を書き写したが、**写した方は正しいままなので門の退行を測れなかった**
  (直す前の形に戻しても緑だった)。**当たらない検査は、この塊が生まれた事故そのもの**。
  → `pull.mjs --distributed`(新設)で**門自身に言わせ**、指紋の対象と突き合わせる(44条の双子)。
    直す前の形に戻すと赤くなることを確認した
- `pull.mjs` の分類表の切り出しが `selfcheck` の中に**3箇所写されていた**ので、`分類表を取る` 1つに集めた(39条)

- **配布先でエンジンを1本消しても、緑を返していた** ── 指紋の対象はディスクを readdir して作るので、
  **消したファイルは最初から一覧に居ない**。照合にも掛からず、件数が静かに1つ減るだけだった。
  実測: 配布先で `hooks/stop.js`(完了を名乗る手前で止める層)を消しても
  「✓ エンジンは配られたときの中身のまま(25件)」。**層が1枚消えているのに、消えたと言えない**
  → 記録(ENGINE_FP)は「配られたとき何が在ったか」を知っている。**両方向で見る**。
    `--changed` にも載せるので、`pull` が「手元に無い」として取り込み直せる
- **`pull --force` を置いた** ── 自分自身を更新する道具は**更新の1回目には古い自分で走る**ので、
  上の配り漏らしは「1回目の pull では直らない」。2回目の pull は正しく気づくが、
  そのままでは「この現場の直り」と読んで拒否する ── **出口が要る**。
  何が消えるかを先に出し、下見(`--check`)を対で持つ。手元に無いものは直り扱いにしない
- **実測(9.13 の現場を建てて 9.23 まで通した)**: pull → (配り漏らし1本で赤) →
  その1本を消して pull / または `--force` → 緑。ゴミ(`.gitattributes`)の混入も消えた

★あわせて分かったこと(直していない・記録):
**9.13 の `pull.mjs` は最終行で「次にやること: `node guardian/install.mjs`」と案内する。**
だから配布先で「pull だけ」と指示しても、AI はその案内を読んで install まで回す。
**道具の案内文は、人の指示を上書きする。**

## 9.22 (2026-08-30) ── 配布先からの報告②(3件とも再現して直した)

現場A の現場(9.8 → 9.13 に取り直し)からの報告。**鵜呑みにせず、こちらで再現してから直した。**
★1件は**報告者の見立てと軸が違った** ── 測ったら、名前ではなく「何を指しているか」だった。

- **`install.mjs` が「地図が空です」と、何も見ずに毎回言っていた** ── 条件が1つも無かった。
  配布先は **1239行の地図**があるのに毎回そう言われる。**計器が嘘をついている**形で、
  一度そう学ばれると**本当に空のときも読み飛ばされる**
  → 雛形に無い見出しの数で判定。**在るときは「N項あるので案内は出しません」と言う**
- **`install.mjs` が CI を二重に置いた** ── 冪等の判定が `${KIT}/check.mjs` という1本の綴り頼りだった。
  ★報告者の案(古い名前の一覧に足す)は名前の軸。**測ったら軸は「何を指しているか」**で、
  5つの現実的な形のうち**3つで二重置き**(改名前のパス / verdict だけの手直し版 / npm script)
  → 塊の道具(check / verdict / selfcheck / neighbors / index)を指していれば気づく。
    **それでも当たらない npm script は、置いたうえで「既に N本ある」と名指しする**
- **守りが「こちらで直した」と「正本が進んだ」を区別できていなかった**(報告者が判断を仰いだ点)
  ── 指紋が食い違えばどちらも同じ扱いで止まり、報告者は**堂々巡り**に入った
  (手で新しくする → 止まる → 指紋を手で入れる → 別のファイルが違うと言われる → git で丸ごと戻す)。
  ★材料は手元にあった ── **正本を一時領域に取っているのだから、突き合わせればよい**(報告者の指摘)
  → 実測: 指紋だけ古く中身は正本と同じ → **通る** / 本当に直した → 止まる(出口を2つ示す) /
    混ざっている → 古い分は名指しで通し、直りの分だけで止まる
  → どのファイルが食い違ったかは **`selfcheck --changed`**(新設)から受け取る。
    ★直す前は赤い文の**文言**を includes で見ていた ── **文言は変わる**(9.21 で実際に変えた)

## 9.21 (2026-08-30) ── 見るためには、やるしかなかった(この塊自身が起こした事故)

★**開発中に、私(AI)が実際に踏んだ事故から始まっている。**
`--send` の**表示だけ**を確かめるつもりで `--why --send` を回したら、
その機械には `gh` が入っていて認証も通っていたので、**本物の issue が公開リポジトリに立った**
(`ricordi-studio/guardian#2`。試験用の作り話だったが、公開されたことに変わりはない。許可を得て閉じた)。

- **根は「見るには、やるしかなかった」こと** ── `--send` に下見の口が無く、
  何がどこへ出るのかを知る方法が、実際に出す以外に存在しなかった。
  ★**`--stamp` とまったく同じ形**(9.17 で直したばかり)。**同じ日に、同じ形で二度目を踏んだ**
  → `--send --dry` を足した(宛先・題・バイト数・件数・見張った語数を出して**送らない**)。
    送る場合も同じ一行を必ず先に出す。案内文の既定も `--send --dry` にした
- **`--send` は「個人情報を見張っていない現場」からでも送れた** ── `private` が空だと
  B1c(見張り)は何もせず、その結果は B1e(送る)から**見えなかった**(局所変数だった)。
  しかも報告書の冒頭に「宣言が空のため見張っていません」と**自分で書いたうえで**送る
  → 見張りの結果を外へ出し、**見張っていない / 引っかかっている / 送り先が読めない**なら送らない。
  ★「足した事故に人名が混ざっている」場合は**既存の守りが効いていた**(穴はこの1つだけ)
- **正本のアドレスが2箇所にあった** ── `pull.mjs` の 正本(取り直し先)と `selfcheck.mjs` の
  べた書き(報告先)。引っ越したら**報告だけが古い方へ飛び続ける**
  → `pull.mjs` から読む。読めなければ送らない(推測で外へ出さない)

- **打ち切りを黙っていたので、道具が逆の助言をした** ── `--escaped` の環の展開は
  `ring <= 12` で**無言**に止まり、超えた犯人は `∞(環を増やしても捕まらない)` に化けて
  「この軸の外」に数えられ、「**環を増やすより**別の計器を疑え」と出ていた。**逆である**。
  実測(対照つき): 同じ事故を上限12→「環3・rings を上げる材料」/ 上限1→「∞・逆の助言」。
  ★「実測でここに達したことは無い」と書いてあったが、**達しても何も言わない**ので確かめようがなかった
  → 打ち切ったら言う。`打ち切り(N環まで見て届かなかった ── 測れていない)` と出し、
    逆向きの助言を止め、台帳にも記録する

- **`RULES.md` が指紋の対象外だった** ── 理由は「配布先で事故が増えて当然」だが、それは `WHY.md` の話。
  作法は配布先で勝手に育たない。外したままだと配布先が作法を書き換えても緑で、**還る道が無い**
  → 対象へ戻した(25件 → 26件)。実測: 配布先で1条足す → 赤になり、還す1枚にも載り、`pull` も守った

- `pull.mjs` の `spawnSync(cmd, args, {shell:true})`(**Node が DEP0190 で非推奨**)を引数の配列へ。
  括る作法(`JSON.stringify`)ごと要らなくなった ── 括り忘れる場所を消す

## 9.20 (2026-08-30) ── 余分な CR 1つが、git のテキスト判定を裏返していた

★**揃えるだけの変更**。改行を除いた中身が同一であることを機械で確かめてから出している。

- `neighbors.mjs` の230行目の末尾が `\r\r\n` だった。**CR が在るとバイナリ扱い**(正規化しない)、
  **消すとテキスト扱い**になり `core.autocrlf` が全行を書き換え扱いにする ──
  実測: 104行の変更が **1384行の書き換え**に見えた。
  エンジン15本のうち CRLF はこの1本だけで、**この CR のせいで正規化から外れ続けていた**
- ★**門は git の差分を読む。** 差分が全行に膨らむと全記号が「触れた」になり、
  近傍は `known` に吸われて **0件**になる ── **差分が膨らむと門は鳴らなくなる**
- → `.gitattributes`(`* text=auto eol=lf`)でリポジトリが流儀を決める。機械任せにしない。
  `neighbors.mjs` を LF に揃えた(残り14本は元から LF)
- → `.gitattributes` は**配らない**(`pull.mjs` の `現場のもの`)── 指紋は改行を正規化して
  照合するので配布先には要らず、配ると**他人のリポジトリの guardian/ 以下の扱いを黙って変える**

## 9.19 (2026-08-30) ── クラスで書かれた現場では、門が丸ごと盲目だった

9.18 の作業中に気づいた1件(引き継ぎの⑧)。**実測しないと一生出ない条件が、これで4件目。**

- **`class Foo {}` が定義として拾われていなかった** ── 拾うのは `function` と `const` / `let` だけ。
  クラスの中身は字下げされているのでメソッドも拾えず、**そのファイルは丸ごと持ち主なし**。
  実測: `class Cart` の `total()` から**税の掛け算を落とす**(本物の破壊)→
  **「触れた記号: (器のコードに変更なし)」・近傍なし・無言**。
  直したら `触れた記号: Cart` / `[環1] Checkout ← 「Cart」を呼んでいる`
  ★**JS/TS の現場の多くはクラスで書かれている。そこでは門が最初から最後まで鳴らない**
  → `DEF_CLASS` を足し、`export default` / `var` / 生成器 `function*` の取りこぼしも直した
- **持ち主の記号が引けなかった行を、黙って捨てていた** ── 最初の定義より前(import・冒頭の定数・
  HTML の `<style>`)も同じ穴。問えないのは軸の限界だが、**言わなければ「見た」と同じに見える**
  → 「持ち主の記号が引けなかった変更 N行は**問えていません**」と出す。
  ★見た目でコメント行を落とすのは**この報せだけ** ── 囲みコメントの途中は行だけ見ても
  `const x = a` / `  * b;` のような続きの式と区別がつかない。**門で落とすと見えない失敗になる**
  (実測: この塊自身で 55行 → 4行)
- 検査を足した(`selfcheck` B8c): 見本を建てて**実際に門を回す**。
  ★`class` を拾わない形に戻すと赤くなることも確認済み。git が無い機械では【未測】

## 9.18 (2026-08-30) ── 物差しが【ASCII と自分の現場】を前提にしていた

9.17 の続き。残していた根A・根C・根Dを実装した。**どれも「測ったつもり」の形**である。
掘っている最中に、**いちばん重いもの**がもう1件出た(下の★)。

**根A ── 「測れなかった」を緑に数えていた**
- `selfcheck` に **ok / ng の2語しか無く**、「見ていません」を ok に混ぜていた。
  まっさらな導入先が **「59件すべて期待どおり」** ── 全部宣言済みの正本と**同じ数字・同じ文**。
  ★この自己検査は `verdict` の**証拠①**である。
  → 第3の語彙【未測】。止めないが**数を分ける**。合否で拾いたい現場は
    evidence に `"unknownIf": "未測 [1-9]"` を足せば【不明】になる
- `skip_dirs` で外した場所は門でも定義が引けず、編集しても「器のコードに変更なし」。**しかも無言**
  → 「宣言 skip_dirs で外した場所の変更 N件は**見ていません**」と言う(--list / --gate の両方)
  ★地図には「門には効かない」と**逆**が書いてあった(9.17 で地図を実測に合わせ、今回は実装を直した)
- `測れなかった` が `--escaped` から二度と見られなかった → そこでも見る

**根C ── ASCII と自分の現場を前提にした物差し**
- `length >= 2` / 写経の `>= 4` は**ラテン文字の物差し**。日本語では1文字が1語。
  実測: `const 仮`(= `rmSync(recursive, force)` が消す先)の行を書き換えたら、
  触れた記号は **`走る`**(3つ前の無関係な関数)。直したら `仮` になり、読む9件が近傍に出た
- `#` を無条件にコメント扱いしていた。**既定の `ext` には html が入っている**。
  実測(対照つき): `.html` の `#main .row` → `#side`(直下の `querySelector(" #main")` が壊れる)は
  **「コメントと空白だけの変更」**と報告されていた ── 黙るのではなく**嘘を言っていた**。
  → `#` を落とすのは sh/py/rb/yml… のときだけ。知らない拡張子では落とさない(鳴る側へ倒す)
- ノートの欄名が `/g` 無しの **1行1件**・ASCII・3文字以上で、一般語12語も**エンジンにべた書き**。
  実測: 欄3つを全部変えて **1行JSON 0/3 / 整形済み 1/3** → **3/3**。
  → 全件拾う / 日本語を許す / 一般語は `neighbors.common_keys` で差し替え可能に(39条)

**根D ── 自分が作ったものを、次の回の根拠にしていた**
- `install` の「Claude Code の現場か」の判定材料に `CLAUDE.md` が入っていたが、
  **その `CLAUDE.md` は同じ install が下で作る**。実測: `.claude` の無い現場で
  1回目「仕掛けが見当たらないのでフックは入れていません」→ **何も変えずに2回目「フックを4本足しました」**。
  → マーカー区間を外して**何か残るときだけ**「人が書いた文書」と見なす。5経路とも実測

**★掘っている最中に出た、いちばん重いもの**
- **経路に空白があると、git は差分の見出しの末尾にタブを足す**(`+++ b/src/my file.js<TAB>`)。
  落とさないと拡張子の判定が外れ、**そのファイルは門から丸ごと消える**。
  `--escaped` の話ではなく、**日常の門でも、空白入りのファイルは一度も見られていなかった。**
  ★「空白入りの経路」で壊れるのは**3度目**(install.mjs の冒頭 / --escaped / ここ)
- あわせて `neighbors.mjs` の git 呼び出しを**シェル経由から引数の配列へ**(括り忘れる場所を消した)

## 9.17 (2026-08-30) ── 記録に【鮮度】が無く、門も指紋も一言で無効化できた

9.16 で掘り残した違和感17件に段2を当てた。**14件が実在**(前回 11/12。20〜25%の帯からも出た)。
うち3件が同じ根を指していた ── **この塊が作る記録は「在るか」しか見ておらず、「いつのものか」を持たない。**
直したのはその根(根B)だけ。残り(根A・C・D と単独5件)は `docs/HANDOVER.md` に実測つきで置いた。

- **門が、別の変更の古い回答で通った** ── `--gate` は近傍の顔ぶれだけで照合していた。
  回答ファイルは `range` を**記録しているのに、照合していなかった**(そして `range` は
  作業木が汚れていれば常に `"HEAD"` なので、そもそも別物を区別できない)。
  実測: 無害な変更に「影響なし」と答えて通したあと、**同じ関数から冪等の守りを削除**しても
  `--list` 無しの `--gate` が「通過(2件すべてに回答あり)」。**回答はコミットに含めるので、次の回も引き継ぐ。**
  → 回答に【根の本体の指紋】を持たせた。根が変わればそこから生えた回答だけが失効する。
    **無関係な直しでは失効しない**ことも実測(鳴り過ぎる門は読み飛ばされる・7条)
- **`--stamp` 一言で、配布先の直りが記録ごと消せた** ── `pull.mjs` は「配られたときの中身と違います」
  という**文字列**で守っている。`--stamp` は確認も一覧も無くその文字列を消す。
  しかも `--stamp` は**配布先の人が読む赤い文の中で案内されていた**
  (「意図した変更なら」── バグを直した現場は、それをしている)。
  → 塊が単独のリポジトリか(=正本か)で分けた。配布先では止め、案内からも消す。
    正本では**何を押すのかを先に一覧で出してから**押す。還す道(`--report`)はそのまま
- **人が理由を書くファイルを、機械が黙って全消しできた** ── `--list` が `JSON.parse` の失敗を
  **空の catch** で握り潰し、下書きで上書きしていた。実測: **末尾に `,` を1文字**足すだけで全部消えた。
  → 読めなければ上書きせずに止める。近傍から外れる回答は名前を出してから落とす
- 地図の誤りを1つ直した: `skip_dirs` は「門(--gate)には効かない」と書いてあったが、
  実測では**門もそこで黙る**(しかも無言)。地図を実測に合わせた(振る舞い自体は判断待ち)
- ★記録(直していない): `selfcheck` の B6a が使う `正本か` は `HERE/../.git` も見るので
  **配布先でも真**になり、「ここは正本ではないので見ていません」の枝は通常の置き方では**一度も通らない**

## 9.16 (2026-08-30) ── 違和感が、自分のバグを11件出した

依頼主「今日入れた仕組みは価値があったの?」── **使っていなかった。**
hunch.md を作った日の監査で、その2段を一度も走らせていなかった。
そこで Guardian 自身のエンジン4本に当てたら、**36秒で違和感30件。12件を掘って11件が実在**した。
**59件の自己検査も、直前の監査(4観点)も、1件も見つけていない。**

- **根A 「測れなかった」が出口0(通過)だった** ── `sh()` の三項が両辺同じで git の失敗を握り潰し、
  CI の浅いクローンでは**門が作った日から死んでいた**。宣言なし・コーパス0も同じく通過していた。
  しかも合否は「✓ 通過 / 測れた割合 100%」と出していた ── **測っていないのに。**
  → 出口2(不明)を新設。4条件とも 通過→不明 に変わることを実測
- **根B 配布物の集合が2箇所にあり、片方が短かった** ── 指紋12件 / 実際に配るのは23項目。
  `index.mjs` / `githooks` / `templates` / 文書は配られるのに指紋が無く、**取り直しで現場の直りが消えた**。
  → ENGINE_FILES を【配るもの】から導く(39条)。12件 → 25件。
  → 上流から消えたものを配布先から撤去する道も無かったので、足した
- **根C 形頼みの拾い方が、当たらなくても緑** ── **selfcheck が生まれた事故そのもの**。
  → 見本(probe)を添え、拾えなければ照合自身を落とす
- ★掘っている最中にもう1件: `inDirs` が `code:["."]` で直下のファイルを拾っておらず、
  **環の測定(--escaped)が作った日から一度も動いていなかった**
- 他: 一時フォルダが塊の外(正本では**デスクトップ直下**)で先に rm -rf していた /
  報告書が cwd に落ち**自分の自己検査を赤くした** / 台帳に重複判定が無く誤った助言を出した /
  分類表が古いままで**取り直しを拒む自己ロック** / CLAUDE.md の追記が KIT 依存で**毎回増えた**

★11件のうち少なくとも4件が、**この塊自身が WHY に書いた規律の違反**だった
(文字列で判定するな / 当たらない検査を緑にするな / 同じことを2か所に書くな)。
★規律: **道具を作った日に、その道具を自分に当てる。**使っていない道具の価値は、測っていない。

## 9.15 (2026-08-30) ── アクセルを1つ置いた(Guardian で初めて、止めない機構)

依頼主「事故例が集まれば自然と性能が上がると思っていたが、今は、**人の脳をどうシミュレートするかを
悩み続けること**にしか発展は無い気がしてきた」── 方針の転換。

実測が裏づけた: **194件集めた状態で、同じ欠陥が1セッション中に4回出た**(4回とも人が先に正解に着いた)。
事故ごとに門が1本増えるだけで、**門は判断に合成されない**。

- **hunch.md を新設**(作法。器ではない)── 2段:
  段1 **理由を書かせずに**違和感を20件(10秒)/ 段2 出た**全部**にサーチライトを当てて掘る(4分)
  ★実測: 変更した本人は6つの壊れのうち**5つの現場に手を入れて1件しか指摘しなかった**(1/6)。
    同じコードに「一から書くなら?」と問うた別の1体は **6/6**
  ★**違和感は「主張」ではなく「座標」** ── 外れた思いつきの隣に本物があった(実測2件)
- **宣言に `context`(運用の文脈)** を新設 + 雛形にも。**ここが空だと運用の欠落は永久に出ない**
  ── 問いを3通りに変えても1ミリも動かず(5回とも同じ3件を落とした)、
     文脈4行を足した1回だけが動いた(3/6 → 5〜6/6)
- **selfcheck に B9**: `context` が空なら「**運用の欠落は見ていません**」と正直に言う(止めない)。
  個人情報の見張り(B1c)と同じ形 ── 無音を安全と読ませない。両方向を実測で確認
- `audit.md` に**観点0**(観点1〜4は探すものを指定しているので、指定していない形は通り抜ける)
- SPEC に **R18**。★**他の要件が全部ブレーキ**(止める・緑にさせない・戻らせない)なのに対し、
  **これだけがアクセル** ── Guardian が3週間で作ってきたものは、全部「止まろうとする力を止める」装置だった

★**これは自発性ではない。** 引き金は必ず外(人が呼ぶか、完了の瞬間)。
  文書にそう書いた ── 承知で低い段を選んだなら、承知だと書く(RULES 46条の精神)。
★**まっさらな現場に入れて実走させたら、直したはずの所が届いていなかった** ──
  雛形に `context` を足したのに、`install.mjs` は雛形を写さず宣言を自分で組み立てていた。
  **この日の要が「文脈を宣言に書く」ことなのに、書く場所が作られず、書けとも言われなかった。**
  導入が組み立てる宣言に `context` と、**空なら促す1行**を足した(場所を作るだけでは書かれない)。
  ★あわせて実測できたこと: 9.13の守りが効き、degit が `guardian/CLAUDE.md` を持ち込んでいるのに
  導入は**プロジェクトの根**へ正しく置いた。配布先で自己検査59件すべて期待どおり。

★**保留の置き場と浮上**(「なんかね」の本体)は**まだ測っていないので作っていない。**
  測る前に作れば、この塊が何度も踏んだ「効かないものを作る」をまた踏む。

## 9.14 (2026-08-29) ── 配布境界を【白と黒の両方】にした / 別の仕事は名前を分けた

依頼主の判断2つ。どちらも「二択で詰まっていた私の見方」を外から直したもの。

**① ホワイトアンドブラック**(配布境界)

ブラックリストだけだと**新しく作ったものが既定で配られる**(1日で3回漏れた)。
反転してホワイトリストだけにすると、今度は**書き忘れたものが黙って配られない**
(FIND-001 Case G)。依頼主「両方持てばいい。配布と導入は日常の作業じゃないんだから厳重でいい」。

- `pull.mjs` が `配るもの` と `現場のもの` を**両方**持つ。
  どちらにも載っていないものが1つでもあれば**取らずに止まる**(門)
- `selfcheck.mjs` が同じ2つの宣言を読み、3方向を照合(双子・RULES 44条):
  直下が全部どちらか一方に分類済みか / エンジンが【配るもの】に入っているか(**欠落の向き**) /
  install が作るものが【現場のもの】に入っているか(**混入の向き**)
- 3つとも、わざと崩して赤くなることを確認済み
- ★これで**書き忘れが「事故」ではなく「赤」になる**。どちらの向きに忘れても止まる

**② `findRoot` → `findInstallRoot`**(名前を分けた)

監査で「同名の関数が2ファイルに(写経の疑い)」と報告したが、依頼主の指摘どおり
**これは重複ではなかった** ── 片方は導入**前**に走るので宣言を目印にできず、目印が違う以上
ひとつにできない。同じ名前にしていたことの方が間違いだった。改名し、なぜ2つ在るかを
その場に書いた。走査の誤検出が1件減った。

## 9.13 (2026-08-29) ── 混入したゴミが【根】を狂わせ、導入が嘘の成功を報告していた

依頼主「変更 → GitHub 更新 → 入れている現場が反映、という流れは問題なく動くのか?」
**動いていなかった。**模擬の配布先を建てて端から端まで実走させて分かった。

配布先(9.8)の古い `pull.mjs` は `CLAUDE.md` を【配らないもの】に持っていないので、
取り直すと `guardian/CLAUDE.md` が出来る。すると根を探す判定がそこで止まり、
`install.mjs` はフックを `guardian/.claude/settings.json` へ書いて
**「✓ .claude/settings.json にフックを 4 本足しました」と報告する** ──
配布先の本当の設定は一度も更新されない。**フックが1本も入らないまま「入りました」と言う。**

- `install.mjs` / `hooks/lib-root.js` の根の判定に守りを追加: **塊のフォルダ自身を根と見なさない**
  (塊は `check.mjs` + `selfcheck.mjs` で見分ける。正本では `.git` が同じ場所に在るのでそちらが勝つ)
- `selfcheck.mjs` に **B8** を新設: 見本を建て、混入した目印があっても根が1つ上を指すことを実測する。
  守りを外すと赤くなることを確認済み
- ★**9.8 は2種類ある**ことも分かった(除外リストを足したとき版を上げなかった)。
  同じ番号で中身が違う配布物が世に出ていた ── 版は嘘をつかないときだけ役に立つ

★教訓: **「動くか」は、流れの端から端まで実際に走らせるまで答えない。**
道具が「次にやること」を案内しているなら、その次も実行して確かめる。

## 9.12 (2026-08-29) ── 配布境界の列挙に、独立した根拠を与えた(配布先からの報告)

配布先が 9.11 へ取り直そうとして `--check` で気づき、取らずに報告してきた ──
【配らないもの】に `.claude/` と `CLAUDE.md` が入っておらず、Guardian 自身のフック登録と
開発規範が配布先の `guardian/` へ流れる状態だった。**列挙に網羅の根拠が無かった。**

- `pull.mjs` の【配らないもの】に `.claude` と `CLAUDE.md` を追加
- `selfcheck.mjs` に **B7** を新設: `install.mjs` が配布先の ROOT に作るものは、定義上すべて
  現場固有物である ── `pull.mjs` の【配らないもの】がそれを全部含んでいるかを機械で照合する。
  わざと戻して `.claude, CLAUDE.md` が名指しで赤くなることを確認済み
- ★これは AI Council EXP-001 が Unknown として残した「独立した第二根拠は何か」への部分的な答え。
  **「配る道具」と「据え付ける道具」は、同じ境界を逆側から知っている**
- ★報告の見立ての訂正: 配布先ルートの `CLAUDE.md` は上書きされない(`pull.mjs` は `guardian/`
  配下にしか書かない)。模擬の配布先を建てて実走させ、ルート側が無傷であることを確かめた
- ★まだ閉じていない: install が作らない現場固有物(`.guardian` / `research`)はこの照合では守れない。
  ホワイトリストへの反転は保留(反転すると配り忘れが静かに起きる ── EXP-001 Round2 Case G)

## 9.11 (2026-08-29) ── Council EXP-001から、最初の検査を1本昇格

`research/council/` のCouncil EXP-001(pull.mjsの配布境界事故を3AIで検討)で見つかった
FIND-001「配布境界は宣言すれば守れるわけではない」から、実装コストが低く効果の範囲が明確な
部分だけを昇格させた。全体解ではない ── 誤分類そのものを一般的に検出する方法はまだUnknown。

- `selfcheck.mjs` にB6を新設: `ENGINE_FILES`(配布物として指紋照合しているエンジン一覧)が、
  `pull.mjs`の【配らないもの】(現場のもの)に誤って分類されていないかを検査する。
  わざと壊して(`hooks`を配らないもの側に混入させて)赤くなることを確認済み
- ついでに、指紋照合(B2)とこの新しい検査(B6)が**同じエンジン一覧を1箇所(`ENGINE_FILES`)
  から読む**ようにした(直すまでは2箇所に同じ配列が重複していた)

## 9.10 (2026-08-29) ── 複数AIで構造破壊の原因を研究する場(Council)を新設

Guardianの開発そのものを、複数のAI(Claude・ChatGPT・Gemini)の独立回答→相互批判→自己反証で
検討する場を `research/council/` に作った。AIを判定者として使わない(多数決をしない)のが核。
いまはコード0行・全部手動。将来像(orchestrator・adapter等)は `VISION.md` に分け、
「実験で必要性が確認されるまで実装しない」と明記した ── 器を情報より先に固めない、という
Guardian自身の原則をCouncil自身にも適用した。

- `research/council/`: README・VISION(将来像)・protocol(Round0/1/2・昇格の手順)・roles・
  `experiments/EXP-001`(被験体: pull.mjsが正本分離直後の混入事故をなぜ検出できなかったか)
- `pull.mjs` の除外リスト(`現場のもの`)に `research` を追加。**配布物ではない**ので配布先へ流さない
  (直近の事故 ── 正本を分けた瞬間に自分の現場ごと配ってしまった ── と同じ穴を、今回の追加で
  再現しないため)

## 9.9 (2026-08-29) ── 塊が自分自身を監査しながら開発できるようにした

正本をこのリポジトリへ分けた直後、**自動で鳴る部分が1つも入っていなかった**。
手で回せば全部動くが、フックが無い ── **監査を受けながら開発する形が半分しか実現していなかった**。

原因は2つ、どちらも**塊が自分自身の現場に居るとき**にしか出ない:

- **KIT が存在しないパスを指していた**: 塊がリポジトリそのものだと相対パスが**空文字**になる。
  空なら `guardian` に落としていたので、案内が `node guardian/check.mjs` ── **そんなパスは無い**。
  → 空なら `.` ──「その場所そのものが塊である」と正しく言う
- **導入の判定**: 塊自身の現場では `CLAUDE.md` が配布物なので判定材料にならない。
  → `KIT === "."` なら Claude Code 前提で入れる(要らなければ `--no-hooks`)

★**直した瞬間、塊自身の検査が止めた。**`install.mjs` を直したので指紋が食い違い、
  完了を名乗る手前のフックが差戻を返した ── **監査を受けながら監査を直す**形が実演された。

★さらにこの版を書いている最中、**版の照合が働いた** ── `KIT_VERSION` を 9.9 にしたのに
  CHANGELOG が 9.8 のままで、自己検査が落ちた。**版が嘘をつく前に止まった。**

## 9.8 (2026-08-29) ── Claude Code でない道具のための、git フック

依頼主「フックの機能が無いのは Codex でしたっけ? **その代案はもうあるのですか?**」
**無かった。**外部評価にも同じ指摘を受け「構想はあります」と答えていたが、1行も書いていなかった。

- **githooks/pre-commit を新設**: `git config core.hooksPath guardian/githooks` の1行で有効。
  コミットの手前で合否を回し、**差戻なら止める**(不明・注意は止めない)
- **代替できるのは3本まで**と正直に書いた。編集の直前に地図を差し込む / 応答の直前に監査を催促する
  ── **この2つは代替できない**(その瞬間が git に無い)。失われると明記
- 導入のときに**有効にする1行をその場で出す**(同梱しただけでは誰も使わない)

★実測: Claude Code の仕掛けが無い現場で、**差戻ならコミットが止まり、直せば通る**ことを確かめた。

## 9.7 (2026-08-29) ── 外部評価の指摘を全部つぶした

依頼主が文書を外部のAI2つ(Gemini / ChatGPT)に読ませ、指摘を全部対応した。

- **逃げ道(guardian:ok)の歯止め**(Gemini): 理由の無い逃げ道は落とす。数が宣言の okMax を
  超えたら落ちる(--tighten で下げるラチェット)。★入れた直後に「理由が【同上】」を1件捕まえた
- **測れた割合**(ChatGPT): 「不明1件」と「不明8件」が同じ言葉だった。割合を出し、
  下がったら「宣言が実装に追いついていない合図」と言う。★**割合は判定に使わない**(表示だけ)
- **WHY の索引**(ChatGPT + 依頼主の思想): index.mjs が**重み付き**の索引を作る。
  ★依頼主「WHYに溜まっていくのは、構造化がちゃんと出来ていないことによる結果が積みあがってるだけ。
  欲しいのは**美しい構造化の実現につながる発見・発明の芽**」── これがそのまま分類の軸。
  実測: 全183件のうち**構造の芽は21件**。残りは宿題の山である。
  ★索引が古くなったら合否が落ちる(作る道具を書いただけでは腐る)
- **SPEC に e2e の位置づけ**(ChatGPT): Guardian 自身は実行時の正しさを判定しない。
  プロジェクトが持つ test/e2e を**証拠として集めて4語に落とすだけ**

★要件は R15 まで。SPEC の検査が、足した口とファイルを全部自動で捕まえた。

## 9.6 (2026-08-29) ── 自分で禁じた「数の腐り」を、自分でやっていた(外部評価の指摘)

依頼主が文書6枚を**外部のAI2つ**に読ませて評価させた。その指摘:

> READMEでは「事故181件」、同じREADMEの後半では「既存の177件」。
> METHOD は「実数を文書へ書くな。書いた瞬間から腐る」と明記しています。
> **Guardianが自分で禁止している事故が、Guardian自身にもう起きています。**

そのとおりで、実際は**4箇所**ずれていた。

★**規範も検査も両方あったのに漏れた。**検査の網が「全N条 / 全N件」の形だけで、
  「事故181件」のような**裸の実数は素通り**していた ── **網の外は、検査が無いのと同じ**。

- 実数を文書から追い出した(6箇所)。規模は「件数は selfcheck が出す」に
- **網を広げた**: 「事故N件」「作法N条」「規律N条」も実数と突き合わせる。
  「自己検査N件」は**書くこと自体を禁じる**(実行のたびに変わるので正しい数が存在しない)
- 逃げ道は行末の guardian:ok ── **過去の版の数を語る文**は落とさない

★その過程で自分でもう1つ壊した: 「実数を追い出す」を機械的に当てて、
  ラチェットの下限(機械が読み書きする値)まで消した。検査が即座に落ちて気づけた。
  **飾りの数と、機械が使う数は、見た目が同じである。**

★**この事故は内部の監査を2回回しても出なかった。出したのは初見の外部AI。**
  配って評価してもらうことに、これだけの価値がある。

## 9.5 (2026-08-29) ── 入口を整えた(公開したので、README が最初に見られる)

依頼主「コードが格納されているフォルダの中には、Readme なり何なり、
それが何なのか? 何から始めたらいいか? を説明しているものがあるのが普通ですよね?」

あった。だが**SPEC.md を作った翌日、README がそれを案内していなかった** ──
文書を足すたび入口が遅れる、という同じ形(46条)。

- **README の冒頭を【目的別の入口】に**: 何を知りたいかで読むものが決まる表。
  ★他のAIに理解してもらうなら **SPEC.md → METHOD.md の2枚**で足りる、と明記
- **SPEC の冒頭に「これは何か」**: 単体で渡されても迷子にならないように。
  ★SPEC だけだと「規約に書けばいいのでは」と思われる ── それは最初に否定した道なので、
  METHOD へ送る1行を置いた
- **README の案内漏れを機械が見張る**: 塊に .md を足したら、README が名指しするまで赤くなる。
  ★中身の良し悪しは測れないので、**名指しされているかだけ**を数える

★役割は分けたまま: **README = 入口**(GitHub のトップに出る)/ **SPEC = 要件**(機械が見張る)。
  冒頭の1行だけ重なるが、**どちらから読み始めても迷子にならないため**の意図的な重複
  ── 39条に触れるので、承知のうえと明記する(依頼主の判断 2026-08-29)。

## 9.4 (2026-08-29) ── 仕様書と地図の役割を分けた(監査が2回連続で拾った件)

2026-08-28 と 29 の監査で、**2回連続**「塊の新しい層が地図(CODEMAP)から漏れている」が出た。
原因は、塊の説明が **SPEC.md と地図の2箇所**にあり、塊が変わるたび両方を手で直していたこと。

依頼主の判断で、こう分けた:

- **仕様は SPEC.md が正本**(何を満たすか / 満たさないか / 口の一覧)。**機械が見張る**
- **地図に書くのは【その現場でどう使っているか】だけ** ── 仕様を写さない
- ★**地図は機械に見張られない**(索引なので全部の照合は筋が違う)。
  だから**監査で拾う**と決めた ── **回さなければ漏れたまま**という弱点を承知のうえで

→ `audit.md` の観点2に足した: **配布物を使っているなら、その仕様書と地図を突き合わせる**。
  選択(監査で拾う)を実効あるものにするための1行。

★**弱点を承知で選んだことは、承知だと書く。**「監査を回さなければ漏れる」を隠して
  「対策した」と書くと、次の人はそれを守りだと思う。

## 9.3 (2026-08-28) ── **事故の記録そのものを集める道**(本体を育てる)

依頼主「事故レポートが集まるほど性能が上がるので、インストールしたプロジェクトには
レポート提出の協力をお願いしたい。もちろんそのレポートを集める方法についても。」

それまで還せたのは**塊のコードの直り**だけで、**本体(WHY.md の事故)を育てる道が無かった**。

- **WHY_SEEN**: 配られたときに何の事故があったかを記録(--stamp が一緒に押す)
- **--why**: その現場で**足した事故だけ**を1枚にする。増えた分は WHY_SEEN との差で分かる
- **--send**: 承認の表明。既定は送らず**送るコマンドを見せる**だけ
- ★**個人情報が混ざっていれば、1枚そのものを作らない** ── 作ってから気をつけろ、では遅い
- README に「なぜ協力をお願いするか」を実例つきで(空白入りフォルダのバグは、
  元の現場では一生出なかった。**現場が増えるほど踏む石の種類が増える**)

### 途中で、自分の穴が2つ出た

- **宣言を読む所が2箇所にあり、片方が【2つ上】を見たまま**だった(9.0 の引っ越しの取りこぼし)。
  読めなければ黙って空を返す作りなので、個人情報の見張りが**混ざっているのに
  「していません」と言い続けた**。→ 読み口を1つにした(39条)
- 試すときの仕込みが、**意図した節ではなく別の節に入っていた**(replace が最初の1件に当たった)。
  検査は正しく、**私の試し方が間違っていた** ── 仕込みも実測で確かめる

## 9.2 (2026-08-28) ── **公開した**。誰でも1行で入る

正本: **https://github.com/ricordi-studio/guardian**(公開)

- **入れ方が1行になった**(アカウントも認証も要らない):
  ```
  npx degit ricordi-studio/guardian guardian
  ```
- `pull.mjs`(取り直し)も**認証なしで動く** ── 実測: 認証情報を無効にした環境で確認

### 公開の前にやったこと

- 過去のコミットに残っていた個人情報を消すため、**履歴を1つに畳んだ**
- ★force push では**消えなかった**(SHAを知っていれば直接読める。実測)。
  確実に消す唯一の方法は**リポジトリの作り直し**だったので、消して作り直した
- ★**匿名で clone して、到達できる全オブジェクト31個を総なめ**し、
  人名・アカウント名・現場名が1つも無いことを確かめた
  ── 「伏せたつもり」で公開しない

★それ以前の版の記録は CHANGELOG に全部残っている(14版ぶん)。
  履歴を畳んで失うのはコミットの粒度だけで、**何をなぜ変えたかは1つも失っていない**。

## 9.1 (2026-08-28) ── 要件(SPEC.md)を置き、**遅れないことを機械が見張る**

依頼主「新しい機能が追加されたら、その要件定義書も更新されるべき。
そして**それのチェックを自分でやるところまで**。」

- **SPEC.md を新設**: 解く問題 / 満たすこと(R1〜R11)/ **満たさないこと(境界)** /
  動作の前提 / **口の一覧** / 検証済みの事実
- ★いちばん価値があるのは**満たさないこと** ── コードの正しさは見ない / 実行時のバグは見ない /
  セキュリティ検査ではない / 勝手に直さない / 列挙は判定ではない / 他の道具では自動で鳴らない。
  口頭でしか伝えていなかった境界を文書にした
- **更新しないと通れない**(注意書きではなく検査):
  口(--xxx)が SPEC に載っていなければ赤 / エンジンのファイルが名指しされていなければ赤
- ★実測: 書いた直後に**フック4本が名指しされていない**と落ちた(まとめて書いていたため)。
  架空の口 --zz-new-port を足したら**名指しで落ちる**ことも確かめた
- ★**測れない所は、測れないと書いた**: 「満たさないこと」は機械が見張れない
  ──**できることは数えられても、できないと言い切ったことは数えられない**。
  だから監査のたびに人が読み直す、と SPEC 自身に書いた

自己検査 50件 → **53件**

## 9.0 (2026-08-28) ── ルート直下へ引っ越し、どの開発ツールでも使える形に

### ① 置き場を tools/guardian → guardian へ

依頼主「名前を Guardian にしたのに、そのルートが tools なのは気になる。おかしくないですか?」
**おかしかった。**tools は「道具です」以上の情報を持たず、しかも Guardian の本体は
事故の記録(WHY.md)と規律(RULES.md)── **「道具」という括りは中身より狭い**。
8.0 で codemap → guardian を直したのと**同じ問題が、1階層上に残っていた**。

- `guardian/` … 配られる塊(コードと読み物)。**見える** ── 読んでほしいものだから
- `.guardian/` … この現場の作業記録(回答・監査時刻)。**隠れる** ── 機械が書くものだから
- `guardian.config.json` … この現場の宣言。ルート直下のまま(**人が書く**ものだから見える)

★あわせて、門の回答ファイルが `.claude/` に置かれていたのを `.guardian/` へ移した。
  他の道具の現場で `.claude/` に書くのは筋が通らない。

### ② Claude Code でない現場に対応した

`install.mjs` が「この現場に Claude Code の仕掛けがあるか」(`.claude/` か `CLAUDE.md`)を見て、
**無ければ `.claude/` を作らずに、手で回す手順をその場で全部出す**(`--hooks` / `--no-hooks` で上書き)。
どちらにしたかは**必ず言う** ── 黙って片方を選ぶと、なぜ鳴らないのか分からなくなる。

`install.md` に「この道具が Claude Code でない場合」の節を追加。
**本体は1つも減らない**(検査・門・合否・監査は Node だけで動く)。
Claude Code に依存するのは**自動で鳴る部分だけ**、という境界を表で示した。

★実測: `.claude` も `CLAUDE.md` も無い現場で導入し、**`.claude` を作らないこと**と、
  出した手順(検査・自己検査・全体走査・合否)が**全部そのまま動くこと**を確かめた。

## 8.2 (2026-08-28) ── 個人情報を伏せた(公開できる状態に)

依頼主の指示。塊は事故の記録が本体なので、**実機で起きたこと**を具体的に書いてある ──
その具体性が価値であると同時に、個人名が入る所でもあった。

- 人名を**役割の語**に置き換えた(201箇所)。「◯◯の実機で」→「依頼主の実機で」
  ★伏せても**教訓が死なない**ことが条件。「実機で見つかった」まで削ると、
    机上の話に見えてしまう ── それが一番の損
- 家族の名前(3箇所)・秘書の名前(4箇所)・配布先のフォルダ名を伏せた
- **残したもの**: 「この現場(AI秘書)」── 現場の【種類】であって個人情報ではない。
  むしろ「全部実機で起きた」ことの根拠なので残す
- 機械で確認: 人名・アカウント名・現場名が**1つも残っていない**ことを数えた
  (自分の目で「たぶん消えた」と言わない)

## 8.1 (2026-08-28) ── 正本を GitHub に置いた

**正本: https://github.com/ricordi-studio/guardian**(非公開)

これまで正本は「現場A の tools/ 」で、配布はフォルダのコピーだった。
どのコピーが最新かは人の記憶に頼っており、実際に**配った先で直した分が戻らず、
両方の版が同じ番号のまま食い違った**(7.6 の事故)。正本を1つにして、そこから取る形にした。

- **pull.mjs を新設**: 正本から取り直す1行。
  ★取り直す前に **この現場で塊を直していないか**を必ず見る ── 直していたら**止まる**。
    上書きすると、その現場の直りが黙って消えるから(先に --report で1枚を作って還すこと)
  ★何が変わるかを数えて出してから置き換える(黙って上書きしない)
  ★--check なら取らずに差だけ見る
- README の入れ方を「正本から取る」形に。手元のコピーは最新とは限らない
- 指紋の対象に pull.mjs を追加

★これで【配る → 直す → 還す → 配り直す】の輪が、記憶ではなく道具の上で回る。
  ただし**人の承認は残す**(--report の1枚を人が読んでから渡す)。

## 8.0 (2026-08-28) ── 名前を **Guardian** にした

依頼主「この検査ツール全てを Guardian という名称にするのはどうですか?」

**賛成した理由**: `codemap`(地図)は、いまや塊の**一部**でしかない。
実際の中身は5層(合否/門/検査/計器/規律)+ 監査 + 還流で、地図はその1つ。
**名前が中身より狭い = 名前が嘘をついている** ── この塊がずっと直してきたのと同じ形が、
塊自身の名前に残っていた。

### 変えたもの

| 前 | 後 |
|---|---|
| `tools/codemap/` | `guardian/` |
| `codemap.config.json` | `guardian.config.json` |
| `/codemap-audit` | `/guardian-audit` |
| 通行印 `codemap:ok` | `guardian:ok`(器の49箇所を一括置換) |
| `.claude/codemap_audit_at` | `.guardian/audit_at` |

### 変えなかったもの(理由つき)

- **`docs/CODEMAP.md`(地図)** ── 地図は Guardian の**中の1層**。地図という機能の名前として正確
- **`hooks/codemap.js`** ── このフックは「編集の直前に**地図**を差し込む」役。名前が正確
  (一括置換で参照だけ `guardian.js` になっていたのを、7ファイル分ぜんぶ戻した)
- **WHY.md / RULES.md / METHOD.md** ── 中身の名前であって、塊の名前ではない
- **`claude/codemap-audit` ブランチ** ── クラウド側の設定なのでリポジトリからは変えられない。
  文書だけ改名すると**実際と食い違う**ので、実名のまま残し理由を添えた

### ついでに門を直した(改名が炙り出した)

通行印を一括置換したら、**コメントしか変わっていないのに31件の近傍に答えを求められた**。
ロジックは1文字も動いていないので、答えは全部「影響なし・コメントだけ」になる ──
**それは門ではなく儀式である。**答えを埋めれば通る門は、次に本物が来ても同じように埋められて通る。

→ `parseDiff` を作り直し、差分の塊ごとに**消えた行と足された行をコメントと空白を除いて突き合わせ**、
  同じなら数えないようにした。**除いた行数は黙らず表に出す**(何を見なかったか分からない計器は計器ではない)。
  実測: 31件 → **18件**(58行を「中身が変わっていない」と正しく除いた)。

### 配布先へ

**この版から、配布先はフォルダ名ごと入れ替えが要ります。**
`tools/codemap/` を消して `guardian/` を置き、`node guardian/install.mjs` を回してください
(冪等。フックの登録と宣言の名前が新しくなります)。

## 7.7 (2026-08-28) ── 改善の還流(配った先の直りが、元へ還る道)

7.6 で「配られたときの中身と違う」ことは分かるようになった。だが**気づいた後、人が手で運ぶ**
しかなかった。依頼主「監査後にこちらに送る文章を書いて、ユーザーから承認を得られたら、
こちらに送ってきて、それを反映できる、みたいな」── その形にした。

- **指紋をファイルごとに持つ**(`ENGINE_FP`)── 全体で1つだと「どれかが変わった」しか言えず、
  **何を直したのか報告できない**(報告できなければ、還る道が無い)
- **`selfcheck --report`**: 変わったファイルの中身と渡し方の説明を `guardian-report.md` に1枚で書く
- **人の承認を挟む**: その1枚を人が読んで承認し、元の塊のAIへ渡す。**自動送信にはしない**
  ── 中身がそのまま外へ出るので、何を送るかは人が見て決める
- **受け取る側はAIが読んで取り込む**(機械が丸ごと上書きしない ── 元の改善が黙って消えるため)

★サーバーは**いまは要らない**。同じPCの別フォルダなら1枚をコピーで運べば足りる。
  別のPC・他人へ配るようになったら要るが、**流れも人の承認もそのまま**で、
  運ぶ手段が「コピー」から「送信」に変わるだけ。

★実測: 架空の現場を作って塊を配り、そこで1ファイル直し、
  **名指しで赤くなる → --report で1枚ができる**ところまで通した。

## 7.6 (2026-08-28) ── 配った先で直った分が、戻ってこなかった

別プロジェクト(積立と引き出しの金利シミュレータ)へ配って使ってもらった。
**塊自身のバグが2つ見つかり、向こうで直された。だが元の塊には戻ってこなかった。**
しかも両方の `KIT_VERSION` が同じ 7.5 のまま ── **版が嘘をついていた。**

### 逆輸入した2つ(どちらも汎用のバグ)

- **`install.mjs` のパス解決**: ファイルURLから自前で pathname を削っていたので、
  **フォルダ名に空白が入ると壊れる**(URLでは %20 になる)。`fileURLToPath` を使う形へ。
  ★こちらのパスには空白が無いので**一生出なかった** ── 配布先(空白入り)で初めて出た
- **`check.mjs` のセレクタ照合**: `className = ...` の = の前後の空白を許していなかった

### 構造として塞いだ: エンジンの指紋(`ENGINE_FP`)

版の数字は人が上げるので、直した人が上げ忘れれば黙って食い違う。
だから**中身そのものの指紋**で見る(名前ではなく中身で照合)。

- `selfcheck.mjs` がエンジン(mjs とフック)の指紋を計算し、`ENGINE_FP` と照合する
- 違えば **★この塊は配られたときの中身と違います** と赤にし、
  「直したなら元の塊へ戻して版を上げること」と促す
- 意図した変更は `node guardian/selfcheck.mjs --stamp` で押し直す
  (`check.mjs --tighten` と同じ思想 ── 一方通行を人の手作業にしない)
- 文書(WHY.md・RULES.md)は数えない ── 配布先で事故が増えるのは正当

★実際に配布先の塊へこの検査を当てて、**「配られたときと違う」と言えることを確かめた。**

### 使ってもらった評価から(向こうのAIの言葉)

> いちばん価値を発揮したのは「事前に仕込んだ検査」よりも、**「監査という行為そのもの」**でした
> ── 実際に手を動かして地図と実装を突き合わせたからこそ、ズレも、ツール自体の欠陥も見つかった。
> **検査を仕込んで放置するだけでは、ここまでの効果は出なかった**と思います。

この観察は正しく、しかもこの塊の設計そのものを言い当てている ──
だから催促は**時間ではなく変更で駆動する**(`hooks/clock.js`)。
「回す気になったとき」ではなく「実装が動いたとき」に監査を促す仕掛けが、この塊の中心にある。

## 7.5 (2026-08-28) ── **実際に他所へコピーして動かした**(初めて)

「持ち出せる塊」と言いながら、**一度も持ち出して試していなかった**。まっさらなgitリポジトリへ
コピーして端から端まで回したら、**穴が4つ**出た ── どれも「置いてあるだけで誰も読まない/動かない」形:

- **雛形のCLAUDE.mdが書く層の数が古いまま**(1つ足りない)── 導入した瞬間から自己検査が赤かった
- **門(neighbors)の宣言が導入時に入らない** ── `--gate` も `--sweep` も「宣言が無いので回しません」で黙る。
  近傍照合という一番新しい層が、他所では**最初から効いていなかった**
- **証拠(evidence)が空でも案内が無い** ── 合否が【不明】のまま放置される
- **雛形が合否と門を案内していない** ── 他所のAIがCLAUDE.mdを読んでも、その2つを知らない

直したうえで、まっさらな場所で再度確認した(自己検査48件緑 / 門が仕込んだ死にコードを自力で発見)。

★規律に2条追加: **46. 注意書きは検査ではない** / **47. 自分が回した測定にも計器の規律を当てる**

★教訓: **「移植できる」も、実際に移植して確かめるまでは主張であって事実ではない。**
  これは塊自身が説く「無音を成功と読まない」の、塊自身への適用だった。

## 7.4 (2026-08-22) ── 危険駆動の実例を1つ埋めた

- 塊そのものの変更は無し。**危険駆動(RULES.md 43条)で置いた検査の実例**が1件増えた:
  「利用者の表は【誰のデータか】を名乗る口からしか触らない」(onlyIn・max=0)
- 事故を待っていては永久に埋まらない領域(絞り込みが抜けても**例外が出ない**ので、
  事故として現れず WHY.md に1行が書かれない)に、先に検査を置いた例

## 7.3 (2026-08-22) ── いま有効な決定と、履歴を分ける

- **`perSection` を新設**: 節ごとに決めた1行があること(決定記録に**状態**を必須にする用)
- **`citeLive` を新設**: **もう現行でない決定を、いまの規則として引いていないか**。
  履歴として引くのは正当なので、行末の `guardian:ok` で通せる(理由を書くのが通行料)
- どちらも見本で毎回試す(自己検査 48件)

★この2つは、規範文書の**絶対規則**が1か月以上前に改定された決定を語り続けていた事故から。
決定記録は履歴なので古い節が残るのは正しい ── 問題は、古い節と現在の節が**同じ見た目で並ぶ**こと。

## 7.2 (2026-08-22) ── 完了の判定を、AIの外へ移した

- **`verdict.mjs`(合否)を新設**。宣言(`guardian.config.json` の `evidence`)に並べた証拠を集め、
  **通過 / 差戻 / 注意 / 不明** の4語に落とす。**判定は1つも持たない**(理屈は各証拠の出口コード)
  ── ★**不明を緑に数えない**(出口 差戻=1 / 不明=2 / 通過=0)
- **`hooks/stop.js`(Stop フック)を新設**。「できました」と言い終える**手前**で合否を回し、
  **差戻のときだけ止めて理由を返す**。不明・注意は止めないが**必ず本人に返す**
  (実装が変わっていなければ回さない / 二度は止めない / 失敗しても黙って通す)
- **夜間CIを合否1本に寄せた**。証拠の一覧が「CIの yaml」と「宣言」の2枚に割れていたのを1枚に(39条)
- `--fast` で回さなかった証拠は**名前を出す** ── 「速い方だけ回して全部通った」が
  「全部通った」に化けるのを防ぐ
- 道具の有無は**出口コードで**確かめる(`needsCmd`)。文言で当てる実装は、
  日本語Windowsで丸ごと外れた(実測)

## 7.1 (2026-08-22) ── 事故が【どの層まで届いたか】を数える

- **WHY.md の全事故に札**を付けた: `[到達/由来]`。到達=検査/門/計器/**規律(=文章にしかない)**、由来=事故/予防
- **selfcheck が数える**: 札の無い事故があれば赤 / **守り(門か検査)の件数は下げない**
  (`selfcheck.mjs --tighten` で下限を上げる。check.mjs の `max` と同じラチェットで向きだけ逆)
  ── 初回の実測は **事故69件・守り25・計器15・文章だけ29**
- 件数の照合を書き分けた: **総数は「全N条」「全N件」**(実数と一致)/ **参照は「N条」**(実在する条番号か)
  ── 「RULES.md 39条」が総数の主張と読まれて誤検出になった(誤検出は検査ごと外される)
- RULES.md 43条「事故を待てるものと、待てないもの」(事故駆動と危険駆動)
- RULES.md 44条「門には必ず双子を置く」── 門は黙って死ねるので、同じ宣言を読む検査が要る
- WHY.md の「配るときの約束」を落とした(README と重複していた・39条)

## 7.0 (2026-08-21) ── 塊が塊を検査するようになった

**この版から `selfcheck.mjs` が同梱されます。** 塊を配る前に必ず回してください。

- **`selfcheck.mjs` を新設**(塊の自己検査・34件)。見本の小さなリポジトリを一時領域に建て、
  **1箇所ずつわざと壊して**「壊したら赤 / 壊さなければ緑」を確かめる。
  ＋塊そのものの健康診断(版が嘘をついていないか / 一度も試されていない検査の種類が無いか /
  エンジンに現場固有が混じっていないか / 文書が書いた件数が実数と合っているか)
- **`callArgs` を新設**(【既定に任せてはいけない引数】を数える)。
  ★これに伴い、エンジンに直接書かれていた**現場固有の検査を撤去**した
  ── 「`check.mjs` はどのプロジェクトでも同じ中身」がやっと本当になった
- **`check.mjs --tighten`**: 口が減っていれば宣言の `max` を実数まで機械が書き下げる
  (一方通行を人の手作業にしない)
- `noInline`: 見張るはずの語が実装から消えていたら【緑にしない】(永久に0件=緑を返していた穴)
- RULES.md に42条「計器そのものを、計器で確かめる」/ WHY.md に事故を4件追加

## 6.1 (2026-08-20)

- **METHOD.md を新設**(まとめの入口)。弱点6つ・対策4層(門/検査/計器/規律)・情報/作法/器の分け方・人間側の一言
- RULES.md に7条追加(13〜19): 相手の答えを捨てない / 症状を抑えたら原因を問う /
  合格の判定は実際の使い方に合わせる / 情報が足りないときは器で埋めない /
  器は種類の名前すら持たない / 未確認と失敗を区別する / 指示は形と説明を混ぜない
- WHY.md に事故を7件追加(2026-08-20)。合計29件
- hooks/no-fixed-names.js(固有名の門)を収録 ── 禁止語は検査の宣言から読む
- check.mjs: kind ごとの必須の欄が無い検査は【赤】にする(何も見ていない検査を許さない)

配った先の `KIT_VERSION` と見比べて、**どのコピーが古いか**を判断するための記録です。
上げ忘れると、この番号は嘘をつきます(**実際に v4 を上げ忘れました**・下記)。

## v6 — 2026-08-17
- **監査の呼び出し口を同梱**。`templates/guardian-audit-command.md` を `install.mjs` が
  `.claude/commands/guardian-audit.md` に置く → **誰でも `/guardian-audit` の1語で回せる**
  (無人の定期実行はアカウント側にしか置けないが、**呼び出し口はリポジトリに置ける**)

## v5 — 2026-08-17
- **夜間の見張りを同梱**。`templates/nightly-check.yml` を `install.mjs` が
  `.github/workflows/guardian-nightly.yml` に置く。**毎晩01:00・赤ければIssue・緑で閉じる**
- README に「夜中は2段」を明記(①機械検査=付いてくる / ②観点を選んだ監査=付いてこない)
- ⚠ **この版から、コピー先も入れ直しが要ります**(上書きコピー → `install.mjs` を再実行。冪等)

## v4 — 2026-08-17(番号を上げ忘れた版)
- `install.mjs` / `install.md` を追加。**フォルダを置いてAIに一言**で導入が済むように
- 検査エンジンの2つの穴を修正:
  - 宣言した検査が値を拾えなくても黙って通っていた → **落とす**(何も見ていない検査は無い検査より悪い)
  - 地図の雛形の例示を実装への主張と読み、初日に嘘の指摘が出ていた → 囲みコードを拾わない

## v3 — 2026-08-16
- 塊として切り出し。エンジン(記号実在 / same・sameMap・sameSet・shape / noInline / セレクタ)、
  フック2本(置き場所に依存しない)、WHY.md・RULES.md・audit.md・templates を同梱
