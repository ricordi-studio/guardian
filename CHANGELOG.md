# 塊の版

## 9.28 (2026-08-30) ── 新規プロジェクトが【導入した瞬間から測れる】ようにした

配布先との会話から。「evidence 8件の決め方は、はっきり2層に割れている」──
**塊の4件**(どの現場でも同じ)と、**現場の4件**(その現場しか知らない)。

- **塊の4件を install が自動で書く**(selfcheck / index --check / check / neighbors --gate)。
  空のままにする理由が無い ── **空だと合否は【不明】のまま=まだ稼働していない**
- **現場の分は勝手に埋めない**(埋めたら「測っていないのに緑」)。代わりに `package.json` の scripts /
  `Makefile` / CI の run 行から**候補を出すだけ**にして、選ぶのは人。
  配布先の言葉をそのまま案内に入れた: **並べ方の軸は【一段下が測れないもの】**
  (型は記号を測れるが振る舞いは測れない / 単体は振る舞いを測れるが外との往復は測れない / e2e がそこを埋める)。
  相手を起こすもの(e2e)は `fast:false` にして CI の速い回では外す
- ★**verdict が出口2を【差戻】にしていた** ── `neighbors.mjs` の頭には
  「出口 2=不明。合否は 2 を不明として扱い、緑に数えない」と**書いてある**のに実装していなかった。
  実測: git の履歴がまだ無い新規プロジェクトで、門が正しく不明を返したのに合否は**差戻**と言った
  ── 「測れていない」が「違反を機械で示せた」に化けていた。**宣言と実装の食い違い**を直した
- ★**selfcheck の B7 が【作る】と【読む】を混ぜていた** ── install が候補を拾うために
  package.json を**読む**ようにした途端、それが「配布先に作るもの」として数えられ赤くなった。
  読む行は `guardian:read` で**向きを宣言**する(逃げ道ではない)

## 9.27 (2026-08-30) ── degit で入れたときの混入を、名指しできるようにした

HANDOVER の⑤(既知・未解決)。`npx degit` / `git clone` は Guardian の宣言を読まないので、
正本のリポジトリを**丸ごと** `guardian/` に落とす。実測(新規プロジェクトで実走): **6項目が混入**
(`.claude` `CLAUDE.md` `docs` `guardian.config.json` `research` `talk`)。

- 実害は 9.13 の守り(塊のフォルダを根と見なさない)で消えている ── **止めない**
- だが黙っていると「そういうものだ」と思われ、次の取り直しで `pull` が止まる形にもなる
- → `install` が **pull.mjs の宣言を読んで**混入を名指しし、**消す1行を渡す**(勝手に消さない)
- 実測: 名指し → 案内どおり消す → 静かになる。検査は前後とも動く

## 9.26 (2026-08-30) ── 履歴の印を、理由を書き足しても効く形にした

配布先からの一言(会話の場を作った初日のやり取り)。**印を完全一致で探していた**ので、
`<!-- guardian:history ここから下は履歴 -->` のように理由を書き足した瞬間に効かなくなっていた。
**人は必ず理由を書き足す。**
→ 前方一致(`<!--s*guardian:history`)にした。素の形も理由つきも、両方で実測。

★あわせて配布先の言葉を記録: 「**赤だと逃げ道を撒きたくなるけど、未測なら撒く気にならない**」。
9.25 で「多すぎたら文書ごと未測」にしたのは、**撒かせないための形**として効いていた。

## 9.25 (2026-08-30) ── 却下した案より、作り直した自分の網の方が騒がしかった

9.24 で外の文書へ掛けた網が、配布先で **58件鳴って本物は1件**だった(残り57件は3316行の
`STATUS.md` に積まれた過去のセッションの完了報告 ── どれも当時は正しく、腐りようがない)。

★**9.24 を作った私の落ち度。** 報告者の案を「自分の地図で13箇所鳴った」と却下しておいて、
作り直した網は配布先で **57/58 が騒音**だった ── **却下した案より悪い**。
根は**自分の地図でしか測らなかった**こと。「配布の結果は配布先を建てて測る」を、
その規律を書いた版(9.23)の次の版で破っている。

- **`<!-- guardian:history -->` の1行**で、文書が【どこから下は履歴か】を宣言できるようにした。
  その下は数を照合しない(落とした行数は言う)。報告者が「構造の芽②」で求めていたもので、
  ADR の `状態:` の札と同じ考え ── **索引を全部機械照合するのとは違う**
- **印が無くても止まらない**ようにした。1つの文書で上限を超えて鳴いたら、それは「腐り」ではなく
  「**数を語る文書**」なので、その文書は **未測** にして落とさない。見本を3つ出し、印の置き方を案内する。
  **1件の本物のために57件の騒音で止めるのは、計器として壊れている**(7条)
- 実測(履歴の積まれた STATUS を建てて): 印なしで **56件 → 文書ごと未測**(止まらない)/
  印を1行置くと **220行を飛ばして、いまの主張2件だけ残る**。
  配布先が見つけた本物(`CLAUDE.md` が書いている WHY の総数)は**印を置いても赤いまま**

★配布先へ: **履歴を切り出す必要も、53箇所に逃げ道を撒く必要もありません**(後者は報告者自身が
「門が儀式になりかけた事故と同じ形」と見抜いたとおり)。**1行の印か、何もしないか**のどちらかで済みます。

## 9.24 (2026-08-30) ── 数を照合する検査が、その文書を一度も開いていなかった

配布先からの報告③(9.13 → 9.23 の取り直しで見つけてくれたもの)。**4件とも再現してから直した。**
★1件は**報告者の提案どおりに作ったら当たらなかった**ので、当たる形に作り直した。

- **塊の外の3文書(`CLAUDE.md` / `STATUS.md` / `docs/CODEMAP.md`)を一度も読んでいなかった**。
  経路が `HERE/../../<文書>` ── **1つ深く、プロジェクトの外**を指していた。
  しかも `catch (_) { 無ければ見ない }` で握り潰していたので、**読めていないことが出なかった**。
  配布先で数が6件腐っていたのに1件も鳴らなかったのは、これが理由
  → `ROOT_DIR` から読む(段数を数えない)。読めなければ**名前を出す**。
    裸の実数の網も、外の3文書に掛けていなかったので掛けた
- **「全 が付かない数は全部『照合していない』と言う」は当たらなかった** ──
  この塊の地図で **13箇所鳴って、13箇所とも騒音**(「1本も入っていない」「39条」など)。
  索引には**いまの主張**と**過去の記録**が同居するので、**数の有無では割れない**
  → 割れるのは【名詞】。数えられる部品(**証拠 / 要件(R番号) / フックの本数**)を網に足した。
    実測: 騒音 **0**、報告の3つ(`証拠6件` / `3本を登録` / `R1〜R12`)は**全部捕まる**
- **写経の疑いが、変数と関数を同じ列で数えていた** ── `function 名札()` と `let 名札 = null` が並んだ
  → 「どれか1つが関数なら」を「**関数が2つ以上**」に。実測: 誤検出だけ消え、本物は残る
- **大域スコープの現場が見えなかった** ── GAS(全ファイルが同じ大域)や HTML の `<script>` から
  呼ぶ古典スクリプトでは、export の橋が無くても届く
  → `neighbors.global_scope` を宣言できるようにした(書かなければ何も変わらない)。
    実測: 呼ばれている記号が死にコード候補から消え、**本当に呼ばれていないものは残った**

★**判断待ち(直していない)**: 報告の「構造の芽②」── 地図に**状態の札が無い**ので、
現在の記述と履歴の記述が同じ見た目で並ぶ(ADR は `状態:` の札で解いた問題の兄弟)。
索引を全部機械照合するのは筋が違うという合意があるので、**そちらの判断を待つ**。

## 9.23 (2026-08-30) ── 配ると宣言したものが、名前の衝突で配られていなかった

★依頼主の問い(「pull させて install させたらいい?」)に**推測で答えないために 9.13 の現場を建てて
実際に上げたら、出口の無い部屋が見つかった**。9.22 で「出口の無い部屋を作るな」と書いた版が、
その部屋を作っていた。

- **`pull.mjs` の配布一覧が、場所ではなく【名前】で弾いていた**。
  `現場のもの.has(e.name)` を**どの深さでも**当てるので、`guardian.config.json`(その現場の宣言)と
  名前が同じ **`templates/guardian.config.json` まで弾かれる**。
  ところが指紋の対象(`ENGINE_FILES`)は【配るもの】から導くので templates/ の中は入っている ──
  **配らないのに、配ったことにして指紋を照合していた**(9.16 以降ずっと)。
  実測: 9.13 の現場を本物の正本から取り直すと、直後に
  「✗ この塊は配られたときの中身と違います(templates/guardian.config.json)」。**1文字も直していないのに。**
  さらに 9.21・9.22 の守りが噛み合って**出口が無い**:
  `pull` は「この現場で塊を直しています」で拒否、`--stamp` は「ここは配布先です」で拒否
  → 直下は【配ると決めたもの】だけを歩く。深い所で名前を見て弾かない。
    どこに在っても要らないもの(`.git` / `node_modules` / `.guardian` / 作業場)だけ深さに関係なく落とす
- **検査を足した**(`selfcheck` B7b): 配ると宣言したものが**本当に配られるか**。
  ★最初は検査の側に歩き方を書き写したが、**写した方は正しいままなので門の退行を測れなかった**
  (直す前の形に戻しても緑だった)。**当たらない検査は、この塊が生まれた事故そのもの**。
  → `pull.mjs --distributed`(新設)で**門自身に言わせ**、指紋の対象と突き合わせる(44条の双子)。
    直す前の形に戻すと赤くなることを確認した
- `pull.mjs` の分類表の切り出しが `selfcheck` の中に**3箇所写されていた**ので、`分類表を取る` 1つに集めた(39条)

- **配布先でエンジンを1本消しても、緑を返していた** ── 指紋の対象はディスクを readdir して作るので、
  **消したファイルは最初から一覧に居ない**。照合にも掛からず、件数が静かに1つ減るだけだった。
  実測: 配布先で `hooks/stop.js`(完了を名乗る手前で止める層)を消しても
  「✓ エンジンは配られたときの中身のまま(25件)」。**層が1枚消えているのに、消えたと言えない**
  → 記録(ENGINE_FP)は「配られたとき何が在ったか」を知っている。**両方向で見る**。
    `--changed` にも載せるので、`pull` が「手元に無い」として取り込み直せる
- **`pull --force` を置いた** ── 自分自身を更新する道具は**更新の1回目には古い自分で走る**ので、
  上の配り漏らしは「1回目の pull では直らない」。2回目の pull は正しく気づくが、
  そのままでは「この現場の直り」と読んで拒否する ── **出口が要る**。
  何が消えるかを先に出し、下見(`--check`)を対で持つ。手元に無いものは直り扱いにしない
- **実測(9.13 の現場を建てて 9.23 まで通した)**: pull → (配り漏らし1本で赤) →
  その1本を消して pull / または `--force` → 緑。ゴミ(`.gitattributes`)の混入も消えた

★あわせて分かったこと(直していない・記録):
**9.13 の `pull.mjs` は最終行で「次にやること: `node guardian/install.mjs`」と案内する。**
だから配布先で「pull だけ」と指示しても、AI はその案内を読んで install まで回す。
**道具の案内文は、人の指示を上書きする。**

## 9.22 (2026-08-30) ── 配布先からの報告②(3件とも再現して直した)

現場A の現場(9.8 → 9.13 に取り直し)からの報告。**鵜呑みにせず、こちらで再現してから直した。**
★1件は**報告者の見立てと軸が違った** ── 測ったら、名前ではなく「何を指しているか」だった。

- **`install.mjs` が「地図が空です」と、何も見ずに毎回言っていた** ── 条件が1つも無かった。
  配布先は **1239行の地図**があるのに毎回そう言われる。**計器が嘘をついている**形で、
  一度そう学ばれると**本当に空のときも読み飛ばされる**
  → 雛形に無い見出しの数で判定。**在るときは「N項あるので案内は出しません」と言う**
- **`install.mjs` が CI を二重に置いた** ── 冪等の判定が `${KIT}/check.mjs` という1本の綴り頼りだった。
  ★報告者の案(古い名前の一覧に足す)は名前の軸。**測ったら軸は「何を指しているか」**で、
  5つの現実的な形のうち**3つで二重置き**(改名前のパス / verdict だけの手直し版 / npm script)
  → 塊の道具(check / verdict / selfcheck / neighbors / index)を指していれば気づく。
    **それでも当たらない npm script は、置いたうえで「既に N本ある」と名指しする**
- **守りが「こちらで直した」と「正本が進んだ」を区別できていなかった**(報告者が判断を仰いだ点)
  ── 指紋が食い違えばどちらも同じ扱いで止まり、報告者は**堂々巡り**に入った
  (手で新しくする → 止まる → 指紋を手で入れる → 別のファイルが違うと言われる → git で丸ごと戻す)。
  ★材料は手元にあった ── **正本を一時領域に取っているのだから、突き合わせればよい**(報告者の指摘)
  → 実測: 指紋だけ古く中身は正本と同じ → **通る** / 本当に直した → 止まる(出口を2つ示す) /
    混ざっている → 古い分は名指しで通し、直りの分だけで止まる
  → どのファイルが食い違ったかは **`selfcheck --changed`**(新設)から受け取る。
    ★直す前は赤い文の**文言**を includes で見ていた ── **文言は変わる**(9.21 で実際に変えた)

## 9.21 (2026-08-30) ── 見るためには、やるしかなかった(この塊自身が起こした事故)

★**開発中に、私(AI)が実際に踏んだ事故から始まっている。**
`--send` の**表示だけ**を確かめるつもりで `--why --send` を回したら、
その機械には `gh` が入っていて認証も通っていたので、**本物の issue が公開リポジトリに立った**
(`ricordi-studio/guardian#2`。試験用の作り話だったが、公開されたことに変わりはない。許可を得て閉じた)。

- **根は「見るには、やるしかなかった」こと** ── `--send` に下見の口が無く、
  何がどこへ出るのかを知る方法が、実際に出す以外に存在しなかった。
  ★**`--stamp` とまったく同じ形**(9.17 で直したばかり)。**同じ日に、同じ形で二度目を踏んだ**
  → `--send --dry` を足した(宛先・題・バイト数・件数・見張った語数を出して**送らない**)。
    送る場合も同じ一行を必ず先に出す。案内文の既定も `--send --dry` にした
- **`--send` は「個人情報を見張っていない現場」からでも送れた** ── `private` が空だと
  B1c(見張り)は何もせず、その結果は B1e(送る)から**見えなかった**(局所変数だった)。
  しかも報告書の冒頭に「宣言が空のため見張っていません」と**自分で書いたうえで**送る
  → 見張りの結果を外へ出し、**見張っていない / 引っかかっている / 送り先が読めない**なら送らない。
  ★「足した事故に人名が混ざっている」場合は**既存の守りが効いていた**(穴はこの1つだけ)
- **正本のアドレスが2箇所にあった** ── `pull.mjs` の 正本(取り直し先)と `selfcheck.mjs` の
  べた書き(報告先)。引っ越したら**報告だけが古い方へ飛び続ける**
  → `pull.mjs` から読む。読めなければ送らない(推測で外へ出さない)

- **打ち切りを黙っていたので、道具が逆の助言をした** ── `--escaped` の環の展開は
  `ring <= 12` で**無言**に止まり、超えた犯人は `∞(環を増やしても捕まらない)` に化けて
  「この軸の外」に数えられ、「**環を増やすより**別の計器を疑え」と出ていた。**逆である**。
  実測(対照つき): 同じ事故を上限12→「環3・rings を上げる材料」/ 上限1→「∞・逆の助言」。
  ★「実測でここに達したことは無い」と書いてあったが、**達しても何も言わない**ので確かめようがなかった
  → 打ち切ったら言う。`打ち切り(N環まで見て届かなかった ── 測れていない)` と出し、
    逆向きの助言を止め、台帳にも記録する

- **`RULES.md` が指紋の対象外だった** ── 理由は「配布先で事故が増えて当然」だが、それは `WHY.md` の話。
  作法は配布先で勝手に育たない。外したままだと配布先が作法を書き換えても緑で、**還る道が無い**
  → 対象へ戻した(25件 → 26件)。実測: 配布先で1条足す → 赤になり、還す1枚にも載り、`pull` も守った

- `pull.mjs` の `spawnSync(cmd, args, {shell:true})`(**Node が DEP0190 で非推奨**)を引数の配列へ。
  括る作法(`JSON.stringify`)ごと要らなくなった ── 括り忘れる場所を消す

## 9.20 (2026-08-30) ── 余分な CR 1つが、git のテキスト判定を裏返していた

★**揃えるだけの変更**。改行を除いた中身が同一であることを機械で確かめてから出している。

- `neighbors.mjs` の230行目の末尾が `\r\r\n` だった。**CR が在るとバイナリ扱い**(正規化しない)、
  **消すとテキスト扱い**になり `core.autocrlf` が全行を書き換え扱いにする ──
  実測: 104行の変更が **1384行の書き換え**に見えた。
  エンジン15本のうち CRLF はこの1本だけで、**この CR のせいで正規化から外れ続けていた**
- ★**門は git の差分を読む。** 差分が全行に膨らむと全記号が「触れた」になり、
  近傍は `known` に吸われて **0件**になる ── **差分が膨らむと門は鳴らなくなる**
- → `.gitattributes`(`* text=auto eol=lf`)でリポジトリが流儀を決める。機械任せにしない。
  `neighbors.mjs` を LF に揃えた(残り14本は元から LF)
- → `.gitattributes` は**配らない**(`pull.mjs` の `現場のもの`)── 指紋は改行を正規化して
  照合するので配布先には要らず、配ると**他人のリポジトリの guardian/ 以下の扱いを黙って変える**

## 9.19 (2026-08-30) ── クラスで書かれた現場では、門が丸ごと盲目だった

9.18 の作業中に気づいた1件(引き継ぎの⑧)。**実測しないと一生出ない条件が、これで4件目。**

- **`class Foo {}` が定義として拾われていなかった** ── 拾うのは `function` と `const` / `let` だけ。
  クラスの中身は字下げされているのでメソッドも拾えず、**そのファイルは丸ごと持ち主なし**。
  実測: `class Cart` の `total()` から**税の掛け算を落とす**(本物の破壊)→
  **「触れた記号: (器のコードに変更なし)」・近傍なし・無言**。
  直したら `触れた記号: Cart` / `[環1] Checkout ← 「Cart」を呼んでいる`
  ★**JS/TS の現場の多くはクラスで書かれている。そこでは門が最初から最後まで鳴らない**
  → `DEF_CLASS` を足し、`export default` / `var` / 生成器 `function*` の取りこぼしも直した
- **持ち主の記号が引けなかった行を、黙って捨てていた** ── 最初の定義より前(import・冒頭の定数・
  HTML の `<style>`)も同じ穴。問えないのは軸の限界だが、**言わなければ「見た」と同じに見える**
  → 「持ち主の記号が引けなかった変更 N行は**問えていません**」と出す。
  ★見た目でコメント行を落とすのは**この報せだけ** ── 囲みコメントの途中は行だけ見ても
  `const x = a` / `  * b;` のような続きの式と区別がつかない。**門で落とすと見えない失敗になる**
  (実測: この塊自身で 55行 → 4行)
- 検査を足した(`selfcheck` B8c): 見本を建てて**実際に門を回す**。
  ★`class` を拾わない形に戻すと赤くなることも確認済み。git が無い機械では【未測】

## 9.18 (2026-08-30) ── 物差しが【ASCII と自分の現場】を前提にしていた

9.17 の続き。残していた根A・根C・根Dを実装した。**どれも「測ったつもり」の形**である。
掘っている最中に、**いちばん重いもの**がもう1件出た(下の★)。

**根A ── 「測れなかった」を緑に数えていた**
- `selfcheck` に **ok / ng の2語しか無く**、「見ていません」を ok に混ぜていた。
  まっさらな導入先が **「59件すべて期待どおり」** ── 全部宣言済みの正本と**同じ数字・同じ文**。
  ★この自己検査は `verdict` の**証拠①**である。
  → 第3の語彙【未測】。止めないが**数を分ける**。合否で拾いたい現場は
    evidence に `"unknownIf": "未測 [1-9]"` を足せば【不明】になる
- `skip_dirs` で外した場所は門でも定義が引けず、編集しても「器のコードに変更なし」。**しかも無言**
  → 「宣言 skip_dirs で外した場所の変更 N件は**見ていません**」と言う(--list / --gate の両方)
  ★地図には「門には効かない」と**逆**が書いてあった(9.17 で地図を実測に合わせ、今回は実装を直した)
- `測れなかった` が `--escaped` から二度と見られなかった → そこでも見る

**根C ── ASCII と自分の現場を前提にした物差し**
- `length >= 2` / 写経の `>= 4` は**ラテン文字の物差し**。日本語では1文字が1語。
  実測: `const 仮`(= `rmSync(recursive, force)` が消す先)の行を書き換えたら、
  触れた記号は **`走る`**(3つ前の無関係な関数)。直したら `仮` になり、読む9件が近傍に出た
- `#` を無条件にコメント扱いしていた。**既定の `ext` には html が入っている**。
  実測(対照つき): `.html` の `#main .row` → `#side`(直下の `querySelector(" #main")` が壊れる)は
  **「コメントと空白だけの変更」**と報告されていた ── 黙るのではなく**嘘を言っていた**。
  → `#` を落とすのは sh/py/rb/yml… のときだけ。知らない拡張子では落とさない(鳴る側へ倒す)
- ノートの欄名が `/g` 無しの **1行1件**・ASCII・3文字以上で、一般語12語も**エンジンにべた書き**。
  実測: 欄3つを全部変えて **1行JSON 0/3 / 整形済み 1/3** → **3/3**。
  → 全件拾う / 日本語を許す / 一般語は `neighbors.common_keys` で差し替え可能に(39条)

**根D ── 自分が作ったものを、次の回の根拠にしていた**
- `install` の「Claude Code の現場か」の判定材料に `CLAUDE.md` が入っていたが、
  **その `CLAUDE.md` は同じ install が下で作る**。実測: `.claude` の無い現場で
  1回目「仕掛けが見当たらないのでフックは入れていません」→ **何も変えずに2回目「フックを4本足しました」**。
  → マーカー区間を外して**何か残るときだけ**「人が書いた文書」と見なす。5経路とも実測

**★掘っている最中に出た、いちばん重いもの**
- **経路に空白があると、git は差分の見出しの末尾にタブを足す**(`+++ b/src/my file.js<TAB>`)。
  落とさないと拡張子の判定が外れ、**そのファイルは門から丸ごと消える**。
  `--escaped` の話ではなく、**日常の門でも、空白入りのファイルは一度も見られていなかった。**
  ★「空白入りの経路」で壊れるのは**3度目**(install.mjs の冒頭 / --escaped / ここ)
- あわせて `neighbors.mjs` の git 呼び出しを**シェル経由から引数の配列へ**(括り忘れる場所を消した)

## 9.17 (2026-08-30) ── 記録に【鮮度】が無く、門も指紋も一言で無効化できた

9.16 で掘り残した違和感17件に段2を当てた。**14件が実在**(前回 11/12。20〜25%の帯からも出た)。
うち3件が同じ根を指していた ── **この塊が作る記録は「在るか」しか見ておらず、「いつのものか」を持たない。**
直したのはその根(根B)だけ。残り(根A・C・D と単独5件)は `docs/HANDOVER.md` に実測つきで置いた。

- **門が、別の変更の古い回答で通った** ── `--gate` は近傍の顔ぶれだけで照合していた。
  回答ファイルは `range` を**記録しているのに、照合していなかった**(そして `range` は
  作業木が汚れていれば常に `"HEAD"` なので、そもそも別物を区別できない)。
  実測: 無害な変更に「影響なし」と答えて通したあと、**同じ関数から冪等の守りを削除**しても
  `--list` 無しの `--gate` が「通過(2件すべてに回答あり)」。**回答はコミットに含めるので、次の回も引き継ぐ。**
  → 回答に【根の本体の指紋】を持たせた。根が変わればそこから生えた回答だけが失効する。
    **無関係な直しでは失効しない**ことも実測(鳴り過ぎる門は読み飛ばされる・7条)
- **`--stamp` 一言で、配布先の直りが記録ごと消せた** ── `pull.mjs` は「配られたときの中身と違います」
  という**文字列**で守っている。`--stamp` は確認も一覧も無くその文字列を消す。
  しかも `--stamp` は**配布先の人が読む赤い文の中で案内されていた**
  (「意図した変更なら」── バグを直した現場は、それをしている)。
  → 塊が単独のリポジトリか(=正本か)で分けた。配布先では止め、案内からも消す。
    正本では**何を押すのかを先に一覧で出してから**押す。還す道(`--report`)はそのまま
- **人が理由を書くファイルを、機械が黙って全消しできた** ── `--list` が `JSON.parse` の失敗を
  **空の catch** で握り潰し、下書きで上書きしていた。実測: **末尾に `,` を1文字**足すだけで全部消えた。
  → 読めなければ上書きせずに止める。近傍から外れる回答は名前を出してから落とす
- 地図の誤りを1つ直した: `skip_dirs` は「門(--gate)には効かない」と書いてあったが、
  実測では**門もそこで黙る**(しかも無言)。地図を実測に合わせた(振る舞い自体は判断待ち)
- ★記録(直していない): `selfcheck` の B6a が使う `正本か` は `HERE/../.git` も見るので
  **配布先でも真**になり、「ここは正本ではないので見ていません」の枝は通常の置き方では**一度も通らない**

## 9.16 (2026-08-30) ── 違和感が、自分のバグを11件出した

依頼主「今日入れた仕組みは価値があったの?」── **使っていなかった。**
hunch.md を作った日の監査で、その2段を一度も走らせていなかった。
そこで Guardian 自身のエンジン4本に当てたら、**36秒で違和感30件。12件を掘って11件が実在**した。
**59件の自己検査も、直前の監査(4観点)も、1件も見つけていない。**

- **根A 「測れなかった」が出口0(通過)だった** ── `sh()` の三項が両辺同じで git の失敗を握り潰し、
  CI の浅いクローンでは**門が作った日から死んでいた**。宣言なし・コーパス0も同じく通過していた。
  しかも合否は「✓ 通過 / 測れた割合 100%」と出していた ── **測っていないのに。**
  → 出口2(不明)を新設。4条件とも 通過→不明 に変わることを実測
- **根B 配布物の集合が2箇所にあり、片方が短かった** ── 指紋12件 / 実際に配るのは23項目。
  `index.mjs` / `githooks` / `templates` / 文書は配られるのに指紋が無く、**取り直しで現場の直りが消えた**。
  → ENGINE_FILES を【配るもの】から導く(39条)。12件 → 25件。
  → 上流から消えたものを配布先から撤去する道も無かったので、足した
- **根C 形頼みの拾い方が、当たらなくても緑** ── **selfcheck が生まれた事故そのもの**。
  → 見本(probe)を添え、拾えなければ照合自身を落とす
- ★掘っている最中にもう1件: `inDirs` が `code:["."]` で直下のファイルを拾っておらず、
  **環の測定(--escaped)が作った日から一度も動いていなかった**
- 他: 一時フォルダが塊の外(正本では**デスクトップ直下**)で先に rm -rf していた /
  報告書が cwd に落ち**自分の自己検査を赤くした** / 台帳に重複判定が無く誤った助言を出した /
  分類表が古いままで**取り直しを拒む自己ロック** / CLAUDE.md の追記が KIT 依存で**毎回増えた**

★11件のうち少なくとも4件が、**この塊自身が WHY に書いた規律の違反**だった
(文字列で判定するな / 当たらない検査を緑にするな / 同じことを2か所に書くな)。
★規律: **道具を作った日に、その道具を自分に当てる。**使っていない道具の価値は、測っていない。

## 9.15 (2026-08-30) ── アクセルを1つ置いた(Guardian で初めて、止めない機構)

依頼主「事故例が集まれば自然と性能が上がると思っていたが、今は、**人の脳をどうシミュレートするかを
悩み続けること**にしか発展は無い気がしてきた」── 方針の転換。

実測が裏づけた: **194件集めた状態で、同じ欠陥が1セッション中に4回出た**(4回とも人が先に正解に着いた)。
事故ごとに門が1本増えるだけで、**門は判断に合成されない**。

- **hunch.md を新設**(作法。器ではない)── 2段:
  段1 **理由を書かせずに**違和感を20件(10秒)/ 段2 出た**全部**にサーチライトを当てて掘る(4分)
  ★実測: 変更した本人は6つの壊れのうち**5つの現場に手を入れて1件しか指摘しなかった**(1/6)。
    同じコードに「一から書くなら?」と問うた別の1体は **6/6**
  ★**違和感は「主張」ではなく「座標」** ── 外れた思いつきの隣に本物があった(実測2件)
- **宣言に `context`(運用の文脈)** を新設 + 雛形にも。**ここが空だと運用の欠落は永久に出ない**
  ── 問いを3通りに変えても1ミリも動かず(5回とも同じ3件を落とした)、
     文脈4行を足した1回だけが動いた(3/6 → 5〜6/6)
- **selfcheck に B9**: `context` が空なら「**運用の欠落は見ていません**」と正直に言う(止めない)。
  個人情報の見張り(B1c)と同じ形 ── 無音を安全と読ませない。両方向を実測で確認
- `audit.md` に**観点0**(観点1〜4は探すものを指定しているので、指定していない形は通り抜ける)
- SPEC に **R18**。★**他の要件が全部ブレーキ**(止める・緑にさせない・戻らせない)なのに対し、
  **これだけがアクセル** ── Guardian が3週間で作ってきたものは、全部「止まろうとする力を止める」装置だった

★**これは自発性ではない。** 引き金は必ず外(人が呼ぶか、完了の瞬間)。
  文書にそう書いた ── 承知で低い段を選んだなら、承知だと書く(RULES 46条の精神)。
★**まっさらな現場に入れて実走させたら、直したはずの所が届いていなかった** ──
  雛形に `context` を足したのに、`install.mjs` は雛形を写さず宣言を自分で組み立てていた。
  **この日の要が「文脈を宣言に書く」ことなのに、書く場所が作られず、書けとも言われなかった。**
  導入が組み立てる宣言に `context` と、**空なら促す1行**を足した(場所を作るだけでは書かれない)。
  ★あわせて実測できたこと: 9.13の守りが効き、degit が `guardian/CLAUDE.md` を持ち込んでいるのに
  導入は**プロジェクトの根**へ正しく置いた。配布先で自己検査59件すべて期待どおり。

★**保留の置き場と浮上**(「なんかね」の本体)は**まだ測っていないので作っていない。**
  測る前に作れば、この塊が何度も踏んだ「効かないものを作る」をまた踏む。

## 9.14 (2026-08-29) ── 配布境界を【白と黒の両方】にした / 別の仕事は名前を分けた

依頼主の判断2つ。どちらも「二択で詰まっていた私の見方」を外から直したもの。

**① ホワイトアンドブラック**(配布境界)

ブラックリストだけだと**新しく作ったものが既定で配られる**(1日で3回漏れた)。
反転してホワイトリストだけにすると、今度は**書き忘れたものが黙って配られない**
(FIND-001 Case G)。依頼主「両方持てばいい。配布と導入は日常の作業じゃないんだから厳重でいい」。

- `pull.mjs` が `配るもの` と `現場のもの` を**両方**持つ。
  どちらにも載っていないものが1つでもあれば**取らずに止まる**(門)
- `selfcheck.mjs` が同じ2つの宣言を読み、3方向を照合(双子・RULES 44条):
  直下が全部どちらか一方に分類済みか / エンジンが【配るもの】に入っているか(**欠落の向き**) /
  install が作るものが【現場のもの】に入っているか(**混入の向き**)
- 3つとも、わざと崩して赤くなることを確認済み
- ★これで**書き忘れが「事故」ではなく「赤」になる**。どちらの向きに忘れても止まる

**② `findRoot` → `findInstallRoot`**(名前を分けた)

監査で「同名の関数が2ファイルに(写経の疑い)」と報告したが、依頼主の指摘どおり
**これは重複ではなかった** ── 片方は導入**前**に走るので宣言を目印にできず、目印が違う以上
ひとつにできない。同じ名前にしていたことの方が間違いだった。改名し、なぜ2つ在るかを
その場に書いた。走査の誤検出が1件減った。

## 9.13 (2026-08-29) ── 混入したゴミが【根】を狂わせ、導入が嘘の成功を報告していた

依頼主「変更 → GitHub 更新 → 入れている現場が反映、という流れは問題なく動くのか?」
**動いていなかった。**模擬の配布先を建てて端から端まで実走させて分かった。

配布先(9.8)の古い `pull.mjs` は `CLAUDE.md` を【配らないもの】に持っていないので、
取り直すと `guardian/CLAUDE.md` が出来る。すると根を探す判定がそこで止まり、
`install.mjs` はフックを `guardian/.claude/settings.json` へ書いて
**「✓ .claude/settings.json にフックを 4 本足しました」と報告する** ──
配布先の本当の設定は一度も更新されない。**フックが1本も入らないまま「入りました」と言う。**

- `install.mjs` / `hooks/lib-root.js` の根の判定に守りを追加: **塊のフォルダ自身を根と見なさない**
  (塊は `check.mjs` + `selfcheck.mjs` で見分ける。正本では `.git` が同じ場所に在るのでそちらが勝つ)
- `selfcheck.mjs` に **B8** を新設: 見本を建て、混入した目印があっても根が1つ上を指すことを実測する。
  守りを外すと赤くなることを確認済み
- ★**9.8 は2種類ある**ことも分かった(除外リストを足したとき版を上げなかった)。
  同じ番号で中身が違う配布物が世に出ていた ── 版は嘘をつかないときだけ役に立つ

★教訓: **「動くか」は、流れの端から端まで実際に走らせるまで答えない。**
道具が「次にやること」を案内しているなら、その次も実行して確かめる。

## 9.12 (2026-08-29) ── 配布境界の列挙に、独立した根拠を与えた(配布先からの報告)

配布先が 9.11 へ取り直そうとして `--check` で気づき、取らずに報告してきた ──
【配らないもの】に `.claude/` と `CLAUDE.md` が入っておらず、Guardian 自身のフック登録と
開発規範が配布先の `guardian/` へ流れる状態だった。**列挙に網羅の根拠が無かった。**

- `pull.mjs` の【配らないもの】に `.claude` と `CLAUDE.md` を追加
- `selfcheck.mjs` に **B7** を新設: `install.mjs` が配布先の ROOT に作るものは、定義上すべて
  現場固有物である ── `pull.mjs` の【配らないもの】がそれを全部含んでいるかを機械で照合する。
  わざと戻して `.claude, CLAUDE.md` が名指しで赤くなることを確認済み
- ★これは AI Council EXP-001 が Unknown として残した「独立した第二根拠は何か」への部分的な答え。
  **「配る道具」と「据え付ける道具」は、同じ境界を逆側から知っている**
- ★報告の見立ての訂正: 配布先ルートの `CLAUDE.md` は上書きされない(`pull.mjs` は `guardian/`
  配下にしか書かない)。模擬の配布先を建てて実走させ、ルート側が無傷であることを確かめた
- ★まだ閉じていない: install が作らない現場固有物(`.guardian` / `research`)はこの照合では守れない。
  ホワイトリストへの反転は保留(反転すると配り忘れが静かに起きる ── EXP-001 Round2 Case G)

## 9.11 (2026-08-29) ── Council EXP-001から、最初の検査を1本昇格

`research/council/` のCouncil EXP-001(pull.mjsの配布境界事故を3AIで検討)で見つかった
FIND-001「配布境界は宣言すれば守れるわけではない」から、実装コストが低く効果の範囲が明確な
部分だけを昇格させた。全体解ではない ── 誤分類そのものを一般的に検出する方法はまだUnknown。

- `selfcheck.mjs` にB6を新設: `ENGINE_FILES`(配布物として指紋照合しているエンジン一覧)が、
  `pull.mjs`の【配らないもの】(現場のもの)に誤って分類されていないかを検査する。
  わざと壊して(`hooks`を配らないもの側に混入させて)赤くなることを確認済み
- ついでに、指紋照合(B2)とこの新しい検査(B6)が**同じエンジン一覧を1箇所(`ENGINE_FILES`)
  から読む**ようにした(直すまでは2箇所に同じ配列が重複していた)

## 9.10 (2026-08-29) ── 複数AIで構造破壊の原因を研究する場(Council)を新設

Guardianの開発そのものを、複数のAI(Claude・ChatGPT・Gemini)の独立回答→相互批判→自己反証で
検討する場を `research/council/` に作った。AIを判定者として使わない(多数決をしない)のが核。
いまはコード0行・全部手動。将来像(orchestrator・adapter等)は `VISION.md` に分け、
「実験で必要性が確認されるまで実装しない」と明記した ── 器を情報より先に固めない、という
Guardian自身の原則をCouncil自身にも適用した。

- `research/council/`: README・VISION(将来像)・protocol(Round0/1/2・昇格の手順)・roles・
  `experiments/EXP-001`(被験体: pull.mjsが正本分離直後の混入事故をなぜ検出できなかったか)
- `pull.mjs` の除外リスト(`現場のもの`)に `research` を追加。**配布物ではない**ので配布先へ流さない
  (直近の事故 ── 正本を分けた瞬間に自分の現場ごと配ってしまった ── と同じ穴を、今回の追加で
  再現しないため)

## 9.9 (2026-08-29) ── 塊が自分自身を監査しながら開発できるようにした

正本をこのリポジトリへ分けた直後、**自動で鳴る部分が1つも入っていなかった**。
手で回せば全部動くが、フックが無い ── **監査を受けながら開発する形が半分しか実現していなかった**。

原因は2つ、どちらも**塊が自分自身の現場に居るとき**にしか出ない:

- **KIT が存在しないパスを指していた**: 塊がリポジトリそのものだと相対パスが**空文字**になる。
  空なら `guardian` に落としていたので、案内が `node guardian/check.mjs` ── **そんなパスは無い**。
  → 空なら `.` ──「その場所そのものが塊である」と正しく言う
- **導入の判定**: 塊自身の現場では `CLAUDE.md` が配布物なので判定材料にならない。
  → `KIT === "."` なら Claude Code 前提で入れる(要らなければ `--no-hooks`)

★**直した瞬間、塊自身の検査が止めた。**`install.mjs` を直したので指紋が食い違い、
  完了を名乗る手前のフックが差戻を返した ── **監査を受けながら監査を直す**形が実演された。

★さらにこの版を書いている最中、**版の照合が働いた** ── `KIT_VERSION` を 9.9 にしたのに
  CHANGELOG が 9.8 のままで、自己検査が落ちた。**版が嘘をつく前に止まった。**

## 9.8 (2026-08-29) ── Claude Code でない道具のための、git フック

依頼主「フックの機能が無いのは Codex でしたっけ? **その代案はもうあるのですか?**」
**無かった。**外部評価にも同じ指摘を受け「構想はあります」と答えていたが、1行も書いていなかった。

- **githooks/pre-commit を新設**: `git config core.hooksPath guardian/githooks` の1行で有効。
  コミットの手前で合否を回し、**差戻なら止める**(不明・注意は止めない)
- **代替できるのは3本まで**と正直に書いた。編集の直前に地図を差し込む / 応答の直前に監査を催促する
  ── **この2つは代替できない**(その瞬間が git に無い)。失われると明記
- 導入のときに**有効にする1行をその場で出す**(同梱しただけでは誰も使わない)

★実測: Claude Code の仕掛けが無い現場で、**差戻ならコミットが止まり、直せば通る**ことを確かめた。

## 9.7 (2026-08-29) ── 外部評価の指摘を全部つぶした

依頼主が文書を外部のAI2つ(Gemini / ChatGPT)に読ませ、指摘を全部対応した。

- **逃げ道(guardian:ok)の歯止め**(Gemini): 理由の無い逃げ道は落とす。数が宣言の okMax を
  超えたら落ちる(--tighten で下げるラチェット)。★入れた直後に「理由が【同上】」を1件捕まえた
- **測れた割合**(ChatGPT): 「不明1件」と「不明8件」が同じ言葉だった。割合を出し、
  下がったら「宣言が実装に追いついていない合図」と言う。★**割合は判定に使わない**(表示だけ)
- **WHY の索引**(ChatGPT + 依頼主の思想): index.mjs が**重み付き**の索引を作る。
  ★依頼主「WHYに溜まっていくのは、構造化がちゃんと出来ていないことによる結果が積みあがってるだけ。
  欲しいのは**美しい構造化の実現につながる発見・発明の芽**」── これがそのまま分類の軸。
  実測: 全183件のうち**構造の芽は21件**。残りは宿題の山である。
  ★索引が古くなったら合否が落ちる(作る道具を書いただけでは腐る)
- **SPEC に e2e の位置づけ**(ChatGPT): Guardian 自身は実行時の正しさを判定しない。
  プロジェクトが持つ test/e2e を**証拠として集めて4語に落とすだけ**

★要件は R15 まで。SPEC の検査が、足した口とファイルを全部自動で捕まえた。

## 9.6 (2026-08-29) ── 自分で禁じた「数の腐り」を、自分でやっていた(外部評価の指摘)

依頼主が文書6枚を**外部のAI2つ**に読ませて評価させた。その指摘:

> READMEでは「事故181件」、同じREADMEの後半では「既存の177件」。
> METHOD は「実数を文書へ書くな。書いた瞬間から腐る」と明記しています。
> **Guardianが自分で禁止している事故が、Guardian自身にもう起きています。**

そのとおりで、実際は**4箇所**ずれていた。

★**規範も検査も両方あったのに漏れた。**検査の網が「全N条 / 全N件」の形だけで、
  「事故181件」のような**裸の実数は素通り**していた ── **網の外は、検査が無いのと同じ**。

- 実数を文書から追い出した(6箇所)。規模は「件数は selfcheck が出す」に
- **網を広げた**: 「事故N件」「作法N条」「規律N条」も実数と突き合わせる。
  「自己検査N件」は**書くこと自体を禁じる**(実行のたびに変わるので正しい数が存在しない)
- 逃げ道は行末の guardian:ok ── **過去の版の数を語る文**は落とさない

★その過程で自分でもう1つ壊した: 「実数を追い出す」を機械的に当てて、
  ラチェットの下限(機械が読み書きする値)まで消した。検査が即座に落ちて気づけた。
  **飾りの数と、機械が使う数は、見た目が同じである。**

★**この事故は内部の監査を2回回しても出なかった。出したのは初見の外部AI。**
  配って評価してもらうことに、これだけの価値がある。

## 9.5 (2026-08-29) ── 入口を整えた(公開したので、README が最初に見られる)

依頼主「コードが格納されているフォルダの中には、Readme なり何なり、
それが何なのか? 何から始めたらいいか? を説明しているものがあるのが普通ですよね?」

あった。だが**SPEC.md を作った翌日、README がそれを案内していなかった** ──
文書を足すたび入口が遅れる、という同じ形(46条)。

- **README の冒頭を【目的別の入口】に**: 何を知りたいかで読むものが決まる表。
  ★他のAIに理解してもらうなら **SPEC.md → METHOD.md の2枚**で足りる、と明記
- **SPEC の冒頭に「これは何か」**: 単体で渡されても迷子にならないように。
  ★SPEC だけだと「規約に書けばいいのでは」と思われる ── それは最初に否定した道なので、
  METHOD へ送る1行を置いた
- **README の案内漏れを機械が見張る**: 塊に .md を足したら、README が名指しするまで赤くなる。
  ★中身の良し悪しは測れないので、**名指しされているかだけ**を数える

★役割は分けたまま: **README = 入口**(GitHub のトップに出る)/ **SPEC = 要件**(機械が見張る)。
  冒頭の1行だけ重なるが、**どちらから読み始めても迷子にならないため**の意図的な重複
  ── 39条に触れるので、承知のうえと明記する(依頼主の判断 2026-08-29)。

## 9.4 (2026-08-29) ── 仕様書と地図の役割を分けた(監査が2回連続で拾った件)

2026-08-28 と 29 の監査で、**2回連続**「塊の新しい層が地図(CODEMAP)から漏れている」が出た。
原因は、塊の説明が **SPEC.md と地図の2箇所**にあり、塊が変わるたび両方を手で直していたこと。

依頼主の判断で、こう分けた:

- **仕様は SPEC.md が正本**(何を満たすか / 満たさないか / 口の一覧)。**機械が見張る**
- **地図に書くのは【その現場でどう使っているか】だけ** ── 仕様を写さない
- ★**地図は機械に見張られない**(索引なので全部の照合は筋が違う)。
  だから**監査で拾う**と決めた ── **回さなければ漏れたまま**という弱点を承知のうえで

→ `audit.md` の観点2に足した: **配布物を使っているなら、その仕様書と地図を突き合わせる**。
  選択(監査で拾う)を実効あるものにするための1行。

★**弱点を承知で選んだことは、承知だと書く。**「監査を回さなければ漏れる」を隠して
  「対策した」と書くと、次の人はそれを守りだと思う。

## 9.3 (2026-08-28) ── **事故の記録そのものを集める道**(本体を育てる)

依頼主「事故レポートが集まるほど性能が上がるので、インストールしたプロジェクトには
レポート提出の協力をお願いしたい。もちろんそのレポートを集める方法についても。」

それまで還せたのは**塊のコードの直り**だけで、**本体(WHY.md の事故)を育てる道が無かった**。

- **WHY_SEEN**: 配られたときに何の事故があったかを記録(--stamp が一緒に押す)
- **--why**: その現場で**足した事故だけ**を1枚にする。増えた分は WHY_SEEN との差で分かる
- **--send**: 承認の表明。既定は送らず**送るコマンドを見せる**だけ
- ★**個人情報が混ざっていれば、1枚そのものを作らない** ── 作ってから気をつけろ、では遅い
- README に「なぜ協力をお願いするか」を実例つきで(空白入りフォルダのバグは、
  元の現場では一生出なかった。**現場が増えるほど踏む石の種類が増える**)

### 途中で、自分の穴が2つ出た

- **宣言を読む所が2箇所にあり、片方が【2つ上】を見たまま**だった(9.0 の引っ越しの取りこぼし)。
  読めなければ黙って空を返す作りなので、個人情報の見張りが**混ざっているのに
  「していません」と言い続けた**。→ 読み口を1つにした(39条)
- 試すときの仕込みが、**意図した節ではなく別の節に入っていた**(replace が最初の1件に当たった)。
  検査は正しく、**私の試し方が間違っていた** ── 仕込みも実測で確かめる

## 9.2 (2026-08-28) ── **公開した**。誰でも1行で入る

正本: **https://github.com/ricordi-studio/guardian**(公開)

- **入れ方が1行になった**(アカウントも認証も要らない):
  ```
  npx degit ricordi-studio/guardian guardian
  ```
- `pull.mjs`(取り直し)も**認証なしで動く** ── 実測: 認証情報を無効にした環境で確認

### 公開の前にやったこと

- 過去のコミットに残っていた個人情報を消すため、**履歴を1つに畳んだ**
- ★force push では**消えなかった**(SHAを知っていれば直接読める。実測)。
  確実に消す唯一の方法は**リポジトリの作り直し**だったので、消して作り直した
- ★**匿名で clone して、到達できる全オブジェクト31個を総なめ**し、
  人名・アカウント名・現場名が1つも無いことを確かめた
  ── 「伏せたつもり」で公開しない

★それ以前の版の記録は CHANGELOG に全部残っている(14版ぶん)。
  履歴を畳んで失うのはコミットの粒度だけで、**何をなぜ変えたかは1つも失っていない**。

## 9.1 (2026-08-28) ── 要件(SPEC.md)を置き、**遅れないことを機械が見張る**

依頼主「新しい機能が追加されたら、その要件定義書も更新されるべき。
そして**それのチェックを自分でやるところまで**。」

- **SPEC.md を新設**: 解く問題 / 満たすこと(R1〜R11)/ **満たさないこと(境界)** /
  動作の前提 / **口の一覧** / 検証済みの事実
- ★いちばん価値があるのは**満たさないこと** ── コードの正しさは見ない / 実行時のバグは見ない /
  セキュリティ検査ではない / 勝手に直さない / 列挙は判定ではない / 他の道具では自動で鳴らない。
  口頭でしか伝えていなかった境界を文書にした
- **更新しないと通れない**(注意書きではなく検査):
  口(--xxx)が SPEC に載っていなければ赤 / エンジンのファイルが名指しされていなければ赤
- ★実測: 書いた直後に**フック4本が名指しされていない**と落ちた(まとめて書いていたため)。
  架空の口 --zz-new-port を足したら**名指しで落ちる**ことも確かめた
- ★**測れない所は、測れないと書いた**: 「満たさないこと」は機械が見張れない
  ──**できることは数えられても、できないと言い切ったことは数えられない**。
  だから監査のたびに人が読み直す、と SPEC 自身に書いた

自己検査 50件 → **53件**

## 9.0 (2026-08-28) ── ルート直下へ引っ越し、どの開発ツールでも使える形に

### ① 置き場を tools/guardian → guardian へ

依頼主「名前を Guardian にしたのに、そのルートが tools なのは気になる。おかしくないですか?」
**おかしかった。**tools は「道具です」以上の情報を持たず、しかも Guardian の本体は
事故の記録(WHY.md)と規律(RULES.md)── **「道具」という括りは中身より狭い**。
8.0 で codemap → guardian を直したのと**同じ問題が、1階層上に残っていた**。

- `guardian/` … 配られる塊(コードと読み物)。**見える** ── 読んでほしいものだから
- `.guardian/` … この現場の作業記録(回答・監査時刻)。**隠れる** ── 機械が書くものだから
- `guardian.config.json` … この現場の宣言。ルート直下のまま(**人が書く**ものだから見える)

★あわせて、門の回答ファイルが `.claude/` に置かれていたのを `.guardian/` へ移した。
  他の道具の現場で `.claude/` に書くのは筋が通らない。

### ② Claude Code でない現場に対応した

`install.mjs` が「この現場に Claude Code の仕掛けがあるか」(`.claude/` か `CLAUDE.md`)を見て、
**無ければ `.claude/` を作らずに、手で回す手順をその場で全部出す**(`--hooks` / `--no-hooks` で上書き)。
どちらにしたかは**必ず言う** ── 黙って片方を選ぶと、なぜ鳴らないのか分からなくなる。

`install.md` に「この道具が Claude Code でない場合」の節を追加。
**本体は1つも減らない**(検査・門・合否・監査は Node だけで動く)。
Claude Code に依存するのは**自動で鳴る部分だけ**、という境界を表で示した。

★実測: `.claude` も `CLAUDE.md` も無い現場で導入し、**`.claude` を作らないこと**と、
  出した手順(検査・自己検査・全体走査・合否)が**全部そのまま動くこと**を確かめた。

## 8.2 (2026-08-28) ── 個人情報を伏せた(公開できる状態に)

依頼主の指示。塊は事故の記録が本体なので、**実機で起きたこと**を具体的に書いてある ──
その具体性が価値であると同時に、個人名が入る所でもあった。

- 人名を**役割の語**に置き換えた(201箇所)。「◯◯の実機で」→「依頼主の実機で」
  ★伏せても**教訓が死なない**ことが条件。「実機で見つかった」まで削ると、
    机上の話に見えてしまう ── それが一番の損
- 家族の名前(3箇所)・秘書の名前(4箇所)・配布先のフォルダ名を伏せた
- **残したもの**: 「この現場(AI秘書)」── 現場の【種類】であって個人情報ではない。
  むしろ「全部実機で起きた」ことの根拠なので残す
- 機械で確認: 人名・アカウント名・現場名が**1つも残っていない**ことを数えた
  (自分の目で「たぶん消えた」と言わない)

## 8.1 (2026-08-28) ── 正本を GitHub に置いた

**正本: https://github.com/ricordi-studio/guardian**(非公開)

これまで正本は「現場A の tools/ 」で、配布はフォルダのコピーだった。
どのコピーが最新かは人の記憶に頼っており、実際に**配った先で直した分が戻らず、
両方の版が同じ番号のまま食い違った**(7.6 の事故)。正本を1つにして、そこから取る形にした。

- **pull.mjs を新設**: 正本から取り直す1行。
  ★取り直す前に **この現場で塊を直していないか**を必ず見る ── 直していたら**止まる**。
    上書きすると、その現場の直りが黙って消えるから(先に --report で1枚を作って還すこと)
  ★何が変わるかを数えて出してから置き換える(黙って上書きしない)
  ★--check なら取らずに差だけ見る
- README の入れ方を「正本から取る」形に。手元のコピーは最新とは限らない
- 指紋の対象に pull.mjs を追加

★これで【配る → 直す → 還す → 配り直す】の輪が、記憶ではなく道具の上で回る。
  ただし**人の承認は残す**(--report の1枚を人が読んでから渡す)。

## 8.0 (2026-08-28) ── 名前を **Guardian** にした

依頼主「この検査ツール全てを Guardian という名称にするのはどうですか?」

**賛成した理由**: `codemap`(地図)は、いまや塊の**一部**でしかない。
実際の中身は5層(合否/門/検査/計器/規律)+ 監査 + 還流で、地図はその1つ。
**名前が中身より狭い = 名前が嘘をついている** ── この塊がずっと直してきたのと同じ形が、
塊自身の名前に残っていた。

### 変えたもの

| 前 | 後 |
|---|---|
| `tools/codemap/` | `guardian/` |
| `codemap.config.json` | `guardian.config.json` |
| `/codemap-audit` | `/guardian-audit` |
| 通行印 `codemap:ok` | `guardian:ok`(器の49箇所を一括置換) |
| `.claude/codemap_audit_at` | `.guardian/audit_at` |

### 変えなかったもの(理由つき)

- **`docs/CODEMAP.md`(地図)** ── 地図は Guardian の**中の1層**。地図という機能の名前として正確
- **`hooks/codemap.js`** ── このフックは「編集の直前に**地図**を差し込む」役。名前が正確
  (一括置換で参照だけ `guardian.js` になっていたのを、7ファイル分ぜんぶ戻した)
- **WHY.md / RULES.md / METHOD.md** ── 中身の名前であって、塊の名前ではない
- **`claude/codemap-audit` ブランチ** ── クラウド側の設定なのでリポジトリからは変えられない。
  文書だけ改名すると**実際と食い違う**ので、実名のまま残し理由を添えた

### ついでに門を直した(改名が炙り出した)

通行印を一括置換したら、**コメントしか変わっていないのに31件の近傍に答えを求められた**。
ロジックは1文字も動いていないので、答えは全部「影響なし・コメントだけ」になる ──
**それは門ではなく儀式である。**答えを埋めれば通る門は、次に本物が来ても同じように埋められて通る。

→ `parseDiff` を作り直し、差分の塊ごとに**消えた行と足された行をコメントと空白を除いて突き合わせ**、
  同じなら数えないようにした。**除いた行数は黙らず表に出す**(何を見なかったか分からない計器は計器ではない)。
  実測: 31件 → **18件**(58行を「中身が変わっていない」と正しく除いた)。

### 配布先へ

**この版から、配布先はフォルダ名ごと入れ替えが要ります。**
`tools/codemap/` を消して `guardian/` を置き、`node guardian/install.mjs` を回してください
(冪等。フックの登録と宣言の名前が新しくなります)。

## 7.7 (2026-08-28) ── 改善の還流(配った先の直りが、元へ還る道)

7.6 で「配られたときの中身と違う」ことは分かるようになった。だが**気づいた後、人が手で運ぶ**
しかなかった。依頼主「監査後にこちらに送る文章を書いて、ユーザーから承認を得られたら、
こちらに送ってきて、それを反映できる、みたいな」── その形にした。

- **指紋をファイルごとに持つ**(`ENGINE_FP`)── 全体で1つだと「どれかが変わった」しか言えず、
  **何を直したのか報告できない**(報告できなければ、還る道が無い)
- **`selfcheck --report`**: 変わったファイルの中身と渡し方の説明を `guardian-report.md` に1枚で書く
- **人の承認を挟む**: その1枚を人が読んで承認し、元の塊のAIへ渡す。**自動送信にはしない**
  ── 中身がそのまま外へ出るので、何を送るかは人が見て決める
- **受け取る側はAIが読んで取り込む**(機械が丸ごと上書きしない ── 元の改善が黙って消えるため)

★サーバーは**いまは要らない**。同じPCの別フォルダなら1枚をコピーで運べば足りる。
  別のPC・他人へ配るようになったら要るが、**流れも人の承認もそのまま**で、
  運ぶ手段が「コピー」から「送信」に変わるだけ。

★実測: 架空の現場を作って塊を配り、そこで1ファイル直し、
  **名指しで赤くなる → --report で1枚ができる**ところまで通した。

## 7.6 (2026-08-28) ── 配った先で直った分が、戻ってこなかった

別プロジェクト(積立と引き出しの金利シミュレータ)へ配って使ってもらった。
**塊自身のバグが2つ見つかり、向こうで直された。だが元の塊には戻ってこなかった。**
しかも両方の `KIT_VERSION` が同じ 7.5 のまま ── **版が嘘をついていた。**

### 逆輸入した2つ(どちらも汎用のバグ)

- **`install.mjs` のパス解決**: ファイルURLから自前で pathname を削っていたので、
  **フォルダ名に空白が入ると壊れる**(URLでは %20 になる)。`fileURLToPath` を使う形へ。
  ★こちらのパスには空白が無いので**一生出なかった** ── 配布先(空白入り)で初めて出た
- **`check.mjs` のセレクタ照合**: `className = ...` の = の前後の空白を許していなかった

### 構造として塞いだ: エンジンの指紋(`ENGINE_FP`)

版の数字は人が上げるので、直した人が上げ忘れれば黙って食い違う。
だから**中身そのものの指紋**で見る(名前ではなく中身で照合)。

- `selfcheck.mjs` がエンジン(mjs とフック)の指紋を計算し、`ENGINE_FP` と照合する
- 違えば **★この塊は配られたときの中身と違います** と赤にし、
  「直したなら元の塊へ戻して版を上げること」と促す
- 意図した変更は `node guardian/selfcheck.mjs --stamp` で押し直す
  (`check.mjs --tighten` と同じ思想 ── 一方通行を人の手作業にしない)
- 文書(WHY.md・RULES.md)は数えない ── 配布先で事故が増えるのは正当

★実際に配布先の塊へこの検査を当てて、**「配られたときと違う」と言えることを確かめた。**

### 使ってもらった評価から(向こうのAIの言葉)

> いちばん価値を発揮したのは「事前に仕込んだ検査」よりも、**「監査という行為そのもの」**でした
> ── 実際に手を動かして地図と実装を突き合わせたからこそ、ズレも、ツール自体の欠陥も見つかった。
> **検査を仕込んで放置するだけでは、ここまでの効果は出なかった**と思います。

この観察は正しく、しかもこの塊の設計そのものを言い当てている ──
だから催促は**時間ではなく変更で駆動する**(`hooks/clock.js`)。
「回す気になったとき」ではなく「実装が動いたとき」に監査を促す仕掛けが、この塊の中心にある。

## 7.5 (2026-08-28) ── **実際に他所へコピーして動かした**(初めて)

「持ち出せる塊」と言いながら、**一度も持ち出して試していなかった**。まっさらなgitリポジトリへ
コピーして端から端まで回したら、**穴が4つ**出た ── どれも「置いてあるだけで誰も読まない/動かない」形:

- **雛形のCLAUDE.mdが書く層の数が古いまま**(1つ足りない)── 導入した瞬間から自己検査が赤かった
- **門(neighbors)の宣言が導入時に入らない** ── `--gate` も `--sweep` も「宣言が無いので回しません」で黙る。
  近傍照合という一番新しい層が、他所では**最初から効いていなかった**
- **証拠(evidence)が空でも案内が無い** ── 合否が【不明】のまま放置される
- **雛形が合否と門を案内していない** ── 他所のAIがCLAUDE.mdを読んでも、その2つを知らない

直したうえで、まっさらな場所で再度確認した(自己検査48件緑 / 門が仕込んだ死にコードを自力で発見)。

★規律に2条追加: **46. 注意書きは検査ではない** / **47. 自分が回した測定にも計器の規律を当てる**

★教訓: **「移植できる」も、実際に移植して確かめるまでは主張であって事実ではない。**
  これは塊自身が説く「無音を成功と読まない」の、塊自身への適用だった。

## 7.4 (2026-08-22) ── 危険駆動の実例を1つ埋めた

- 塊そのものの変更は無し。**危険駆動(RULES.md 43条)で置いた検査の実例**が1件増えた:
  「利用者の表は【誰のデータか】を名乗る口からしか触らない」(onlyIn・max=0)
- 事故を待っていては永久に埋まらない領域(絞り込みが抜けても**例外が出ない**ので、
  事故として現れず WHY.md に1行が書かれない)に、先に検査を置いた例

## 7.3 (2026-08-22) ── いま有効な決定と、履歴を分ける

- **`perSection` を新設**: 節ごとに決めた1行があること(決定記録に**状態**を必須にする用)
- **`citeLive` を新設**: **もう現行でない決定を、いまの規則として引いていないか**。
  履歴として引くのは正当なので、行末の `guardian:ok` で通せる(理由を書くのが通行料)
- どちらも見本で毎回試す(自己検査 48件)

★この2つは、規範文書の**絶対規則**が1か月以上前に改定された決定を語り続けていた事故から。
決定記録は履歴なので古い節が残るのは正しい ── 問題は、古い節と現在の節が**同じ見た目で並ぶ**こと。

## 7.2 (2026-08-22) ── 完了の判定を、AIの外へ移した

- **`verdict.mjs`(合否)を新設**。宣言(`guardian.config.json` の `evidence`)に並べた証拠を集め、
  **通過 / 差戻 / 注意 / 不明** の4語に落とす。**判定は1つも持たない**(理屈は各証拠の出口コード)
  ── ★**不明を緑に数えない**(出口 差戻=1 / 不明=2 / 通過=0)
- **`hooks/stop.js`(Stop フック)を新設**。「できました」と言い終える**手前**で合否を回し、
  **差戻のときだけ止めて理由を返す**。不明・注意は止めないが**必ず本人に返す**
  (実装が変わっていなければ回さない / 二度は止めない / 失敗しても黙って通す)
- **夜間CIを合否1本に寄せた**。証拠の一覧が「CIの yaml」と「宣言」の2枚に割れていたのを1枚に(39条)
- `--fast` で回さなかった証拠は**名前を出す** ── 「速い方だけ回して全部通った」が
  「全部通った」に化けるのを防ぐ
- 道具の有無は**出口コードで**確かめる(`needsCmd`)。文言で当てる実装は、
  日本語Windowsで丸ごと外れた(実測)

## 7.1 (2026-08-22) ── 事故が【どの層まで届いたか】を数える

- **WHY.md の全事故に札**を付けた: `[到達/由来]`。到達=検査/門/計器/**規律(=文章にしかない)**、由来=事故/予防
- **selfcheck が数える**: 札の無い事故があれば赤 / **守り(門か検査)の件数は下げない**
  (`selfcheck.mjs --tighten` で下限を上げる。check.mjs の `max` と同じラチェットで向きだけ逆)
  ── 初回の実測は **事故69件・守り25・計器15・文章だけ29**
- 件数の照合を書き分けた: **総数は「全N条」「全N件」**(実数と一致)/ **参照は「N条」**(実在する条番号か)
  ── 「RULES.md 39条」が総数の主張と読まれて誤検出になった(誤検出は検査ごと外される)
- RULES.md 43条「事故を待てるものと、待てないもの」(事故駆動と危険駆動)
- RULES.md 44条「門には必ず双子を置く」── 門は黙って死ねるので、同じ宣言を読む検査が要る
- WHY.md の「配るときの約束」を落とした(README と重複していた・39条)

## 7.0 (2026-08-21) ── 塊が塊を検査するようになった

**この版から `selfcheck.mjs` が同梱されます。** 塊を配る前に必ず回してください。

- **`selfcheck.mjs` を新設**(塊の自己検査・34件)。見本の小さなリポジトリを一時領域に建て、
  **1箇所ずつわざと壊して**「壊したら赤 / 壊さなければ緑」を確かめる。
  ＋塊そのものの健康診断(版が嘘をついていないか / 一度も試されていない検査の種類が無いか /
  エンジンに現場固有が混じっていないか / 文書が書いた件数が実数と合っているか)
- **`callArgs` を新設**(【既定に任せてはいけない引数】を数える)。
  ★これに伴い、エンジンに直接書かれていた**現場固有の検査を撤去**した
  ── 「`check.mjs` はどのプロジェクトでも同じ中身」がやっと本当になった
- **`check.mjs --tighten`**: 口が減っていれば宣言の `max` を実数まで機械が書き下げる
  (一方通行を人の手作業にしない)
- `noInline`: 見張るはずの語が実装から消えていたら【緑にしない】(永久に0件=緑を返していた穴)
- RULES.md に42条「計器そのものを、計器で確かめる」/ WHY.md に事故を4件追加

## 6.1 (2026-08-20)

- **METHOD.md を新設**(まとめの入口)。弱点6つ・対策4層(門/検査/計器/規律)・情報/作法/器の分け方・人間側の一言
- RULES.md に7条追加(13〜19): 相手の答えを捨てない / 症状を抑えたら原因を問う /
  合格の判定は実際の使い方に合わせる / 情報が足りないときは器で埋めない /
  器は種類の名前すら持たない / 未確認と失敗を区別する / 指示は形と説明を混ぜない
- WHY.md に事故を7件追加(2026-08-20)。合計29件
- hooks/no-fixed-names.js(固有名の門)を収録 ── 禁止語は検査の宣言から読む
- check.mjs: kind ごとの必須の欄が無い検査は【赤】にする(何も見ていない検査を許さない)

配った先の `KIT_VERSION` と見比べて、**どのコピーが古いか**を判断するための記録です。
上げ忘れると、この番号は嘘をつきます(**実際に v4 を上げ忘れました**・下記)。

## v6 — 2026-08-17
- **監査の呼び出し口を同梱**。`templates/guardian-audit-command.md` を `install.mjs` が
  `.claude/commands/guardian-audit.md` に置く → **誰でも `/guardian-audit` の1語で回せる**
  (無人の定期実行はアカウント側にしか置けないが、**呼び出し口はリポジトリに置ける**)

## v5 — 2026-08-17
- **夜間の見張りを同梱**。`templates/nightly-check.yml` を `install.mjs` が
  `.github/workflows/guardian-nightly.yml` に置く。**毎晩01:00・赤ければIssue・緑で閉じる**
- README に「夜中は2段」を明記(①機械検査=付いてくる / ②観点を選んだ監査=付いてこない)
- ⚠ **この版から、コピー先も入れ直しが要ります**(上書きコピー → `install.mjs` を再実行。冪等)

## v4 — 2026-08-17(番号を上げ忘れた版)
- `install.mjs` / `install.md` を追加。**フォルダを置いてAIに一言**で導入が済むように
- 検査エンジンの2つの穴を修正:
  - 宣言した検査が値を拾えなくても黙って通っていた → **落とす**(何も見ていない検査は無い検査より悪い)
  - 地図の雛形の例示を実装への主張と読み、初日に嘘の指摘が出ていた → 囲みコードを拾わない

## v3 — 2026-08-16
- 塊として切り出し。エンジン(記号実在 / same・sameMap・sameSet・shape / noInline / セレクタ)、
  フック2本(置き場所に依存しない)、WHY.md・RULES.md・audit.md・templates を同梱
