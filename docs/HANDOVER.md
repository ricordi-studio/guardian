# 引き継ぎ(2026-09-03 03:5x 時点)

★**この文書は【まだ記録に無いこと】だけを書く。**済んだことは `CHANGELOG.md` と `WHY.md` に在るので写さない(39条)。
★次のセッションは、まずここを読み、それから下の「読む順」へ。

---

## ★定期監査(27.11、2026-09-03 17:40 ごろ)

```
観点1 実在照合 … ★check.mjs「ずれなし」/ selfcheck 出口2(未測1 ── 正本由来の署名が無い分)
観点2 網羅照合 … ★★**配る30件のうち4件が地図に無かった** → 足した
                 METHOD.md / CHANGELOG.md / ENGINE_FP / WHY_SEEN
                 ★★★同じ形が2度目(23.4 は package.json 1枚)── 機械で突き合わせて見つけた
観点3 死にコード … ★0件(--sweep)
        写経の疑い … ★★1件 `main ×2`(hooks/codemap.js / hooks/stop.js)
                    ★★★フックの入口なので、たぶん正しい重複 ── **勝手に直していない**(報告のみ)
観点4 メタ化余地 … 集中点の上位は rel(145)/判定(125)/HERE(105)/未測(100)/ok(81)
                 ★★selfcheck の 判定・未測・ok が並んで出ている ──
                 ★★★これは【未仕分けの押し 73件】と同じ所を指している(会議で追っている分)
口の増減 … ★無し(`--tighten`「下げられる max はありません(全部ぴったり)」)
```

★**依頼主の判断を仰ぐ分(観点3・4)**: 上の写経1件と、メタ化の集中点。
★★どちらも勝手に直していません。

## いまの状態

| | |
|---|---|
| 版 | **18.1** |
| 合否 | **通過**(4/4・測れた割合 100%) |
| 自己検査 | **通過 81件 / ここでは測れない 2件** |
| 枝 | **`kit/17.0-common-writer`**(main には入れていない)── 13.25 〜 18.1 を6本の commit で押してある |
| 会議 | **外の会議(`<デスクトップ>/外の会議-1.1.0/Guardian改修/外の会議`)に居る**。名は `guardian`。参加者は claude / codex / kozo / guardian |

★**会議の作法**: `wait guardian --timeout 540` を**ターンの最後の行為**にする。
発言は `say guardian --file 下書き.md`。★★他人の名で say しない / `posts/` を直接 触らない / `tsugite.mjs` を改造しない。

---

## ★依頼主が いま目指していること

> **Guardian を、対象プロジェクトに浸食させずに入れ、残滓ゼロで外せる形にできるか。**

★**答えは出た(18.0 で実測)** ── GitHub の枝から clone → pull → install → 使う → 外す → 束を消す、で
`git status` が**空**(現場は入れる前と1バイトも違わない)。

★★だが「残滓ゼロ」に着くまで**3回 名乗り損ねた**(WHY に全部 在る):
空フォルダ / `git status` だけが見た2件の差分 / 比較の正規表現から逃がしが消えていた。

---

## ★★まだ手を付けていない宿題(会議から)

```
★① @kozo の【紙から作る保持一覧】
   .guardian/audit_at と 踏んだこと は、塊が名指しするが★★書き手が居ない(人が書く)
   → 共通の書き手を入れても台帳に載らない ── ★★★UNKNOWN が消えない
   → 「機械で導けないため保持する」一覧を【紙から】作り、期待保持へ上げる
     (各項に「根拠は説明文のみ / 削除不可 / 現場の申し送り」と理由を併記。★片方向)

★★② @codex の【legacy 証拠で所有確定】
   いまは「いま在る + 元を持たない」→ 所有未確定 → UNKNOWN で止めている
   → 版プロファイル(どの版が何を置いたか)が在れば、過去の導入の物だと言い切れる
   → ★★★版プロファイルは、まだ在りません

★③ @codex の【指紋一致】(retained の①を、束27件の配布時指紋と突き合わせる)
   受領証(guardian/.guardian/pulled.json)に SHA は残っている

★★④ 「届かなかった入力」を言う仕掛け
   skip_dirs で外した物は「見ていません」と言えるが、
   ★★★綴りが合わずに素通りした物は言えない(居たことすら知らない)
   → 道.cjs の保存則(発見 = 読めた + 読めなかった)が一部を埋めたが、全部ではない
```

---

## ★★★測っていないこと(はっきり書く)

```
★POSIX の道はタブ・改行を含み得る ── Windows で作れないので【未測】
★★依存の網(外す.mjs)の上限(4000ファイル / 1枚 512KB)に、大きい現場で当たるかは【未測】
★★★hooks/in-loop.js の【呼出し被覆】は、いまも測れない(13.22 から変わっていない)
   block実効 1/1(止まることは証明した)/ 呼出し被覆 0/N ── ★N が出せない
★依頼主が保留にした件: A4 の物差し(根の直下のファイルを file と数えない)── 出し直す必要が在る
```

---

## ★2026-09-03 03:5x の定期監査(CODEMAP)

```
計器      … selfcheck 通過 81件 / ここでは測れない 2件(★赤くない状態で監査した)
観点1     … 地図の記号 27件・名指しファイルとも全て実在
観点2     … ★配る11枚のうち index.mjs だけが地図に無かった → 項を足した
            ★★install.mjs → 台帳.mjs の接点が抜けていた → 書いた
観点3     … 死にコード候補 0件 / 写経の疑い 1件(hooks の main ×2 ── どちらも入口。正しい重複)
観点4     … 参照の集中点は ok / ng / 未測 / problems / notes(検査の集計器。メタ化の余地は薄い)
--tighten … 下げられる max なし(全部ぴったり)。逃げ道 9件(上限 9)
```

★**残った一言(観点2)**: `WHY.md → 台帳.mjs` が「接点が足りない」と出るが、これは
★★**文書がエンジンを名指ししている**だけで、呼んでいるわけではない。地図に偽の接点を書く方が悪い。

---

## 読む順

1. `guardian/METHOD.md` ── 弱点6つ・対策5層
2. `guardian/RULES.md` ── 修繕の作法
3. `guardian/WHY.md` ── 各検査が生まれた事故(★ここがこの塊の本体)
4. `docs/CODEMAP.md` ── 機能→全接点の地図

## ★main へ入れる前の、開いた条件(2026-09-03、@codex が投入条件として置いた)

```
① 署名/固定マニフェスト … ★正本由来を機械で証明する物が無い(★★26.1 で true の分岐を消したので、B21 は常に未測。★★★ラチェットも失われている ── 署名が入るまで戻らない)
   → B21 は「証明できない」で未測に落ちるだけ。証明があれば測れる
② 性能上限の見張り     … ★★selfcheck 単体 37.4秒 / 上限 180秒
   evidence は4件とも fast なので【コミットのたびに全部 回る】
   検査を足すたびに増える(今夜 5本で +5秒)。★★★見張る物が無い
   → fast 分離 か 実時間を証拠にする かを決めるまで、開いたまま
③ 別の目による独立監査 … B21/B22・pull/pre-commit・個人情報の既定値
   → ★定期監査は【本人が本人の直しを見た】ので証拠力が低い(audit.md: 本人 1/6)
```
