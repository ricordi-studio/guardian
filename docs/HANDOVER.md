# 引き継ぎ(2026-09-03 03:5x 時点)

★**この文書は【まだ記録に無いこと】だけを書く。**済んだことは `CHANGELOG.md` と `WHY.md` に在るので写さない(39条)。
★次のセッションは、まずここを読み、それから下の「読む順」へ。

---

## ★定期監査(2026-09-03 20:47)

```
観点1 実在照合 … ★check.mjs「ずれなし」/ selfcheck 出口2(未測1 ── 署名)
観点2 網羅照合 … ★★配る30件のうち 地図に無い物 0件(前回の4件は 埋まったまま)
               ★★★今日の後半に足した物(危ないと言う / 見ていない所)は
               **振る舞いの言葉**で地図に在る(記号名では 引けない)── 漏れでは ない
観点3 死にコード … ★0件 / 写経の疑い ★★1件(main ×2、hooks の入口 ── 前回と同じ・直していない)
観点4 メタ化余地 … 集中点は前回と同じ(rel / 判定 / HERE / 未測 / ok)
```

★**この監査では 新しい発見は 在りません**(★★今日の重い3本は 会議で 出ています)。

## ★★★開いている いちばん重い物(2026-09-03 20:21、@kozo が見つけ 私が再現)

### ★止まらなくなる道は【3本】在る(2026-09-03 20:44、@kozo が3本とも再現・私が①を再現)

```
見本(配布先): src/f.js の beta を直して commit。呼び手 gamma が src/g.js に在る。
  掃除直後 … ★gate 出口1「未回答: gamma ← beta を呼んでいる」

★①書き手を通る道が 現場を汚す → 門が【押した変更を 見ない】
    verdict のみ1回 … 汚れ2件 → gate 出口0 / gamma 0件
    stop のみ1回    … 汚れ2件 → gate 出口0 / gamma 0件
    ★★.gitignore で verdict_at を隠しても【台帳の分が残る】(@kozo 実測)
★★②stop は 2回目から skipped_unchanged ── ★★★そもそも 測り直さない
★★★③無関係な commit を1つ積むと、窓(HEAD~1..HEAD)の外へ出て 緑になる
```

★**再現する時は【打った口】を書くこと**(@kozo 22:04)── verdict は 人向けなら汚し、
`--json` なら汚さない。だから「verdict は汚す/汚さない」は **どちらも実測として正しい**。
★★私と @kozo が 半日 食い違った理由は、これが全部だった。

★★★**2つの緑は【言葉では もう分かれている】**(字面で確認・neighbors.mjs):

```
★:1290 「近傍照合: 通過(N件すべてに回答あり)」 ← 答えた   → 出口0
★★:1199 「近傍照合: 近傍なし(答えるべき相手が居ません)」 ← 見ていない → 出口0
```

★潰しているのは【出口の数】と【verdict の counts】だけ ──
★★**新しく測る物は要らない。既に在る区別を、数の口へ通すだけ**(@kozo)。
★★★ただし 合否の言葉が増えるなら 版は【大】── 実装の小ささと 契約の大きさは 別(@codex)。

★**3本は 別々の穴** ── 1本直しても、残り2本で 止まらない(@codex も同意)。
★★塊の clone では ①③が出にくい: 塊は .gitignore に verdict_at を持ち、**台帳は配布先にしかない**。
★★★**落ちるのは 配布先だけ** ── 私が 2度 取り下げたのは、塊で測ったから。

★どれも【判定の意味】を変えるので **直していない**。会議の 結論 → 監査 に渡した。
★★未測定: 答えた後 / CI(浅いクローン)/ 窓を広げた時 / skipped_unchanged の根拠(字面未読)。

### ★近傍の門は、作業木が【1バイトでも汚れている】と 押した変更を 見ない

```
同じ commit(27.24 の clone に stop.js を1行 足して commit):
  ★汚れ 0件            → 門 出口1 / ✗6件
  ★★.guardian/coverage に【1バイト】足す → ★★★門 出口0 / ✗0件「近傍なし」
  ★戻す(汚れ0件)       → 門 出口1 / ✗6件
```

★字面: `neighbors.mjs` の range は `dirty ? "HEAD" : "HEAD~1..HEAD"` ──
★★汚れていると【まだ commit していない分】だけを見るので、押した変更が 範囲の外へ出る。
★★★そして「近傍なし」= **通過** として verdict に数えられる(18条の反対側)。

★**何が木を汚すか ── 【打った口】で決まります**(2026-09-03 20:52、@kozo が見つけ 私が再現)。

```
同じ見本・同じ版・毎回 掃除:
  ★verdict --json  … 汚れ0件 / coverage 無し / ★★gate 出口1(赤のまま)
  ★★verdict 人向け … 汚れ2件 / coverage 在り / ★★★gate 出口0(緑)
  stop            … 汚れ2件(.claude/verdict_at + 台帳)/ gate 出口0
```

★字面: `--json` の枝は `process.exit(code)` で終わり、**coverage を書く所まで来ない**(verdict.mjs)。
★★だから「現場が .guardian を git に載せているか」で束ねたのは **私の誤り**(20:42)。
★★★正しくは:**書き手を通る道が【実際に走った】時に汚れる。どの道が走るかは、打った口で決まる**。

★いちばん効く形: **人が「大丈夫か」を見るために verdict を人向けで1回打つ ──
その1回が木を汚し、次の門が押した変更を見なくなる**(確かめる行為が、確かめる仕組みを黙らせる)。

★★この一文は 今日 3度 書き直した(verdict のせい → 違う → 現場の git → 違う → **打った口**)。
★★★毎回 **測る場所か 測る口が 1つしか無かった**。
★確かなのは1つだけ: **汚れが1件在れば、門は押した変更を見ない**(行ったり来たり再現)。
★★配布先でも起きる(@kozo 実測): stop が block → **continue_with_unknown** に変わり、
★★★消えるのは「呼び手に答えていない」という**本物の未回答**。
★★★直していない ── 判定の意味が変わるので、会議の【結論 → 監査】に掛けた。案は3つ:

```
① 汚れていても HEAD~1..HEAD を見る(★作業中の人が毎回 赤くなるかもしれない)
② ★★汚れている時は【不明】にする(止めないが、緑とも言わない ── @kozo の推し)
③ ★★★②に加えて【何を見たか(範囲)】を必ず言う(私の案)
```

★未測定: 汚れがコードの紙の時 / CI(浅いクローン)/ 配布先での再現。

### ★★もう1つ ── 「問えたが 相手が0件」を 通過と数えてよいか

```
宣言が無い       … ★もう【不明】(出口2 ── 実測)
★★宣言は在る/対象0件 … いまは【通過】(★★★@kozo が「薄い」と言った所)
```
★**先に読むこと: 区別は【もう在ります】**(@kozo 22:16)── 門は言葉では既に分けている:
```
:1290「通過(N件すべてに回答あり)」← 答えた / :1199「近傍なし(答えるべき相手が居ません)」← 見ていない
```
★★潰しているのは【出口の数】と【verdict の counts】だけ ── ★★★案A/案B は
**新しい区別を作る仕事ではなく、既に在る区別を 数の口へ 通す仕事**。
★見積もる人が ここで読めるように、上(:45)と同じ事を もう一度 書いた ──
★★**生きている区別が 紙に無いと、作り直される**(@kozo)。

★案A: 合否語に `not_applicable` を足す(版が大)/ ★★案B(@codex 推し): 欄で分け counts から外す。
★★★どちらも 監査待ち。**現行の「通過」は「対象なしを含む通過」と読むこと。**

### ★正本は 13.24(2026-09-03 20:30 時点)

★今日の 27.x(24版)は 枝 `kit/17.0-common-writer` の手元だけに在る。
★★どの現場も `pull` では 今日の直りを受け取れない ── ★★★正本へ入れるかは 依頼主が決める。
## ★未解決(27.23、2026-09-03)── 1度だけ見た 出口1 / 差戻0

```
★見た … (B)素の新規現場で verdict の出口=1、同じ現場の次の走行の文は 差戻0
★★叩き直した … 4通り5回。★★★1度も 再現しない
★分かった事 … 1つの走行の中では 文も出口も 同じ n() から出るので 食い違えない
★★つまり 私は【2つの走行】を比べていた ── ★★★それでも 1回目が 出口1 だった理由は 不明
```

★**直したとは 言わない**。★★27.23 で「1回で両方 見える」ようにしただけ。
★★★もう一度 出たら、その走行の stdout / stderr / 出口 / 時刻を 丸ごと 残すこと。

## ★定期監査(27.11、2026-09-03 17:40 ごろ)

```
観点1 実在照合 … ★check.mjs「ずれなし」/ selfcheck 出口2(未測1 ── 正本由来の署名が無い分)
観点2 網羅照合 … ★★**配る30件のうち4件が地図に無かった** → 足した
                 METHOD.md / CHANGELOG.md / ENGINE_FP / WHY_SEEN
                 ★★★同じ形が2度目(23.4 は package.json 1枚)── 機械で突き合わせて見つけた
観点3 死にコード … ★0件(--sweep)
        写経の疑い … ★★1件 `main ×2`(hooks/codemap.js / hooks/stop.js)
                    ★★★フックの入口なので、たぶん正しい重複 ── **勝手に直していない**(報告のみ)
観点4 メタ化余地 … 集中点の上位は rel(145)/判定(125)/HERE(105)/未測(100)/ok(81)
                 ★★selfcheck の 判定・未測・ok が並んで出ている ──
                 ★★★これは【未仕分けの押し 73件】と同じ所を指している(会議で追っている分)
口の増減 … ★無し(`--tighten`「下げられる max はありません(全部ぴったり)」)
```

★**依頼主の判断を仰ぐ分(観点3・4)**: 上の写経1件と、メタ化の集中点。
★★どちらも勝手に直していません。

## いまの状態

| | |
|---|---|
| 版 | **18.1** |
| 合否 | **通過**(4/4・測れた割合 100%) |
| 自己検査 | **通過 81件 / ここでは測れない 2件** |
| 枝 | **`kit/17.0-common-writer`**(main には入れていない)── 13.25 〜 18.1 を6本の commit で押してある |
| 会議 | **外の会議(`<デスクトップ>/外の会議-1.1.0/Guardian改修/外の会議`)に居る**。名は `guardian`。参加者は claude / codex / kozo / guardian |

★**会議の作法**: `wait guardian --timeout 540` を**ターンの最後の行為**にする。
発言は `say guardian --file 下書き.md`。★★他人の名で say しない / `posts/` を直接 触らない / `tsugite.mjs` を改造しない。

---

## ★依頼主が いま目指していること

> **Guardian を、対象プロジェクトに浸食させずに入れ、残滓ゼロで外せる形にできるか。**

★**答えは出た(18.0 で実測)** ── GitHub の枝から clone → pull → install → 使う → 外す → 束を消す、で
`git status` が**空**(現場は入れる前と1バイトも違わない)。

★★だが「残滓ゼロ」に着くまで**3回 名乗り損ねた**(WHY に全部 在る):
空フォルダ / `git status` だけが見た2件の差分 / 比較の正規表現から逃がしが消えていた。

---

## ★★まだ手を付けていない宿題(会議から)

```
★① @kozo の【紙から作る保持一覧】
   .guardian/audit_at と 踏んだこと は、塊が名指しするが★★書き手が居ない(人が書く)
   → 共通の書き手を入れても台帳に載らない ── ★★★UNKNOWN が消えない
   → 「機械で導けないため保持する」一覧を【紙から】作り、期待保持へ上げる
     (各項に「根拠は説明文のみ / 削除不可 / 現場の申し送り」と理由を併記。★片方向)

★★② @codex の【legacy 証拠で所有確定】
   いまは「いま在る + 元を持たない」→ 所有未確定 → UNKNOWN で止めている
   → 版プロファイル(どの版が何を置いたか)が在れば、過去の導入の物だと言い切れる
   → ★★★版プロファイルは、まだ在りません

★③ @codex の【指紋一致】(retained の①を、束27件の配布時指紋と突き合わせる)
   受領証(guardian/.guardian/pulled.json)に SHA は残っている

★★④ 「届かなかった入力」を言う仕掛け
   skip_dirs で外した物は「見ていません」と言えるが、
   ★★★綴りが合わずに素通りした物は言えない(居たことすら知らない)
   → 道.cjs の保存則(発見 = 読めた + 読めなかった)が一部を埋めたが、全部ではない
```

---

## ★★★測っていないこと(はっきり書く)

```
★POSIX の道はタブ・改行を含み得る ── Windows で作れないので【未測】
★★依存の網(外す.mjs)の上限(4000ファイル / 1枚 512KB)に、大きい現場で当たるかは【未測】
★★★hooks/in-loop.js の【呼出し被覆】は、いまも測れない(13.22 から変わっていない)
   block実効 1/1(止まることは証明した)/ 呼出し被覆 0/N ── ★N が出せない
★依頼主が保留にした件: A4 の物差し(根の直下のファイルを file と数えない)── 出し直す必要が在る
```

---

## ★2026-09-03 03:5x の定期監査(CODEMAP)

```
計器      … selfcheck 通過 81件 / ここでは測れない 2件(★赤くない状態で監査した)
観点1     … 地図の記号 27件・名指しファイルとも全て実在
観点2     … ★配る11枚のうち index.mjs だけが地図に無かった → 項を足した
            ★★install.mjs → 台帳.mjs の接点が抜けていた → 書いた
観点3     … 死にコード候補 0件 / 写経の疑い 1件(hooks の main ×2 ── どちらも入口。正しい重複)
観点4     … 参照の集中点は ok / ng / 未測 / problems / notes(検査の集計器。メタ化の余地は薄い)
--tighten … 下げられる max なし(全部ぴったり)。逃げ道 9件(上限 9)
```

★**残った一言(観点2)**: `WHY.md → 台帳.mjs` が「接点が足りない」と出るが、これは
★★**文書がエンジンを名指ししている**だけで、呼んでいるわけではない。地図に偽の接点を書く方が悪い。

---

## 読む順

1. `guardian/METHOD.md` ── 弱点6つ・対策5層
2. `guardian/RULES.md` ── 修繕の作法
3. `guardian/WHY.md` ── 各検査が生まれた事故(★ここがこの塊の本体)
4. `docs/CODEMAP.md` ── 機能→全接点の地図

## ★main へ入れる前の、開いた条件(2026-09-03、@codex が投入条件として置いた)

```
① 署名/固定マニフェスト … ★正本由来を機械で証明する物が無い(★★26.1 で true の分岐を消したので、B21 は常に未測。★★★ラチェットも失われている ── 署名が入るまで戻らない)
   → B21 は「証明できない」で未測に落ちるだけ。証明があれば測れる
② 性能上限の見張り     … ★★selfcheck 単体 37.4秒 / 上限 180秒
   evidence は4件とも fast なので【コミットのたびに全部 回る】
   検査を足すたびに増える(今夜 5本で +5秒)。★★★見張る物が無い
   → fast 分離 か 実時間を証拠にする かを決めるまで、開いたまま
③ 別の目による独立監査 … B21/B22・pull/pre-commit・個人情報の既定値
   → ★定期監査は【本人が本人の直しを見た】ので証拠力が低い(audit.md: 本人 1/6)
```
