# ★Guardian に 送る

★★この塊が 欲しいのは【新しい機能】より ★★★【事故の記録】です。

この塊の 検査は 1本ずつ、★実際に 転んだ所から 生まれました。
だから 一番 価値が 在る贈り物は ── **あなたが 転んだ話** です。

---

## ★① 事故を 報告する(★一番 歓迎します)

Issues → **事故報告** の型に 沿って 書いてください。要るのは 4つだけ:

```
★① 何を したか        ── ★★叩いたコマンドを そのまま
★② 何が 起きたか      ── ★★★出力を そのまま(★要約しないでください)
★③ 何を 期待したか
★④ 版                  ── node KIT_VERSION / guardian --version の 出力
```

★★**「〜のはずです」は 要りません。★★★測った物だけ ください。**
★直し方が 分からなくても 構いません。★★事故そのものが 材料です。

---

## ★★② コードを 送る

1. fork して、枝を 切る
2. `node selfcheck.mjs` が **緑** で 通る事を 確かめる
3. ★★直した理由を、**どの事故から 来たか** で 書く
4. Pull Request を 出す

★★★新しい検査を 足す時は、**それを 生んだ事故** を 一緒に 書いてください。
理由の無い検査は、後で 誰も 消せなくなります。

---

## ★★★③ 権利について(★送る前に 読んでください)

この塊は **AGPL-3.0-or-later** で 配られています。

★あなたが 送った物は、次の 2つを 認めた事に なります:

```
★① あなたに その物を 送る権利が 在る(★他所から 持ってきた物では ない)
★★② 作者(Ricordi.studio)が その物を
     ・AGPL-3.0-or-later で 配ってよい
     ・★★★そして【他の許諾でも】配ってよい
```

★★②が 要る理由:
```
★将来 この塊を【会社向けの 別の許諾】でも 出す事が 在り得ます。
★★その時、送られた物の権利が 送った人の物のままだと ── ★★★出せなくなります。
★これは 後から 直せません(★★送った全員に 聞き直す事に なる)。
★★だから 最初から 書いています。
```

★送る時は、コミットに この1行を 付けてください:

```
Signed-off-by: あなたの名前 <あなたの宛先>
```

`git commit -s` で 自動で 付きます。

---

★★★私は 法律家では ありません。この紙は 作法であって、法律の助言では ありません。
