# 導入の続き（この文書はAI宛です）

あなたがこれを読んでいるということは、このリポジトリに `guardian`（修繕の塊）が置かれ、
導入を任されたということです。**次の順で進めてください。**

---

## 0. まず機械にできる分を済ませる

```bash
node guardian/install.mjs
```

地図の雛形・宣言・フックの登録・CLAUDE.md への追記まで、**冪等に**行われます。
既にあるものは触りません。何が起きるか先に見たければ `--dry` を付けてください。

## 1. 読む（10分）

- **`guardian/WHY_INDEX.md` — まずここ。**事故の索引(重み付き)。
  ★**◎(構造の芽)だけが本当に欲しいもの** ── 情報/作法/器の分け方が一段進んだ記録。
  ○(型)と・(個別)は、いま触っている所に関係するものだけ読めばよい。
  索引は `node guardian/index.mjs` が作る(手で直さず、重みは `WHY_WEIGHT` に書く)
- `guardian/WHY.md` — **各検査が生まれた事故の全文**。**全部は読まなくてよい**が、
  **検査を足すときは必ずここに1行足す**(理由の無い検査は、次の人に外される)
- `guardian/RULES.md` — 修繕の作法。**あなたの実装がこの規律に従う**

## 2. 不変条件を探して宣言に書く（ここが本番）

`guardian.config.json` の `checks` は空で置かれています。**このリポジトリを実際に読んで**埋めてください。

探すのは1種類だけです ── **「同じ意味の事実が、2箇所以上に別々に書かれている所」**。

| 探し方 | 例 |
|---|---|
| 定数を grep する | 税率・為替・上限値・版番号が複数ファイルに散っていないか |
| 表を探す | 単価表・対応表・性質表が2箇所にないか（片方だけ直すと事故） |
| 「この形で書け」という前提 | 他所が正規表現で読んでいる書き方（版番号の宣言など） |
| 社名・種別の分岐 | `if (x === 'a' \|\| x === 'b')` が表の外に散っていないか |

見つけたら `same` / `sameMap` / `sameSet` / `shape` / `noInline` のどれかで書きます
（書き方は `templates/guardian.config.json` と、いま動いている実例を参照）。

**必ず `why` を書くこと。** 「どの事故を防ぐのか」が無い検査は、次の人に真っ先に外されます。
思いつかなければ、それは**まだ検査にする段階ではありません**。

> 最初は 0〜2個で構いません。**事故が起きるたびに1つ足す**のが本来の育ち方です。

## 3. 地図を書き始める（全部書かない）

`docs/CODEMAP.md` は空で置かれています。**いま触っている機能から**書いてください。

- 1機能 = 1項（`## 見出し`）、10行以内
- 接点は**実名をバッククォートで囲む** ── 囲まないと、フックも検査も**見えません**
- 「全部書く」を目指さないこと。**触った所から増やす**のが正しい

## 4. 動くことを確かめる

```bash
node guardian/check.mjs
```

- 記号の実在照合が通るか（地図に書いた実名が実装にあるか）
- 宣言した不変条件が通るか

**わざと1箇所こわして、落ちることを確かめてください。**
落ちない検査は、あっても無いのと同じです。

エンジン側(検査の仕組みそのもの)は、機械が確かめます:

```bash
node guardian/selfcheck.mjs
```

見本のリポジトリを一時領域に建て、1箇所ずつわざと壊して「壊したら赤 / 壊さなければ緑」を確かめます。
**ここが赤いまま `check.mjs` が「ずれなし」と言っても、信用できません。**

## 5. 依頼主に報告する

- 何を検査するようにしたか（`why` と一緒に）
- 地図に何項書いたか、これから何を足すか
- **落ちることを確認したか**

---

## 守ること

1. **この塊にプロジェクト固有のものを入れない。** 固有のことは `guardian.config.json` と、
   そのプロジェクト独自のフックへ。塊が汚れると配れなくなります
2. **検査を足したら `WHY.md` に1行足す**。種類(kind)を足したら `selfcheck.mjs` の見本にも足す
3. **誤検出を出さない。** 1件の誤検出が、検査全体の信用を殺します。
   例外は逃げ道（行末 `guardian:ok`）で黙らせ、**逃げ道を必ず残す**
4. **フックは失敗しても黙って通す。** 落ちると開発そのものが止まります。
   ただし **双子のいない門を足さないこと** ── 門は黙って死ねるので、
   **同じ宣言を読む検査**が書いた後にもう一度落とす形にして初めて、黙って通してよくなります

---

## この道具が Claude Code でない場合(Codex / 他のAI開発ツール など)

**本体は何も減りません。**検査・門・合否・監査は Node だけで動きます
(外部パッケージゼロ・`npm install` 不要)。実測: `.claude` を丸ごと消した現場で
検査50件・門・合否・全体走査が全部そのまま動き、Linux(GitHub Actions)でも回っています。

Claude Code に依存しているのは **自動で鳴る部分だけ** です:

| 動くもの(どの道具でも) | Claude Code 専用 |
|---|---|
| `check.mjs` / `selfcheck.mjs` / `neighbors.mjs` / `verdict.mjs` / `pull.mjs` | `hooks/clock.js`(監査の催促) |
| `WHY.md` / `RULES.md` / `METHOD.md` / `audit.md`(ただのMarkdown) | `hooks/codemap.js`(編集の直前に地図を差し込む) |
| | `hooks/stop.js`(完了を名乗る手前で合否) |
| | `/guardian-audit`(スラッシュコマンド) |

`install.mjs` は **この現場に Claude Code の仕掛けがあるか**(`.claude/` か `CLAUDE.md`)を見て、
無ければ **`.claude/` を作らずに**、手で回す手順をその場で出します
(`--hooks` / `--no-hooks` で上書きできます)。

### 手で回す(これだけ覚えれば同じことができます)

```bash
node guardian/verdict.mjs            # 完了の判定。通過 / 差戻 / 注意 / 不明
node guardian/check.mjs              # 地図と実装のずれ
node guardian/neighbors.mjs --gate   # 修正の2つ外側に答えたか
node guardian/neighbors.mjs --sweep  # 全体の棚卸し(監査の下ごしらえ)
```

定期監査は `guardian/audit.md` をAIに読ませてください(4観点の手順が書いてあります)。

### 鳴る部分も欲しいとき

お使いの道具に**フック相当の仕掛け**(コミット前・応答前に何かを走らせる機能)があれば、
`guardian/hooks/*.js` を呼ぶよう設定すれば同じように働きます。
フックの中身はただの Node スクリプトで、標準入力を読んで標準出力に返すだけです
── **呼び出し方だけの問題**で、中身を書き換える必要はありません。

★どちらにしても、**完了の判定(`verdict.mjs`)だけは必ず回してください。**
これが「できました」を人の感覚から機械の側へ移す唯一の口です。
