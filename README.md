# guardian — AIと一緒に直すための、壊れを見張る塊

**AIに開発を任せると、機能の接点が4層に散っていても1層だけ直され、壊れた側は例外を出しません。**
それを機械で見張る塊です。Node の標準機能だけで動きます(外部依存ゼロ・`npm install` 不要)。

## どこから読むか(目的別)

| あなたの目的 | 読むもの | 分量 |
|---|---|---|
| **これが何か知りたい・評価したい** | **[SPEC.md](SPEC.md)** ── 何を満たし、**何を満たさないか** | 130行 |
| **なぜそう作られているか腹落ちしたい** | [METHOD.md](METHOD.md) ── 弱点6つ → 対策5層 | 130行 |
| **自分のプロジェクトに入れたい** | 下の「入れ方」→ あとはAIが [install.md](install.md) を読みます | 1行 |
| **実装の前に読む(AIへ)** | [RULES.md](RULES.md) ── 修繕の作法(条の数は `selfcheck` が数える) | 350行 |
| **なぜこの検査があるのか調べたい** | **[WHY_INDEX.md](WHY_INDEX.md)** ── 重み付きの索引。**◎(構造の芽)だけが全部読む価値がある** | |
| (全文が要るとき) | [WHY.md](WHY.md) ── 事故の記録。この塊の本体(**全部は読まなくてよい**) | 大 | 3000行超 |
| **定期監査をする** | [audit.md](audit.md) ── 4観点の手順 | 60行 |
| **「もっと良く出来ないか」を問う** | **[hunch.md](hunch.md)** ── 完了の手前で、編集していない目で見る(2段・約4分)。★これは自発性ではなく、外から駆動される監査 | 120行 |
| **何がいつ変わったか** | [CHANGELOG.md](CHANGELOG.md) | |

★**他のAIに理解してもらうなら [SPEC.md](SPEC.md) → [METHOD.md](METHOD.md) の順**が最短です。
SPEC だけだと「規約に書けばいいのでは?」と思われます ── それは この塊が最初に否定した道で、
なぜ注意ではなく機械なのかは METHOD にあります。

**このフォルダごとコピーすれば、他のプロジェクトでも動きます。**

会話型AIと組んで開発すると、AIは**最小の読み込みで改修**します。
その結果、機能の接点が4層に散っていても1層だけ直され、**壊れた側は例外を出さない**。
これを機械で見張るための道具と、実際に事故を起こして手に入れた作法を1つにまとめたものです。

| ファイル | 中身 |
|---|---|
| **WHY.md** | **各検査が生まれた事故**。この塊の本体。検査を足したら必ず1行足す |
| **RULES.md** | 積み上げた作法(推測しない・数える・見えない失敗を作らない…)。AIに読ませる |
| `check.mjs` | 検査エンジン。**どのプロジェクトでも同じ中身**。外から他所も診られる |
| **`verdict.mjs`** | **合否**。宣言された証拠を集めて4語(通過/注意/**不明**/差戻)に落とす。**不明を緑に数えない** |
| `hooks/stop.js` | 「できました」と言い終える**手前**で合否を回す。差戻なら止める |
| `selfcheck.mjs` | **その検査エンジンを検査する**。わざと壊した見本で赤くなることを確かめる＋塊の健康診断 |
| `hooks/codemap.js` | 編集の**直前**に、触った実名で地図を引いて該当項を差し込む |
| `hooks/clock.js` | 毎ターン時刻を注入 + 監査の催促(**実装が変わったときだけ**) |
| `audit.md` | 定期監査の手順(4観点) |
| **hunch.md** | **違和感の手順**(理由を書かせずに出させ、全部に掘る)。Guardian で唯一の**アクセル側** ── 他は全部ブレーキ |
| `templates/` | 地図・宣言・CLAUDE断片・フック登録の雛形 |
| `KIT_VERSION` / `CHANGELOG.md` | 配った版と変更の記録。配布先の番号と見比べて**どのコピーが古いか**を判断する |

---

## 入れ方 ── **このフォルダを置いて、AIに一言**

**人がやることは2つだけです。**

**① 塊を、相手のプロジェクトの `guardian/` に置く**

正本(GitHub)から取る ── PowerShell に貼る1行:

```powershell
npx degit ricordi-studio/guardian guardian
```

手元の別プロジェクトからコピーしても構いませんが、**そのコピーが最新とは限りません** ──
正本から取るのが確実です(2度目からは下の `pull.mjs` が1行で済ませます)。
**② そのプロジェクトのAIに、こう言う**:

> **guardian/install.md を読んで導入して。**

これで終わりです。`install.mjs` を回すのも、宣言を書くのも、地図を書き始めるのも**AIの仕事** ──
人が手で置くファイルも、貼る作業もありません。

★一緒に運ばれるもの: **事故の記録(WHY.md)と作法(RULES.md)** ── これがこの塊の本体です。
  中身は元のプロジェクトで実際に起きた事故なので固有名が出ますが、**そのままで構いません**
  (「なぜこの検査があるか」は、具体的であるほど次の人に効く)。
  **エンジン(コード)だけは、どの現場へ持って行っても同じ中身**であることを自己検査が見張っています。

AIの中で起きること:

| 誰が | 何を |
|---|---|
| `台帳.mjs`(機械) | ★導入が置いた物を「根の種類・相対名・指紋・書き手」で記録する。★★外すときに、持ち主を名前ではなく根で決めるため。★★★台帳に無い物は残す |
| `外す.mjs`(機械) | ★**外す** ── ★★素の走行は【下見】で何も消さない(`--dry` / `--check` も同じ)。★★★実際に消すのは `node guardian/外す.mjs --外す` ── **この塊で唯一の不可逆な口**。台帳と実物を突き合わせ、「塊が作った かつ 入れたときから変わっていない」物だけ外す。台帳に無い物は消さない。出口 0=PASS / 1=CONFLICT / 2=UNKNOWN / 3=引数の誤り |
| `外す.mjs --走査`(機械) | ★**昔の導入に当てる**(台帳が無い現場)── ★★消さない。候補と【証拠】を出すだけ(証拠は足すので、1件が複数 持ちます)。★★★`--root <道>` で外の現場、`--json` で機械が読む形。決めるのは人 |
| `install.mjs`(機械) | 地図の雛形を置く / **リポジトリを実際に見て**宣言の下地を書く / `.claude/settings.json` にフックを登録(**既存は壊さない**) / `CLAUDE.md` に読む順を追記 |
| AI(判断) | 「同じ意味の値が2箇所にある所」を探して宣言に書く / 触った機能から地図を書き始める / **わざと壊して落ちることを確かめる** |

## お願い ── **事故の記録を、送ってください**

この塊の本体は、コードではなく **`WHY.md` の事故の記録**です。
検査も規律も、全部「実際に踏んだ事故」から逆算して生まれています。
**事故が集まるほど、検査と規律が増えます。**

### なぜ、あなたの現場の事故に価値があるのか

**こちらの現場では、こちらで踏める石しか踏めないからです。**

実例: 導入スクリプトに「**フォルダ名に空白があると壊れる**」というバグがありました。
元の現場のパスに空白が無かったので、**一生出ませんでした**。
配布先(空白入りのフォルダ名)で初めて出て、直りました。

現場が増えるほど、踏む石の種類が増えます。それが配ることの見返りです。

### 送り方(1行 + 承認)

```bash
node guardian/selfcheck.mjs --why
```

その現場で `WHY.md` に**新しく足した事故だけ**を `guardian-why-report.md` にまとめます
(配られたときに何があったかを `WHY_SEEN` が覚えているので、増えた分だけが出ます)。

**中身を読んで、送ってよいと判断したら**、表示されるコマンドを叩いてください:

```bash
gh issue create --repo ricordi-studio/guardian --title "事故レポート: N件" --body-file guardian-why-report.md
```

`node guardian/selfcheck.mjs --why --send` なら、その場で送ります。

### ★勝手には送りません

- **自動送信はしません。**人が読んで、送ると決めたときだけ送られます
- **個人情報が混ざっていたら、1枚そのものを作りません。**
  見張る語は `guardian.config.json` の `private` に並べてください
  (例: 関係者の名前・社名・現場の名前)。**作ってから気をつけろ、では遅い**ので、
  混ざっていれば「WHY.md 側を伏せてから、もう一度」と言って止まります
- 宣言が空なら「**見張っていません**」と正直に言います(無音を安全と読ませないため)

### 何を書けば役に立つか

事故の書き方は `WHY.md` の既存の記録がそのまま見本です。効くのはこの3つ:

1. **何が起きたか**(症状ではなく、何を信じて何が違ったか)
2. **なぜ気づけなかったか** ← ここがいちばん価値があります
3. **どう直したか**(注意ではなく、検査・門・計器のどれにしたか)

**直せなかった事故も歓迎です。**「再現できなかった」「原因不明のまま」も記録の価値があります
── 分からなかったことを分からなかったと書けるのが、この塊の流儀です。

---

### ★配った先の直りを、元へ還す(改善の還流)

実際に起きた(2026-08-28): 配布先で塊自身のバグが2つ見つかり、向こうで直された。
**だが元の塊には戻ってこなかった。**しかも両方の版が同じ番号のまま ── 版が嘘をついていた。
そのうち1つは「フォルダ名に空白があると壊れる」という、**元の現場では一生出ないバグ**だった。

**現場が増えると、こちらの現場では踏めない石を踏む。**それが配ることの見返りであり、
戻ってこなければ見返りを捨てている。**配ったものは、戻る道を用意するまで配り終えていない。**

#### 正本(GitHub)

**https://github.com/ricordi-studio/guardian** ── ここが唯一の正本。
配布先はここから取り、直したらここへ還す。

```bash
node guardian/pull.mjs           # 正本の最新を取り直す(1行)
node guardian/pull.mjs --check   # 取らずに、正本と何が違うかだけ見る
```

★`pull.mjs` は取り直す前に **この現場で塊を直していないか**を必ず見る。
  直していたら止まる ── 上書きすると、その現場の直りが黙って消えるから。
  先に `--report` で1枚を作り、正本へ渡してから取り直すこと。

#### 道具

```bash
node guardian/selfcheck.mjs           # 配られたときの中身のままか(ファイルごとに照合)
git config core.hooksPath guardian/githooks   # Claude Code でない道具で、コミット前に合否を回す
node guardian/selfcheck.mjs --report  # 直した分を【元の塊へ渡す1枚】にまとめる(配布先)
node guardian/selfcheck.mjs --stamp   # 意図した変更として指紋を押し直す(元の塊)
```

#### 流れ(人の承認を挟む)

| # | 誰が | 何を |
|---|---|---|
| 1 | 配布先のAI | 塊を直したら `selfcheck` が **どのファイルを直したか名指しで**赤くする |
| 2 | 配布先のAI | `--report` で `guardian-report.md` を作る(変わったファイルの中身+渡し方の説明) |
| 3 | **人** | その1枚を読んで**承認**し、元の塊のAIへ渡す(貼るだけ・ファイルを渡すだけ) |
| 4 | 元の塊のAI | 差分を読んで取り込む(**丸ごと上書きしない** ── こちらの改善が消えるため) |
| 5 | 元の塊のAI | `--stamp` → 版を上げて CHANGELOG に書く → **配った先へ配り直す** |

★**3(人の承認)を機械にさせない。**中身がそのまま外へ出るので、
  何を送るかは人が見て決める ── 自動送信にすると、送ってはいけないものが混ざる日が来る。

★報告は**丸ごとの中身**を出す(差分ではない)。配布先は元の塊を持っていないので差分が作れない。
  受け取る側は**AIが読んで判断する** ── 機械が丸ごと上書きすると、元の改善が黙って消える。

★指紋の対象は**エンジン(コードとフック)だけ**。
  WHY.md / RULES.md は配布先で事故が増えて当然なので数えない。

#### サーバーは要るか

**いまは要らない。**同じPCの別フォルダなら、上の1枚をコピーで運べば足りる。
サーバー(または git の共有リポジトリ)が要るのは、**別のPC・他人へ配るようになったとき**。
そのときも仕組みは変わらない ── 運ぶ手段が「コピー」から「送信」に変わるだけで、
**1〜5の流れと、3の人の承認はそのまま**。

---

何が起きるか先に見たいときは:

```bash
node guardian/install.mjs --dry
```

**冪等です。** 二度実行しても、既にあるものには触りません。

---

## 使い方

```bash
node guardian/check.mjs                    # このリポジトリを検査
node guardian/check.mjs --tighten          # 口が減っていれば宣言の max を下げる(一方通行)
node guardian/selfcheck.mjs                # 塊そのものを検査(鍵もネットも要らない)
node guardian/verdict.mjs                  # 合否 ── 宣言した証拠を全部集めて4語に落とす
node guardian/verdict.mjs --fast           # 自足する証拠だけ(完了を名乗る手前・CI用)
node guardian/check.mjs --root ../other    # 他のリポジトリを外から検査(読むだけ)
node guardian/check.mjs --selectors a.html # 単発でセレクタだけ
```

CIに置くなら、出口コードが 1 になるのでそのまま落ちます。

---

## 夜中に回す(2段ある)

| | どこに置くか | コピーで付いてくるか |
|---|---|---|
| **①機械検査**(毎晩01:00) | `.github/workflows/guardian-nightly.yml` ← **install.mjs が置きます** | **○ 付いてくる** |
| **②観点を選んだ監査**(4観点) | **リポジトリの外**。AIの定期実行として登録する | **✗ 付いてこない** |

**①はクラウド(GitHub Actions)で回るので、PCの電源にもAIのセッションにも依存しません。**
赤くなったらIssueを立て、緑に戻ったら閉じます ── 見に行かなくても気づけます。

**②は人とAIの判断が要る**(重複・死にコード・メタ化余地を「実害か」で仕分ける)ので、機械にはできません。
ただし**回す手間はゼロに近づけてあります**。

- **誰でも1語で回せる**: `install.mjs` が `.claude/commands/guardian-audit.md` を置くので、
  そのリポジトリを開いた人は **`/guardian-audit`** と打つだけ(手順の正典は塊の中)
- **催促は自動**: `hooks/clock.js` が「前回から時間が経った**かつ**実装が変わった」ときだけ促す
- **完全に無人にしたいなら**、そのプロジェクトを対象にした定期実行をAI側に登録し、
  `guardian/audit.md` を読ませる(**アカウント側の設定なので塊には入れられません**)

## 外からできること / 中でしかできないこと

| | 外(パスを渡して診る) | 中(リポジトリに設置) |
|---|---|---|
| 記号の実在照合・不変条件・セレクタ | ◎ | ◎ |
| 毎晩の自動検査(CI) | ○ | ◎ |
| **編集の直前に地図を差し込む** | **✗ 不可能** | ◎ |
| **毎ターンの時刻注入・催促** | **✗ 不可能** | ◎ |

「編集の直前」を観測できるのは、**編集している主体(AIのランタイム)だけ**です。
ファイル監視では**書かれた後**にしか気づけず、MCPは**AIが呼びに来る道具**であって
こちらから押し込む口ではありません。

つまり **外 = 健康診断(事後に見つける) / 中 = 予防接種(事故の前に止める)**。
中に置くのが本筋で、外からの診断はおまけです。

---

## 配るときの約束

1. **失敗しても黙って通す。** フックが落ちると開発そのものが止まる
2. **プロジェクト固有のものを塊に入れない。** 他所で意味の無い催促・検査になる
3. **検査を足したら WHY.md に「どの事故を防ぐか」を書く**。種類(kind)を足したら `selfcheck.mjs` の見本にも足す
   ── 一度も試されていない種類があると、自己検査が落ちます
4. **配る前に `node guardian/selfcheck.mjs` を回す。**版・件数の嘘もここで捕まります
5. **双子のいない門を足さない。** 門は黙って死ねる。**同じ宣言を読む検査**が書いた後に落とすから、黙って通してよい
6. **誤検出を出さない。** 1件の誤検出が、検査全体の信用を殺す

### ★★★この道具に【出来ない事】── 台帳が無い現場の 区間(27.31、2026-09-04)

★**台帳が無い現場では、区間だけを 抜く道が 在りません。**(★★叩いて確かめた・2通りとも)

```
①台帳の道(--外す)   … ★出口2「所有台帳が読めません」── ★★何も消さない
★★②名指しの道       … ★★★出口1「この道には区間が在ります ── 丸ごとは消せません」
```

★**どちらも 正しい振る舞いです** ── ①は 材料が無い / ②は 現場の文まで 消えるから。
★★ですが **結果として、区間は 道具では 抜けません**。

★★★**手で抜く時の段**(@codex 01:02 ── ★手で抜くのも【不可逆】なので):

```
① ★写しを取る(★★.git が無い現場も在る ── 戻せない事が 在る)
   cp CLAUDE.md CLAUDE.md.控え
② ★★前の指紋を取る … node -e "…ファイルの大きさと 行数を 出す…"
③ ★★★対を 目で確かめる:
   ・install の【全文ラベル】が在るか(接頭辞だけの物は 別の道具の区間かもしれない)
   ・その後ろに <!-- guardian:end --> が在るか(対で 揃っているか)
   ・★区間の【外】に 人が書いた行が在るか(在れば それは 残す物)
④ 区間だけ消す … begin の行から end の行まで(★★その外は 1バイトも 触らない)
⑤ ★★★後の指紋を取り、差分を見る:
   git diff -- CLAUDE.md   (git が在る現場)
   diff CLAUDE.md.控え CLAUDE.md  (★git が無い現場)
   → ★消えた行が【区間の中だけ】か を 目で 確かめる
⑥ ★★確かめ終えたら 控えを消す(★★★確かめる前に 消さない)
```

★**④だけを やって ①②⑤を 飛ばさないこと** ── ★★区間の外を 1行 消しても、
★★★機械は もう 何も言いません(この道具は そこを 見ていません)。

★★元の一行:
★★★**手で抜いてください**:

```
<!-- guardian:begin … --> から <!-- guardian:end --> までを 消す(その外は 触らない)
```

★これを書いていないと、次の人は ★★出口2 を見て【壊れている】と読みます ──
★★★出来ない事を 書かないのは、**黙って落ちる**のと 同じです。

### ★★★この道具に【出来ない事】その2 ── ★塊の束(`guardian/`)は 消さない(27.39、2026-09-04)

```
★--外す が PASS を出しても、★★塊の束(guardian/ ── pull.mjs と KIT_VERSION を持つフォルダ)は 残ります。
★★★道具は フォルダごと 消しません ── ★消すのは 人の手です(確かめてから)。
```

★**だから「残滓ゼロ」は【束の 外】について の言葉です。**
★★束を 含めれば ゼロでは ありません ── ★★★判定の文も そう言います(点数つきで 毎回 数えます)。

★点数を ここに 書きません ── ★★束の点数は【どう置いたか】で 変わるからです:

```
★配る宣言どおりに 置いた束 … 40点ほど(@claude の見本)
★★clone / degit で 丸ごと 落とした束 … 160点ほど(@kozo の見本)
★★★install 自身が 警告しています ── 「degit / clone は 宣言を読まないので 丸ごと落ちてくる」
```

★★**数は 紙ではなく【走行】が 出します** ── 判定の文が その現場の 実測を 言います。

