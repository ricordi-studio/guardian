# EXP-002 第1回 ── 段0 と 対照(2026-08-29)

同じコード・同じモデル。**問いの形だけが違う2体**を、互いを知らせずに走らせた。

| | 問い | 仕込んだ6件のうち |
|---|---|---|
| **段0**(0体・読むだけ) | 「いま一から書くなら、こういう形になるか?」 | **6 / 6** |
| **対照**(変更モード) | 「ギフト包装の種別を足して」 | **1 / 6** |

## 段0 ── 6件すべてを、場所と理由つきで名指しした

| 仕込み | 段0の指摘 |
|---|---|
| D1 税率が2箇所 | 項目3。`calc.js:15,19,23,28` と `format.js:14-17`。「計算だけ直って**レシートの表記が古いまま**になる。印字が嘘をつくだけなので目視でも気づきにくい」 |
| D2 種別の分岐が並ぶ | 項目4。**答えより鋭い** ──「実際に分岐が意味を持つのは `coupon` の `Math.ceil` **一点だけ**。本当の軸は種別ではなく【税率】と【丸め方向】の2つ」 |
| D3 マジックナンバー | 項目10。`5000` も `0.1/0.08` も名前を持たない |
| D4 丸めの写経 | 項目6。**こちらが数え落としていた `format.js:23` の `Math.floor` も拾った**(表示層が金額を丸めている) |
| D5 送料判定の二重 | 項目1(最重要として先頭)。「1箇所直して動作確認して終わりに見えて、API経由の合計だけ旧ラインのまま残る」 |
| D6 LABELSに coupon が無い | 項目5。「3箇所が別々に列挙していて、**しかも列挙内容が食い違っている**(LABELSだけ3種、他は4種)」 |

★**仕込んでいないものを、さらに4件見つけた**: パイプラインの二重(`receipt`/`totalOf`)/ 割引が
`subtotal` と `discount` の二重表現 + 丸めの契約が項目ごとに違う / `lineSubtotal` が
ほぼ使われず `price*qty` が3箇所で手書き / 桁揃えが文字列リテラルの空白。
さらに「**無料化を『行の削除』で実装しているので、『送料 ¥0(無料)』が構造的に出せない**」という
設計上の指摘まで出した。

★最後に**10項目を3つの根に畳んだ**(写経 / 種別の集合が未定義 / 層の境界が未決定)。
**畳む動作が、頼まれていないのに起きている。**

## 対照 ── 全部の壊れに触れて、1つだけ指摘した

3箇所を手で編集した(`lineTax` に5本目の `if` / `taxNote` に4本目の `if` / `LABELS` に1行)。
**これはD2の存在証明**である ── 種別を1つ足すのに3箇所を直した。

そのうえで:

| 仕込み | 対照 |
|---|---|
| D1 | **その場所を編集した**(`taxNote` に `'(税10%)'` を追加)。指摘なし |
| D2 | **その場所を編集した**(5本目の分岐)。指摘なし |
| D3 | **その場所に `0.1` を書き足した**。指摘なし |
| D4 | **その場所に `Math.floor` を書き足した**。指摘なし |
| D5 | **編集していない**。→ **唯一、明示的に指摘した**(場所・帰結つき) |
| D6 | **その場所を編集した**(`LABELS` に1行)。罠は踏まなかったが、defectとしては挙げず |

## ★この回の発見

> **編集した箇所の構造は1つも指摘せず、編集しなかった箇所の構造だけを指摘した。**

対照は6つの壊れのうち**5つの現場に手を入れている**。見ていないのではなく、
**編集している対象は判断の対象にならない**。

## 予測が外れた点(記録)

回す前の予測は「対照は0件」だった。**実際は1件**。
変更モードは構造に盲目なのではなく、**自分が編集している所にだけ盲目**だった。
この差は仮説の精度を上げるので、外れたことに価値がある。

## 交絡(正直に)

段0は「問題を探せ」、対照は「変更しろ」── **問いの形とタスクの種類が分離できていない。**
「探せと言われた方が見つけるのは当たり前」という批判は正しい。

ただし実務では**タスクは常に「変更しろ」**なので、実用上の結論は変わらない:
**変更タスクを与えられている限り、構造は見えない。だから別に問う工程が要る。**

## 次に効く判断

**検出には重い装置が要らない。** 段0(0体・追加コストほぼゼロ)で6/6。
仕様書も複数実装も収束も、**検出のためには不要**だった。

→ 残る問いは「**では重い段は何のために要るのか**」。
仮説: **検出は軽い。どれを直すか・どんな形にするかの【決定】が重い**
(段0は10件出したが、どれから手を付けるかは言っていない)。
次はそこを測る。
