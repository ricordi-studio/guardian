# 違和感 ── 完了を名乗る手前で、編集していない目で見る

★**これは自発性ではありません。** 外から駆動される監査です。
道具は自分から「なんかおかしい」と言い出しません ── 呼ばれたときに、上手に言うだけです。
**気づくべきだと気づくのは、いまも人の仕事です。**(承知のうえで低い段を選んでいる・RULES 46条の精神)

## なぜ在るか

Guardian の機構は、これまで**全部ブレーキ**だった ── 合否が止め、門が拒み、ラチェットが戻らせない。
**「もっと良くできないか」を問う側(アクセル)が1つも無かった。**

そして実測で分かったこと(`research/lab/FINDINGS.md`):

> **編集している対象は、判断の対象にならない。**
> 変更を依頼された1体は、6つの壊れのうち**5つの現場に手を入れて**、
> 指摘したのは**編集しなかった1件だけ**だった(1/6)。
> 同じコードに「いま一から書くなら?」と問うた1体は **6/6**。

**同じモデル・同じコード。問いの形だけが違う。**

---

## 手順(2段)

### 段1 ── 違和感を出す(理由を書かせない・10秒)

**変更を一切見ていない**1体に、対象のファイルを読ませて、こう問う:

> このコードを読んで感じた **違和感だけ** を並べてください。
> **★理由を書かないでください。** 説明も根拠も要りません。「なんか変」で終わってください。
> - **1項目 20字以内**
> - 各項目に **確信度(0〜100%)** だけ添える
> - **確信度が低いものこそ書いてください。20% でも構いません**
> - 思いついた順に。**整理も分類もしないこと**
> - 「無い」と思ったら「無い」で構いません

★**理由を禁じるのが要。**理由を要求すると件数が半分になり、低い帯が消える(実測: 10〜11件 → 20〜23件)。
★独立した2体を走らせると、**確信度の数字まで一致する**(65/65・55/55・30/30・20/20)。**雑音ではない。**

### 段2 ── サーチライトを当てる(掘る・4分)

段1で出た項目を **全部**、別の1体に渡して確かめさせる:

> 別の人が確信の無いまま挙げた引っかかりです。1つずつ確かめてください。
> - **実在するか / しないか**
> - 実在するなら: どういう条件で起きるか / 起きたら何が困るか
> - **可能なら実際に動かして再現する**
>
> ★**勘違いが混じっています。実在しないものは「実在しない」とはっきり書いてください。**
> 無理に問題として説明しないこと。**否定することも成果です。**

★**確信度で選り分けない。全部当てる。**
実測: 30%の帯に**本物と偽物が両方**居た。確信度も、2体の一致も、真偽を並べない。

★**「実在しない」と書かせる一文を必ず入れる。**
入れずに掘らせると、何にでも理由を付ける道具になる。実測では、
わざと混ぜた誤りを含む**7件中3件を正しく否定**した。

---

## ★実測で分かった、いちばん大事なこと

> **違和感は「主張」ではなく「座標」である。**

外れた違和感の**隣に**、本物があった:

- 「`to` と `from` が同じだったら」── **外れ**。だがその隣に、
  **配布元と配布先に同じ根を渡すと実行のたびにファイルが不可逆に汚れる**(104→204→304バイト)
- 「エラーが message だけ」── 当たり。掘ったら**もっと悪いもの**が出た
  (失敗を報告している途中で道具自身が落ち、**残りの報告が全部消える**)

**思いつきが正しい必要はない。掘る価値のある場所を指していればよい。**
だから「確信度で絞れない」は欠点ではなく、**絞る必要が無い**ということ。

---

## ★運用の文脈を渡さないと、運用の欠落は永久に見えない

**これが最大の落とし穴。**

壊れが全部「無いもの」の題材で測った結果:

| 問い方 | 6件のうち |
|---|---|
| 「一から書くなら?」 | **3/6** |
| 「何が起きたら困る?」 | **3/6**(しかも**同じ3件**を見つけ、**同じ3件**を見逃した) |
| 理由なし・確信度つき ×2体 | **3/6**(やはり同じ3件) |
| **「一から書くなら?」+ 運用の文脈4行** | **5〜6/6** |

**問いをどう変えても1ミリも動かず、文脈を足したら動いた。**
しかも足した文脈と出たものが**1対1で対応**した(数百MB→上限 / cron5分→錠 / 5人・2年→記録)。

★**文脈は判断を精密にする。心配性にするのではない** ── 文脈ありの回は
「この道具は**小さなテキストを1人が手で閉じた場所へ写す前提でなら妥当**」と、
批判の範囲を明示的に絞った。

だから宣言に **`context`** を書く:

```json
"context": [
  "共有サーバーの cron で5分ごとに動く(人が手でも叩く)",
  "台帳は5人が編集する",
  "扱うファイルは数KB〜数百MB",
  "配布先は社外に公開される",
  "運用が2年続いている"
]
```

★**書かなければ、上限も錠も記録も永久に出ない。** そして道具は
「**運用の文脈が宣言されていないので、運用の欠落は見ていません**」と正直に言う
(無音を安全と読ませないため)。

★これは**人にも同じ制約**である ── 運用を知らない人が同じコードを見ても、錠は思いつかない。
**サーチライトは、まず広く見たその広さの中しか照らせない。**

---

## いつ回すか

- **完了を名乗る手前**(合否が通ったあと、報告する前)
- **監査のとき**(`audit.md` の観点0)
- ★**毎回の編集では回さない。** 鳴り過ぎる計器は読み飛ばされる(RULES 7条)

## 費用(実測)

```
段1  10秒 → 20件の違和感
段2  4分  → 7件を決着(実在4・否定3)+ 掘る過程で新発見2件
```

## この手順が【できないこと】

- **自分から始めない。** 引き金は必ず外(人が呼ぶか、完了の瞬間)
- **宣言された文脈の外には届かない。** 文脈を書くのは人の仕事
- **経験で広くならない。** 同じ現場で何年やっても、広さは宣言された分だけ
- **違和感を持ち歩かない。** 確信度は問われた瞬間に作られ、ターンが終われば消える

★最後の1つを埋める(**保留の置き場**と**浮上**)は、**まだ測っていないので作っていない。**
測る前に作れば、この塊が何度も踏んだ「効かないものを作る」をまた踏む。
